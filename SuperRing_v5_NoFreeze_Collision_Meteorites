--[[
    ═══════════════════════════════════════════════════════════════════════════
    Yin yang
    ═══════════════════════════════════════════════════════════════════════════
]]

print("\n" .. string.rep("=", 80))
print("EVADE v5.2 BETA - FRONTAL JUMP + BETA NEW")
print(string.rep("=", 80))

--// SERVICES
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local Debris = game:GetService("Debris")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalizationService = game:GetService("LocalizationService")

if not LocalPlayer then
    error("ERROR: No local player available")
    return
end

--// ═════════════════════════════════════════════════════════════════════════════
--// SAE 24H KEY SYSTEM — el resto de la beta no se inicializa sin validación
--// SAE 24H KEY SYSTEM — the rest of the beta does not initialize without validation
--// ═════════════════════════════════════════════════════════════════════════════

local KEY_PORTAL_URL = "https://yinkeysys-2xyu9dbe.manus.space"
local KEY_VALIDATE_URL = KEY_PORTAL_URL .. "/api/validate?key="
local KEY_CACHE_FILE = "SAE_EvadeBeta_Key.txt"
local KeyHttpService = game:GetService("HttpService")
local KeyTweenService = game:GetService("TweenService")

--// Paleta exclusiva Yin–Yang: blanco, negro y grises, independiente del tema principal.
local KeyVisualTheme = {
    Background = Color3.fromRGB(7, 7, 7),
    Surface = Color3.fromRGB(20, 20, 20),
    Header = Color3.fromRGB(12, 12, 12),
    Text = Color3.fromRGB(248, 248, 248),
    TextDim = Color3.fromRGB(165, 165, 165),
    Accent = Color3.fromRGB(255, 255, 255),
    Borders = {Color3.fromRGB(80, 80, 80), Color3.fromRGB(255, 255, 255), Color3.fromRGB(128, 128, 128), Color3.fromRGB(232, 232, 232)},
}

--// KEY LANGUAGE SYSTEM — detecta el locale del jugador con Roblox LocalizationService.
--// Las traducciones son locales para que la ventana siga funcionando aunque el
--// servicio de traducción no esté disponible. Idiomas no incluidos usan inglés.
local KeyLanguage = "en"
local KeyLanguageTranslations = {
    en = {
        brand_sub = "EVADE BETA • 24H KEY", title = "KEY SYSTEM", subtitle = "Personal 24-hour access",
        check = "CHECK KEY", link = "GET LINK", secure = "Secure server verification", checking = "…  CHECKING",
        revalidating = "REVALIDATING SAVED KEY", contacting = "CONTACTING SERVER", accepted = "KEY ACCEPTED",
        loading = "LOADING BETA", access_granted = "ACCESS GRANTED", invalid_format = "INVALID FORMAT",
        server_unavailable = "SERVER UNAVAILABLE", invalid_response = "INVALID RESPONSE", invalid_expired = "INVALID OR EXPIRED KEY",
        saved_removed = "SAVED KEY REMOVED", link_copied = "LINK COPIED", portal = "PORTAL",
        no_file_support = "YOUR EXECUTOR HAS NO FILE SUPPORT",
    },
    es = {
        brand_sub = "BETA DE EVADE • CLAVE 24H", title = "SISTEMA DE CLAVE", subtitle = "Acceso personal de 24 horas",
        check = "VERIFICAR CLAVE", link = "OBTENER ENLACE", secure = "Verificación segura del servidor", checking = "…  VERIFICANDO",
        revalidating = "REVALIDANDO CLAVE GUARDADA", contacting = "CONTACTANDO AL SERVIDOR", accepted = "CLAVE ACEPTADA",
        loading = "CARGANDO BETA", access_granted = "ACCESO CONCEDIDO", invalid_format = "FORMATO INVÁLIDO",
        server_unavailable = "SERVIDOR NO DISPONIBLE", invalid_response = "RESPUESTA INVÁLIDA", invalid_expired = "CLAVE INVÁLIDA O EXPIRADA",
        saved_removed = "CLAVE GUARDADA ELIMINADA", link_copied = "ENLACE COPIADO", portal = "PORTAL",
        no_file_support = "TU EXECUTOR NO TIENE SOPORTE DE ARCHIVOS",
    },
    pt = {
        brand_sub = "EVADE BETA • CHAVE 24H", title = "SISTEMA DE CHAVE", subtitle = "Acesso pessoal de 24 horas",
        check = "VERIFICAR CHAVE", link = "OBTER LINK", secure = "Verificação segura do servidor", checking = "…  VERIFICANDO",
        revalidating = "REVALIDANDO CHAVE SALVA", contacting = "CONTATANDO O SERVIDOR", accepted = "CHAVE ACEITA",
        loading = "CARREGANDO BETA", access_granted = "ACESSO CONCEDIDO", invalid_format = "FORMATO INVÁLIDO",
        server_unavailable = "SERVIDOR INDISPONÍVEL", invalid_response = "RESPOSTA INVÁLIDA", invalid_expired = "CHAVE INVÁLIDA OU EXPIRADA",
        saved_removed = "CHAVE SALVA REMOVIDA", link_copied = "LINK COPIADO", portal = "PORTAL",
        no_file_support = "SEU EXECUTOR NÃO TEM SUPORTE A ARQUIVOS",
    },
    fr = {
        brand_sub = "EVADE BETA • CLÉ 24H", title = "SYSTÈME DE CLÉ", subtitle = "Accès personnel de 24 heures",
        check = "VÉRIFIER LA CLÉ", link = "OBTENIR LE LIEN", secure = "Vérification sécurisée du serveur", checking = "…  VÉRIFICATION",
        revalidating = "REVALIDATION DE LA CLÉ ENREGISTRÉE", contacting = "CONTACT DU SERVEUR", accepted = "CLÉ ACCEPTÉE",
        loading = "CHARGEMENT DE LA BETA", access_granted = "ACCÈS ACCORDÉ", invalid_format = "FORMAT INVALIDE",
        server_unavailable = "SERVEUR INDISPONIBLE", invalid_response = "RÉPONSE INVALIDE", invalid_expired = "CLÉ INVALIDE OU EXPIRÉE",
        saved_removed = "CLÉ ENREGISTRÉE SUPPRIMÉE", link_copied = "LIEN COPIÉ", portal = "PORTAIL",
        no_file_support = "VOTRE EXECUTOR NE PREND PAS EN CHARGE LES FICHIERS",
    },
    de = {
        brand_sub = "EVADE BETA • 24H SCHLÜSSEL", title = "SCHLÜSSELSYSTEM", subtitle = "Persönlicher 24-Stunden-Zugang",
        check = "SCHLÜSSEL PRÜFEN", link = "LINK ERHALTEN", secure = "Sichere Serverüberprüfung", checking = "…  WIRD GEPRÜFT",
        revalidating = "GESPEICHERTEN SCHLÜSSEL PRÜFEN", contacting = "SERVER WIRD KONTAKTIERT", accepted = "SCHLÜSSEL AKZEPTIERT",
        loading = "BETA WIRD GELADEN", access_granted = "ZUGRIFF GEWÄHRT", invalid_format = "UNGÜLTIGES FORMAT",
        server_unavailable = "SERVER NICHT VERFÜGBAR", invalid_response = "UNGÜLTIGE ANTWORT", invalid_expired = "UNGÜLTIGER ODER ABGELAUFENER SCHLÜSSEL",
        saved_removed = "GESPEICHERTER SCHLÜSSEL ENTFERNT", link_copied = "LINK KOPIERT", portal = "PORTAL",
        no_file_support = "DEIN EXECUTOR UNTERSTÜTZT KEINE DATEIEN",
    },
    it = {
        brand_sub = "EVADE BETA • CHIAVE 24H", title = "SISTEMA CHIAVE", subtitle = "Accesso personale di 24 ore",
        check = "VERIFICA CHIAVE", link = "OTTIENI LINK", secure = "Verifica sicura del server", checking = "…  VERIFICA",
        revalidating = "RIVERIFICA CHIAVE SALVATA", contacting = "CONTATTO CON IL SERVER", accepted = "CHIAVE ACCETTATA",
        loading = "CARICAMENTO BETA", access_granted = "ACCESSO CONSENTITO", invalid_format = "FORMATO NON VALIDO",
        server_unavailable = "SERVER NON DISPONIBILE", invalid_response = "RISPOSTA NON VALIDA", invalid_expired = "CHIAVE NON VALIDA O SCADUTA",
        saved_removed = "CHIAVE SALVATA RIMOSSA", link_copied = "LINK COPIATO", portal = "PORTALE",
        no_file_support = "IL TUO EXECUTOR NON SUPPORTA I FILE",
    },
    ru = {
        brand_sub = "EVADE BETA • КЛЮЧ НА 24 Ч", title = "СИСТЕМА КЛЮЧА", subtitle = "Персональный доступ на 24 часа",
        check = "ПРОВЕРИТЬ КЛЮЧ", link = "ПОЛУЧИТЬ ССЫЛКУ", secure = "Безопасная проверка сервера", checking = "…  ПРОВЕРКА",
        revalidating = "ПОВТОРНАЯ ПРОВЕРКА СОХРАНЁННОГО КЛЮЧА", contacting = "СВЯЗЬ С СЕРВЕРОМ", accepted = "КЛЮЧ ПРИНЯТ",
        loading = "ЗАГРУЗКА БЕТА", access_granted = "ДОСТУП РАЗРЕШЁН", invalid_format = "НЕВЕРНЫЙ ФОРМАТ",
        server_unavailable = "СЕРВЕР НЕДОСТУПЕН", invalid_response = "НЕВЕРНЫЙ ОТВЕТ", invalid_expired = "КЛЮЧ НЕДЕЙСТВИТЕЛЕН ИЛИ ИСТЁК",
        saved_removed = "СОХРАНЁННЫЙ КЛЮЧ УДАЛЁН", link_copied = "ССЫЛКА СКОПИРОВАНА", portal = "ПОРТАЛ",
        no_file_support = "ВАШ EXECUTOR НЕ ПОДДЕРЖИВАЕТ ФАЙЛЫ",
    },
    ja = {
        brand_sub = "EVADE BETA • 24時間キー", title = "キーシステム", subtitle = "24時間の個人アクセス",
        check = "キーを確認", link = "リンクを取得", secure = "安全なサーバー検証", checking = "…  確認中",
        revalidating = "保存されたキーを再確認中", contacting = "サーバーに接続中", accepted = "キーが承認されました",
        loading = "ベータを読み込み中", access_granted = "アクセスが許可されました", invalid_format = "形式が無効です",
        server_unavailable = "サーバーを利用できません", invalid_response = "無効な応答です", invalid_expired = "無効または期限切れのキー",
        saved_removed = "保存されたキーを削除しました", link_copied = "リンクをコピーしました", portal = "ポータル",
        no_file_support = "お使いのExecutorはファイルに対応していません",
    },
    ko = {
        brand_sub = "EVADE BETA • 24시간 키", title = "키 시스템", subtitle = "24시간 개인 액세스",
        check = "키 확인", link = "링크 받기", secure = "안전한 서버 확인", checking = "…  확인 중",
        revalidating = "저장된 키 재확인 중", contacting = "서버에 연결 중", accepted = "키 승인됨",
        loading = "베타 로드 중", access_granted = "접근 허용됨", invalid_format = "잘못된 형식",
        server_unavailable = "서버를 사용할 수 없음", invalid_response = "잘못된 응답", invalid_expired = "유효하지 않거나 만료된 키",
        saved_removed = "저장된 키 삭제됨", link_copied = "링크 복사됨", portal = "포털",
        no_file_support = "사용 중인 Executor는 파일을 지원하지 않습니다",
    },
    zh = {
        brand_sub = "EVADE BETA • 24小时密钥", title = "密钥系统", subtitle = "24小时个人访问",
        check = "验证密钥", link = "获取链接", secure = "安全服务器验证", checking = "…  验证中",
        revalidating = "重新验证已保存的密钥", contacting = "正在连接服务器", accepted = "密钥已通过",
        loading = "正在加载测试版", access_granted = "已获得访问权限", invalid_format = "格式无效",
        server_unavailable = "服务器不可用", invalid_response = "响应无效", invalid_expired = "密钥无效或已过期",
        saved_removed = "已删除保存的密钥", link_copied = "链接已复制", portal = "门户",
        no_file_support = "你的Executor不支持文件",
    },
}

local function getKeyLanguage(localeId)
    local language = string.lower(tostring(localeId or "en")):match("^(%a+)") or "en"
    return KeyLanguageTranslations[language] and language or "en"
end

local function keyText(key)
    local languageTable = KeyLanguageTranslations[KeyLanguage] or KeyLanguageTranslations.en
    return languageTable[key] or KeyLanguageTranslations.en[key] or key
end

function getKeyVisualTheme()
    return KeyVisualTheme
end

function addYinSweep(strokeObject, borderColors, duration)
    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, borderColors[1]),
        ColorSequenceKeypoint.new(0.34, borderColors[2]),
        ColorSequenceKeypoint.new(0.67, borderColors[3]),
        ColorSequenceKeypoint.new(1, borderColors[4]),
    })
    gradient.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.36),
        NumberSequenceKeypoint.new(0.5, 0),
        NumberSequenceKeypoint.new(1, 0.36),
    })
    gradient.Offset = Vector2.new(-1.5, 0)
    gradient.Parent = strokeObject

    KeyTweenService:Create(
        gradient,
        TweenInfo.new(duration or 1.8, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1, false),
        {Offset = Vector2.new(1.5, 0)}
    ):Play()

    KeyTweenService:Create(
        strokeObject,
        TweenInfo.new((duration or 1.8) + 0.35, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true),
        {Transparency = 0.02}
    ):Play()
end

function normalizeAccessKey(value)
    return tostring(value or ""):gsub("%s+", ""):upper()
end

function isAccessKeyFormat(value)
    return value:match("^KEY_%S+$") ~= nil or value:match("^FREE_%S+$") ~= nil
end

function hasKeyCacheSupport()
    return type(isfile) == "function" and type(readfile) == "function" and type(writefile) == "function" and type(delfile) == "function"
end

function loadCachedKey()
    if not hasKeyCacheSupport() then
        return nil
    end

    local ok, cached = pcall(function()
        if isfile(KEY_CACHE_FILE) then
            return normalizeAccessKey(readfile(KEY_CACHE_FILE))
        end
    end)

    if ok and isAccessKeyFormat(cached or "") then
        return cached
    end

    return nil
end

function saveCachedKey(key)
    if hasKeyCacheSupport() then
        pcall(writefile, KEY_CACHE_FILE, normalizeAccessKey(key))
    end
end

function clearCachedKey()
    if hasKeyCacheSupport() then
        pcall(function()
            if isfile(KEY_CACHE_FILE) then
                delfile(KEY_CACHE_FILE)
            end
        end)
    end
end

local PB_SERVICE = 29573
local PB_SECRET = "cf8f925ba913c88480c765f911fa315f49916616a2492b2dc87aa0c8be14449e"
local PB_USE_NONCE = true
local PB_REQUEST = nil
local PB_REQUEST_SOURCE = nil
do
    local candidates = {
        {"syn.request", syn and type(syn.request)=="function" and syn.request},
        {"http.request", http and type(http.request)=="function" and http.request},
        {"http_request", type(http_request)=="function" and http_request},
        {"request", type(request)=="function" and request},
        {"fluxus.request", fluxus and type(fluxus.request)=="function" and fluxus.request},
        {"krnl.request", krnl and type(krnl.request)=="function" and krnl.request},
        {"electron.request", electron and type(electron.request)=="function" and electron.request},
    }
    for _, entry in ipairs(candidates) do
        if entry[2] then
            PB_REQUEST = entry[2]
            PB_REQUEST_SOURCE = entry[1]
            break
        end
    end
end

-- SHA-256 tomado tal cual de Boost_lua.txt (libreria oficial de PlatoBoost).
-- Solo expone PB_DIGEST hacia afuera; el resto queda en su propio scope.
local PB_DIGEST
do
local a=2^32;local b=a-1;local function c(d,e)local f,g=0,1;while d~=0 or e~=0 do local h,i=d%2,e%2;local j=(h+i)%2;f=f+j*g;d=math.floor(d/2)e=math.floor(e/2)g=g*2 end;return f%a end;local function k(d,e,l,...)local m;if e then d=d%a;e=e%a;m=c(d,e)if l then m=k(m,l,...)end;return m elseif d then return d%a else return 0 end end;local function n(d,e,l,...)local m;if e then d=d%a;e=e%a;m=(d+e-c(d,e))/2;if l then m=n(m,l,...)end;return m elseif d then return d%a else return b end end;local function o(p)return b-p end;local function q(d,r)if r<0 then return lshift(d,-r)end;return math.floor(d%2^32/2^r)end;local function s(p,r)if r>31 or r<-31 then return 0 end;return q(p%a,r)end;local function lshift(d,r)if r<0 then return s(d,-r)end;return d*2^r%2^32 end;local function t(p,r)p=p%a;r=r%32;local u=n(p,2^r-1)return s(p,r)+lshift(u,32-r)end;local v={0x428a2f98,0x71374491,0xb5c0fbcf,0xe9b5dba5,0x3956c25b,0x59f111f1,0x923f82a4,0xab1c5ed5,0xd807aa98,0x12835b01,0x243185be,0x550c7dc3,0x72be5d74,0x80deb1fe,0x9bdc06a7,0xc19bf174,0xe49b69c1,0xefbe4786,0x0fc19dc6,0x240ca1cc,0x2de92c6f,0x4a7484aa,0x5cb0a9dc,0x76f988da,0x983e5152,0xa831c66d,0xb00327c8,0xbf597fc7,0xc6e00bf3,0xd5a79147,0x06ca6351,0x14292967,0x27b70a85,0x2e1b2138,0x4d2c6dfc,0x53380d13,0x650a7354,0x766a0abb,0x81c2c92e,0x92722c85,0xa2bfe8a1,0xa81a664b,0xc24b8b70,0xc76c51a3,0xd192e819,0xd6990624,0xf40e3585,0x106aa070,0x19a4c116,0x1e376c08,0x2748774c,0x34b0bcb5,0x391c0cb3,0x4ed8aa4a,0x5b9cca4f,0x682e6ff3,0x748f82ee,0x78a5636f,0x84c87814,0x8cc70208,0x90befffa,0xa4506ceb,0xbef9a3f7,0xc67178f2}local function w(x)return string.gsub(x,".",function(l)return string.format("%02x",string.byte(l))end)end;local function y(z,A)local x=""for B=1,A do local C=z%256;x=string.char(C)..x;z=(z-C)/256 end;return x end;local function D(x,B)local A=0;for B=B,B+3 do A=A*256+string.byte(x,B)end;return A end;local function E(F,G)local H=64-(G+9)%64;G=y(8*G,8)F=F.."\128"..string.rep("\0",H)..G;assert(#F%64==0)return F end;local function I(J)J[1]=0x6a09e667;J[2]=0xbb67ae85;J[3]=0x3c6ef372;J[4]=0xa54ff53a;J[5]=0x510e527f;J[6]=0x9b05688c;J[7]=0x1f83d9ab;J[8]=0x5be0cd19;return J end;local function K(F,B,J)local L={}for M=1,16 do L[M]=D(F,B+(M-1)*4)end;for M=17,64 do local N=L[M-15]local O=k(t(N,7),t(N,18),s(N,3))N=L[M-2]L[M]=(L[M-16]+O+L[M-7]+k(t(N,17),t(N,19),s(N,10)))%a end;local d,e,l,P,Q,R,S,T=J[1],J[2],J[3],J[4],J[5],J[6],J[7],J[8]for B=1,64 do local O=k(t(d,2),t(d,13),t(d,22))local U=k(n(d,e),n(d,l),n(e,l))local V=(O+U)%a;local W=k(t(Q,6),t(Q,11),t(Q,25))local X=k(n(Q,R),n(o(Q),S))local Y=(T+W+X+v[B]+L[B])%a;T=S;S=R;R=Q;Q=(P+Y)%a;P=l;l=e;e=d;d=(Y+V)%a end;J[1]=(J[1]+d)%a;J[2]=(J[2]+e)%a;J[3]=(J[3]+l)%a;J[4]=(J[4]+P)%a;J[5]=(J[5]+Q)%a;J[6]=(J[6]+R)%a;J[7]=(J[7]+S)%a;J[8]=(J[8]+T)%a end;local function Z(F)F=E(F,#F)local J=I({})for B=1,#F,64 do K(F,B,J)end;return w(y(J[1],4)..y(J[2],4)..y(J[3],4)..y(J[4],4)..y(J[5],4)..y(J[6],4)..y(J[7],4)..y(J[8],4))end;
    PB_DIGEST = Z
end

local function pbGenerateNonce()
    local str = ""
    for _ = 1, 16 do
        str = str .. string.char(math.random(97, 122))
    end
    return str
end

-- api.platoboost.com es el host primario y api.platoboost.net el respaldo oficial
-- (ambos vienen en Boost_lua.txt). Antes esto no tenia pcall: si el DNS del
-- primario fallaba, la excepcion no se atajaba y tiraba todo el script abajo
-- (el "HttpError: DnsResolve" que viste). Ahora se prueba cada host y se cae
-- al siguiente si el primero no responde.
local PB_HOST = "https://api.platoboost.com"
if PB_REQUEST then
    for _, candidate in ipairs({ "https://api.platoboost.com", "https://api.platoboost.net" }) do
        local ok, response = pcall(PB_REQUEST, {
            Url = candidate .. "/public/connectivity",
            Method = "GET",
        })
        if ok and response and (response.StatusCode == 200 or response.StatusCode == 429) then
            PB_HOST = candidate
            break
        end
    end
end

local PB_HWID = (type(gethwid)=="function" and gethwid()) or tostring(game:GetService("Players").LocalPlayer.UserId)
-- El servidor espera el identifier hasheado (asi lo manda Boost_lua.txt), antes se mandaba crudo.
local PB_IDENTIFIER = PB_DIGEST and PB_DIGEST(PB_HWID) or PB_HWID

local function redeemAccessKey(key)
    if not PB_REQUEST then
        return false, "SERVER UNAVAILABLE"
    end

    local nonce = pbGenerateNonce()
    local body = { identifier = PB_IDENTIFIER, key = key }
    if PB_USE_NONCE then
        body.nonce = nonce
    end

    local ok, response = pcall(PB_REQUEST, {
        Url = PB_HOST .. "/public/redeem/" .. tostring(PB_SERVICE),
        Method = "POST",
        Body = KeyHttpService:JSONEncode(body),
        Headers = { ["Content-Type"] = "application/json" },
    })

    if not ok or not response or response.StatusCode ~= 200 then
        return false, "SERVER UNAVAILABLE"
    end

    local decodeOk, data = pcall(function()
        return KeyHttpService:JSONDecode(response.Body)
    end)

    if not decodeOk or type(data) ~= "table" then
        return false, "INVALID RESPONSE"
    end

    if data.success == true and data.data and data.data.valid == true then
        if PB_USE_NONCE and data.data.hash ~= PB_DIGEST("true-" .. nonce .. "-" .. PB_SECRET) then
            return false, "INVALID RESPONSE"
        end
        return true, "KEY ACCEPTED"
    end

    return false, "INVALID OR EXPIRED KEY"
end

function validateAccessKey(value)
    local key = normalizeAccessKey(value)
    if not isAccessKeyFormat(key) then
        return false, "INVALID FORMAT"
    end

    if not PB_REQUEST then
        return false, "SERVER UNAVAILABLE"
    end

    local nonce = pbGenerateNonce()
    local endpoint = PB_HOST .. "/public/whitelist/" .. tostring(PB_SERVICE) .. "?identifier=" .. PB_IDENTIFIER .. "&key=" .. key
    if PB_USE_NONCE then
        endpoint = endpoint .. "&nonce=" .. nonce
    end

    local ok, response = pcall(PB_REQUEST, {
        Url = endpoint,
        Method = "GET",
    })

    if not ok or not response or response.StatusCode ~= 200 then
        return false, "SERVER UNAVAILABLE"
    end

    local decodeOk, data = pcall(function()
        return KeyHttpService:JSONDecode(response.Body)
    end)

    if not decodeOk or type(data) ~= "table" then
        return false, "INVALID RESPONSE"
    end

    if data.success == true and data.data and data.data.valid == true then
        return true, "KEY ACCEPTED"
    end

    if data.success == true and data.data and data.data.valid == false then
        return redeemAccessKey(key)
    end

    return false, "INVALID OR EXPIRED KEY"
end

function createKeySystemUI()
    local visualTheme = getKeyVisualTheme()
    local parent
    pcall(function()
        parent = game:GetService("CoreGui")
    end)
    parent = parent or LocalPlayer:WaitForChild("PlayerGui")

    local existing = parent:FindFirstChild("SAEKeySystem")
    if existing then
        existing:Destroy()
    end

    local gui = Instance.new("ScreenGui")
    gui.Name = "SAEKeySystem"
    gui.ResetOnSpawn = false
    gui.IgnoreGuiInset = true
    gui.DisplayOrder = 999999
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    gui.Parent = parent

    local shade = Instance.new("Frame")
    shade.Name = "Shade"
    shade.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    shade.BackgroundTransparency = 0.28
    shade.BorderSizePixel = 0
    shade.Size = UDim2.fromScale(1, 1)
    shade.ZIndex = 1
    shade.Parent = gui

    local shadeGradient = Instance.new("UIGradient")
    shadeGradient.Rotation = 25
    shadeGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)),
        ColorSequenceKeypoint.new(1, visualTheme.Background),
    })
    shadeGradient.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.22),
        NumberSequenceKeypoint.new(1, 0.55),
    })
    shadeGradient.Parent = shade

    local panel = Instance.new("Frame")
    panel.Name = "KeyPanel"
    panel.AnchorPoint = Vector2.new(0.5, 0.5)
    panel.BackgroundColor3 = visualTheme.Background
    panel.BorderSizePixel = 0
    panel.Position = UDim2.fromScale(0.5, 0.5)
    panel.Size = UDim2.new(0.88, 0, 0, 300)
    panel.ZIndex = 2
    panel.Parent = shade

    local maxSize = Instance.new("UISizeConstraint")
    maxSize.MaxSize = Vector2.new(460, 300)
    maxSize.MinSize = Vector2.new(300, 270)
    maxSize.Parent = panel

    local panelCorner = Instance.new("UICorner")
    panelCorner.CornerRadius = UDim.new(0, 16)
    panelCorner.Parent = panel

    local panelStroke = Instance.new("UIStroke")
    panelStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    panelStroke.Color = visualTheme.Accent
    panelStroke.Transparency = 0.14
    panelStroke.Thickness = 1.35
    panelStroke.LineJoinMode = Enum.LineJoinMode.Round
    panelStroke.Parent = panel
    addYinSweep(panelStroke, visualTheme.Borders, 1.55)

    local content = Instance.new("Frame")
    content.BackgroundColor3 = visualTheme.Surface
    content.BorderSizePixel = 0
    content.Position = UDim2.fromOffset(1, 1)
    content.Size = UDim2.new(1, -2, 1, -2)
    content.ZIndex = 2
    content.Parent = panel

    local backgroundImage = Instance.new("ImageLabel")
    backgroundImage.Name = "BackgroundImage"
    backgroundImage.BackgroundTransparency = 1
    backgroundImage.Image = "rbxassetid://139826687551366"
    backgroundImage.ImageTransparency = 0.18
    backgroundImage.ScaleType = Enum.ScaleType.Crop
    backgroundImage.Size = UDim2.fromScale(1, 1)
    backgroundImage.ZIndex = 2
    backgroundImage.Parent = content

    local backgroundImageCorner = Instance.new("UICorner")
    backgroundImageCorner.CornerRadius = UDim.new(0, 15)
    backgroundImageCorner.Parent = backgroundImage

    local contentCorner = Instance.new("UICorner")
    contentCorner.CornerRadius = UDim.new(0, 15)
    contentCorner.Parent = content

    local contentStroke = Instance.new("UIStroke")
    contentStroke.Color = visualTheme.Borders[3]
    contentStroke.Transparency = 0.66
    contentStroke.Thickness = 1
    contentStroke.Parent = content
    addYinSweep(contentStroke, visualTheme.Borders, 2.15)

    local top = Instance.new("Frame")
    top.BackgroundColor3 = visualTheme.Header
    top.BorderSizePixel = 0
    top.Size = UDim2.new(1, 0, 0, 56)
    top.ZIndex = 3
    top.Parent = content

    local topCorner = Instance.new("UICorner")
    topCorner.CornerRadius = UDim.new(0, 12)
    topCorner.Parent = top

    local topCover = Instance.new("Frame")
    topCover.BackgroundColor3 = visualTheme.Header
    topCover.BorderSizePixel = 0
    topCover.Position = UDim2.new(0, 0, 1, -12)
    topCover.Size = UDim2.new(1, 0, 0, 12)
    topCover.ZIndex = 3
    topCover.Parent = top

    local accentLine = Instance.new("Frame")
    accentLine.BackgroundColor3 = visualTheme.Accent
    accentLine.BorderSizePixel = 0
    accentLine.Position = UDim2.new(0, 18, 1, -1)
    accentLine.Size = UDim2.new(1, -36, 0, 1)
    accentLine.ZIndex = 4
    accentLine.Parent = top

    local accentGradient = Instance.new("UIGradient")
    accentGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, visualTheme.Borders[1]),
        ColorSequenceKeypoint.new(0.5, visualTheme.Borders[2]),
        ColorSequenceKeypoint.new(1, visualTheme.Borders[3]),
    })
    accentGradient.Offset = Vector2.new(-1.4, 0)
    accentGradient.Parent = accentLine
    KeyTweenService:Create(
        accentGradient,
        TweenInfo.new(1.75, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1, false),
        {Offset = Vector2.new(1.4, 0)}
    ):Play()

    local brand = Instance.new("TextLabel")
    brand.BackgroundTransparency = 1
    brand.Font = Enum.Font.GothamBold
    brand.Text = "YIN YANG KEY"
    brand.TextColor3 = visualTheme.Text
    brand.TextSize = 14
    brand.TextXAlignment = Enum.TextXAlignment.Left
    brand.Position = UDim2.new(0, 54, 0, 8)
    brand.Size = UDim2.new(0.60, 0, 0, 18)
    brand.ZIndex = 4
    brand.Parent = top

    local brandSub = Instance.new("TextLabel")
    brandSub.BackgroundTransparency = 1
    brandSub.Font = Enum.Font.GothamMedium
    brandSub.Text = keyText("brand_sub")
    brandSub.TextColor3 = visualTheme.TextDim
    brandSub.TextSize = 8
    brandSub.TextXAlignment = Enum.TextXAlignment.Left
    brandSub.Position = UDim2.new(0, 54, 0, 28)
    brandSub.Size = UDim2.new(0.60, 0, 0, 13)
    brandSub.ZIndex = 4
    brandSub.Parent = top

    local close = Instance.new("TextButton")
    close.AutoButtonColor = false
    close.BackgroundColor3 = visualTheme.Background
    close.BorderSizePixel = 0
    close.Font = Enum.Font.GothamBold
    close.Text = "×"
    close.TextColor3 = visualTheme.TextDim
    close.TextSize = 20
    close.AnchorPoint = Vector2.new(1, 0.5)
    close.Position = UDim2.new(1, -12, 0.5, 0)
    close.Size = UDim2.fromOffset(30, 30)
    close.ZIndex = 4
    close.Parent = top

    local closeCorner = Instance.new("UICorner")
    closeCorner.CornerRadius = UDim.new(0, 9)
    closeCorner.Parent = close

    local brandIcon = Instance.new("ImageLabel")
    brandIcon.BackgroundTransparency = 1
    brandIcon.Image = "rbxassetid://113137557066757"
    brandIcon.ImageColor3 = visualTheme.Text
    brandIcon.ImageTransparency = 0.02
    brandIcon.Position = UDim2.new(0, 16, 0, 11)
    brandIcon.Size = UDim2.fromOffset(24, 24)
    brandIcon.ZIndex = 4
    brandIcon.Parent = top

    local title = Instance.new("TextLabel")
    title.BackgroundTransparency = 1
    title.Font = Enum.Font.GothamBold
    title.Text = keyText("title")
    title.TextColor3 = visualTheme.Text
    title.TextSize = 15
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Position = UDim2.new(0, 57, 0, 65)
    title.Size = UDim2.new(1, -75, 0, 21)
    title.ZIndex = 4
    title.Parent = content

    local subtitle = Instance.new("TextLabel")
    subtitle.BackgroundTransparency = 1
    subtitle.Font = Enum.Font.Gotham
    subtitle.Text = keyText("subtitle")
    subtitle.TextColor3 = visualTheme.TextDim
    subtitle.TextSize = 8
    subtitle.TextXAlignment = Enum.TextXAlignment.Left
    subtitle.TextYAlignment = Enum.TextYAlignment.Center
    subtitle.Position = UDim2.new(0, 57, 0, 91)
    subtitle.Size = UDim2.new(1, -75, 0, 15)
    subtitle.ZIndex = 4
    subtitle.Parent = content

    local sectionIcon = Instance.new("ImageLabel")
    sectionIcon.BackgroundColor3 = Color3.fromRGB(17, 17, 17)
    sectionIcon.BackgroundTransparency = 0
    sectionIcon.Image = "rbxassetid://113137557066757"
    sectionIcon.ImageColor3 = visualTheme.Text
    sectionIcon.ImageTransparency = 0.02
    sectionIcon.Position = UDim2.new(0, 18, 0, 64)
    sectionIcon.Size = UDim2.fromOffset(20, 20)
    sectionIcon.ZIndex = 4
    sectionIcon.Parent = content

    local sectionIconCorner = Instance.new("UICorner")
    sectionIconCorner.CornerRadius = UDim.new(0, 8)
    sectionIconCorner.Parent = sectionIcon

    local input = Instance.new("TextBox")
    input.BackgroundColor3 = visualTheme.Background
    input.BorderSizePixel = 0
    input.ClearTextOnFocus = false
    input.Font = Enum.Font.GothamMedium
    input.PlaceholderColor3 = visualTheme.TextDim
    input.PlaceholderText = "KEY_XXXXXXXXXXXXXXXXXXXXXXXX"
    input.Text = ""
    input.TextColor3 = visualTheme.Text
    input.TextSize = 12
    input.TextXAlignment = Enum.TextXAlignment.Left
    input.Position = UDim2.new(0, 18, 0, 118)
    input.Size = UDim2.new(1, -36, 0, 44)
    input.ZIndex = 4
    input.Parent = content

    local inputCorner = Instance.new("UICorner")
    inputCorner.CornerRadius = UDim.new(0, 8)
    inputCorner.Parent = input

    local inputPadding = Instance.new("UIPadding")
    inputPadding.PaddingLeft = UDim.new(0, 50)
    inputPadding.PaddingRight = UDim.new(0, 16)
    inputPadding.Parent = input

    local inputStroke = Instance.new("UIStroke")
    inputStroke.Color = visualTheme.Accent
    inputStroke.Transparency = 0.22
    inputStroke.Parent = input
    inputStroke.Thickness = 1.15
    addYinSweep(inputStroke, visualTheme.Borders, 1.8)

    local keyIcon = Instance.new("ImageLabel")
    keyIcon.BackgroundTransparency = 1
    keyIcon.Image = "rbxassetid://82339297204572"
    keyIcon.ImageColor3 = visualTheme.Text
    keyIcon.ImageTransparency = 0.02
    keyIcon.Position = UDim2.new(0, 34, 0, 131)
    keyIcon.Size = UDim2.fromOffset(18, 18)
    keyIcon.ZIndex = 5
    keyIcon.Parent = content

    local keyDivider = Instance.new("Frame")
    keyDivider.BackgroundColor3 = visualTheme.Borders[3]
    keyDivider.BackgroundTransparency = 0.45
    keyDivider.BorderSizePixel = 0
    keyDivider.Position = UDim2.new(0, 63, 0, 122)
    keyDivider.Size = UDim2.new(0, 1, 0, 36)
    keyDivider.ZIndex = 5
    keyDivider.Parent = content

    function createActionButton(name, text, position, size, accent)
        local button = Instance.new("TextButton")
        button.Name = name
        button.AutoButtonColor = false
        button.BackgroundColor3 = visualTheme.Header
        button.BorderSizePixel = 0
        button.Font = Enum.Font.GothamBold
        button.Text = text
        button.TextColor3 = visualTheme.Text
        button.TextSize = 11
        button.Position = position
        button.Size = size
        button.ZIndex = 4
        button.Parent = content

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 8)
        corner.Parent = button

        local stroke = Instance.new("UIStroke")
        stroke.Color = accent
        stroke.Transparency = 0.18
        stroke.Thickness = 1
        stroke.Parent = button
        addYinSweep(stroke, visualTheme.Borders, name == "CheckKey" and 1.45 or 2.05)

        return button, stroke
    end

    local checkButton, checkStroke = createActionButton("CheckKey", keyText("check"), UDim2.new(0, 18, 0, 171), UDim2.new(1, -36, 0, 44), visualTheme.Accent)
    local linkButton, linkStroke = createActionButton("GetLink", keyText("link"), UDim2.new(0, 18, 0, 231), UDim2.new(1, -36, 0, 32), visualTheme.Borders[2])
    linkButton.BackgroundTransparency = 0.12
    linkButton.TextColor3 = visualTheme.TextDim
    linkButton.Font = Enum.Font.GothamMedium
    linkButton.TextSize = 9
    linkStroke.Transparency = 0.28

    local checkIcon = Instance.new("ImageLabel")
    checkIcon.BackgroundTransparency = 1
    checkIcon.Image = "rbxassetid://113137557066757"
    checkIcon.ImageColor3 = visualTheme.Text
    checkIcon.Position = UDim2.new(0.5, -96, 0, 184)
    checkIcon.Size = UDim2.fromOffset(18, 18)
    checkIcon.ZIndex = 5
    checkIcon.Parent = content

    local linkIcon = Instance.new("ImageLabel")
    linkIcon.BackgroundTransparency = 1
    linkIcon.Image = "rbxassetid://125248903248905"
    linkIcon.ImageColor3 = visualTheme.TextDim
    linkIcon.ImageTransparency = 0.05
    linkIcon.Position = UDim2.new(0.5, -92, 0, 238)
    linkIcon.Size = UDim2.fromOffset(18, 18)
    linkIcon.ZIndex = 5
    linkIcon.Parent = content

    local status = Instance.new("TextLabel")
    status.BackgroundTransparency = 1
    status.Font = Enum.Font.GothamMedium
    status.Text = keyText("secure")
    status.TextColor3 = visualTheme.TextDim
    status.TextSize = 8
    status.TextXAlignment = Enum.TextXAlignment.Left
    status.Position = UDim2.new(0, 47, 0, 270)
    status.Size = UDim2.new(1, -64, 0, 16)
    status.ZIndex = 4
    status.Parent = content

    local alertIcon = Instance.new("ImageLabel")
    alertIcon.BackgroundTransparency = 1
    alertIcon.Image = "rbxassetid://140207627533370"
    alertIcon.ImageColor3 = visualTheme.TextDim
    alertIcon.Position = UDim2.new(0, 21, 0, 268)
    alertIcon.Size = UDim2.fromOffset(14, 14)
    alertIcon.ZIndex = 5
    alertIcon.Parent = content


    local authorized = false
    local closed = false
    local busy = false
    local currentStatusKind = "secure"

    local function renderKeyStatus(kind)
        if kind == "secure" then return keyText("secure") end
        if kind == "revalidating" then return keyText("revalidating") end
        if kind == "contacting" then return keyText("contacting") end
        if kind == "accepted" then return "✓ " .. keyText("accepted") .. " — " .. keyText("loading") end
        if kind == "link_copied" then return keyText("link_copied") .. " — " .. KEY_PORTAL_URL end
        if kind == "portal" then return keyText("portal") .. ": " .. KEY_PORTAL_URL end
        if kind == "no_file_support" then return keyText("no_file_support") end
        if kind == "invalid_format" then return "✕ " .. keyText("invalid_format") end
        if kind == "server_unavailable" then return "✕ " .. keyText("server_unavailable") end
        if kind == "invalid_response" then return "✕ " .. keyText("invalid_response") end
        if kind == "invalid_expired" then return "✕ " .. keyText("invalid_expired") end
        if kind == "invalid_expired_removed" then return "✕ " .. keyText("invalid_expired") .. " — " .. keyText("saved_removed") end
        return keyText("secure")
    end

    local function setKeyStatus(kind, color)
        currentStatusKind = kind
        status.Text = renderKeyStatus(kind)
        status.TextColor3 = color
    end

    local function refreshKeyLanguage()
        brandSub.Text = keyText("brand_sub")
        title.Text = keyText("title")
        subtitle.Text = keyText("subtitle")
        if busy then
            checkButton.Text = keyText("checking")
        elseif authorized then
            checkButton.Text = "✓  " .. keyText("access_granted")
        else
            checkButton.Text = keyText("check")
        end
        linkButton.Text = keyText("link")
        status.Text = renderKeyStatus(currentStatusKind)
    end

    local function setKeyLanguageFromLocale(localeId)
        KeyLanguage = getKeyLanguage(localeId)
        refreshKeyLanguage()
    end

    --// Primero usa el locale del sistema para que el idioma aparezca rápidamente.
    pcall(function()
        local systemLocale = LocalizationService.SystemLocaleId
        if systemLocale and systemLocale ~= "" then
            setKeyLanguageFromLocale(systemLocale)
        end
    end)

    --// Roblox recomienda obtener el Translator del jugador y escuchar cambios
    --// de LocaleId para actualizar el contenido localizado durante la sesión.
    task.spawn(function()
        local ok, translator = pcall(function()
            return LocalizationService:GetTranslatorForPlayerAsync(LocalPlayer)
        end)
        if ok and translator then
            local function updateFromTranslator()
                setKeyLanguageFromLocale(translator.LocaleId)
            end
            updateFromTranslator()
            pcall(function()
                translator:GetPropertyChangedSignal("LocaleId"):Connect(updateFromTranslator)
            end)
        end
    end)

    function tryAccess(value, fromCache)
        if busy or closed then
            return
        end

        busy = true
        input.Text = normalizeAccessKey(value)
        checkButton.Text = keyText("checking")
        setKeyStatus(
            fromCache and "revalidating" or "contacting",
            Color3.fromRGB(210, 210, 210)
        )

        local valid, message = validateAccessKey(input.Text)
        if valid then
            saveCachedKey(input.Text)
            authorized = true
            getgenv().EvadeBetaKeyValidated = true
            setKeyStatus("accepted", Color3.fromRGB(235, 235, 235))
            checkButton.Text = "✓  " .. keyText("access_granted")
            checkButton.BackgroundColor3 = Color3.fromRGB(58, 58, 58)
            task.wait(0.7)
            if gui.Parent then
                gui:Destroy()
            end
        else
            local statusKind =
                message == "INVALID FORMAT" and "invalid_format"
                or message == "SERVER UNAVAILABLE" and "server_unavailable"
                or message == "INVALID RESPONSE" and "invalid_response"
                or message == "INVALID OR EXPIRED KEY" and (fromCache and "invalid_expired_removed" or "invalid_expired")
                or "secure"

            if fromCache and message == "INVALID OR EXPIRED KEY" then
                clearCachedKey()
                input.Text = ""
            end

            setKeyStatus(statusKind, Color3.fromRGB(222, 222, 222))
            checkButton.Text = "✓  " .. keyText("check")
            checkButton.BackgroundColor3 = Color3.fromRGB(43, 43, 43)
            task.wait(0.35)
            if checkButton.Parent then
                checkButton.BackgroundColor3 = visualTheme.Header
            end
        end
        busy = false
    end

    for _, pair in ipairs({{checkButton, checkStroke}, {linkButton, linkStroke}}) do
        pair[1]:SetAttribute("BaseTransparency", pair[1].BackgroundTransparency)
        pair[2]:SetAttribute("BaseStrokeTransparency", pair[2].Transparency)
    end

    function setButtonVisual(button, stroke, hovering)
        local baseBg = button:GetAttribute("BaseTransparency") or button.BackgroundTransparency
        local baseStroke = stroke:GetAttribute("BaseStrokeTransparency") or stroke.Transparency
        KeyTweenService:Create(
            stroke,
            TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out),
            { Transparency = hovering and math.max(0, baseStroke - 0.14) or baseStroke }
        ):Play()
        KeyTweenService:Create(
            button,
            TweenInfo.new(0.18, Enum.EasingStyle.Sine, Enum.EasingDirection.Out),
            { BackgroundTransparency = hovering and math.max(0, baseBg - 0.06) or baseBg }
        ):Play()
    end

    for _, data in ipairs({{checkButton, checkStroke}, {linkButton, linkStroke}}) do
        data[1].MouseEnter:Connect(function()
            if not busy then
                setButtonVisual(data[1], data[2], true)
            end
        end)
        data[1].MouseLeave:Connect(function()
            setButtonVisual(data[1], data[2], false)
        end)
    end

    input.FocusLost:Connect(function(enterPressed)
        if enterPressed then
            tryAccess(input.Text, false)
        end
    end)

    linkButton.Activated:Connect(function()
        if not PB_REQUEST then
            local clip = setclipboard or toclipboard
            local candidates = {
                "syn.request=" .. tostring(syn and type(syn.request)=="function"),
                "http.request=" .. tostring(http and type(http.request)=="function"),
                "http_request=" .. tostring(type(http_request)=="function"),
                "request=" .. tostring(type(request)=="function"),
                "fluxus.request=" .. tostring(fluxus and type(fluxus.request)=="function"),
                "krnl.request=" .. tostring(krnl and type(krnl.request)=="function"),
                "electron.request=" .. tostring(electron and type(electron.request)=="function"),
            }
            local diagnostic = table.concat({
                "PB_DIAGNOSTIC",
                "request_function=nil",
                "request_source=" .. tostring(PB_REQUEST_SOURCE),
                "host=" .. tostring(PB_HOST),
                "service=" .. tostring(PB_SERVICE),
                table.concat(candidates, "\n"),
            }, "\n")
            if clip then pcall(clip, diagnostic) end
            setKeyStatus("server_unavailable", Color3.fromRGB(222, 222, 222))
            return
        end

        local requestUrl = PB_HOST .. "/public/start"
        local requestBody = KeyHttpService:JSONEncode({ service = PB_SERVICE, identifier = PB_IDENTIFIER })
        local ok, response = pcall(PB_REQUEST, {
            Url = requestUrl,
            Method = "POST",
            Body = requestBody,
            Headers = { ["Content-Type"] = "application/json" },
        })

        local diagnostic = {
            "PB_DIAGNOSTIC",
            "request_ok=" .. tostring(ok),
            "host=" .. tostring(PB_HOST),
            "url=" .. tostring(requestUrl),
            "service=" .. tostring(PB_SERVICE),
        }

        if not ok then
            diagnostic[#diagnostic + 1] = "error=" .. tostring(response)
        elseif not response then
            diagnostic[#diagnostic + 1] = "response=nil"
        else
            diagnostic[#diagnostic + 1] = "status=" .. tostring(response.StatusCode)
            diagnostic[#diagnostic + 1] = "status_message=" .. tostring(response.StatusMessage or "")
            diagnostic[#diagnostic + 1] = "body=" .. tostring(response.Body or "")
        end

        local diagnosticText = table.concat(diagnostic, "\n")
        local clip = setclipboard or toclipboard
        if clip then pcall(clip, diagnosticText) end

        if ok and response and response.StatusCode == 200 then
            local decodeOk, data = pcall(function() return KeyHttpService:JSONDecode(response.Body) end)
            if decodeOk and data and data.success and data.data and data.data.url then
                local clipLink = setclipboard or toclipboard
                if clipLink then pcall(clipLink, data.data.url) end
                setKeyStatus("link_copied", Color3.fromRGB(230, 230, 230))
                return
            end
            diagnostic[#diagnostic + 1] = "json_decode_ok=" .. tostring(decodeOk)
            diagnostic[#diagnostic + 1] = "json_error_or_missing_url=true"
            diagnosticText = table.concat(diagnostic, "\n")
            if clip then pcall(clip, diagnosticText) end
        end

        setKeyStatus("server_unavailable", Color3.fromRGB(222, 222, 222))
    end)

    close.Activated:Connect(function()
        closed = true
        gui:Destroy()
    end)

    checkButton.Activated:Connect(function()
        tryAccess(input.Text, false)
    end)

    local cachedKey = loadCachedKey()
    if cachedKey then
        input.Text = cachedKey
        task.spawn(function()
            task.wait(0.15)
            tryAccess(cachedKey, true)
        end)
    elseif not hasKeyCacheSupport() then
        setKeyStatus("no_file_support", Color3.fromRGB(210, 210, 210))
    end
    while not authorized and not closed do
        task.wait()
    end

    return authorized
end

--// KEY SYSTEM: interfaz desactivada (no eliminada). Para reactivar el popup
--// de clave, poner esta bandera en true.
local ENABLE_KEY_SYSTEM_UI = true

if ENABLE_KEY_SYSTEM_UI and not createKeySystemUI() then
    warn("YIN YANG KEY: closed without a valid key")
    return
end

print("\nSERVICES: Initialized successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// MAIN CONFIGURATION
--// ═════════════════════════════════════════════════════════════════════════════

local Config = {
    --// SPEED MULTIPLIER (WalkSpeed real, distinto de Teleport Walk)
    EnableSpeedMultiplier = false,
    SpeedMultiplierValue = 1,
    
    --// BASIC MOVEMENT
    TeleportMovementSpeed = 5,
    EnableTeleportWalk = false,
    JumpHeight = 50,
    EnableEnhancedJump = false,
    --// INFINITE SLIDE
    EnableInfiniteSlide = false,
    InfiniteSlideSpeed = 30,
    AutoJump = false,
    -- Velocidad expresada en saltos por segundo; evita saltos por cada Heartbeat.
    AutoJumpSpeed = 1.5,
    AutoCrouch = false,
    -- Velocidad expresada en toggles (agachar/parar) por segundo.
    AutoCrouchSpeed = 1.5,
    EnableJumpAcceleration = false,
    JumpAccelerationSpeed = 42,
    
    --// GRAVITY MODIFICATION
    EnableGravityMod = false,
    GravityScale = 0.5,
    
    --// FRONTAL JUMP
    EnableFrontalJump = false,

    --// RAMP MULTIPLIER (independiente de Frontal Jump)
    EnableRampMultiplier = false,
    
    --// BACK JUMP
    EnableBackJump = false,
    BackJumpSpeed = 42,
    
    --// MAP FEATURES
    EnableFullBright = false,
    EnableAutoTicket = false,
    EnableVoidEscape = false,
    EnableAfkMap     = false,
    EnableAntiAfk    = false,
    EnableWhistleESP = false,
    
    --// EXTRA
    EnableReviveAura = false,
    EnableAutoCarry = false,
    EnableGoodMode = false, -- Farm of money (auto-revive propio) — aporte de NhatMinh

    --// EMOTE SPEED
    EnableEmoteSpeed = false,
    EmoteSpeedMultiplier = 1.5,

    --// LAYER JUMPS (Multi-Jump via JumpCap interno de Evade)
    EnableLayerJumps = false,
    LayerJumpsCount = 2,

    --// IMPULSE JUMP (impulso frontal automático al saltar)
    EnableImpulseJump = false,
    ImpulseJumpSpeed = 42,

    --// AUTO TRIMP
    EnableAutoTrimp = false,
    AutoTrimpMultiplier = 2.5,
    AutoTrimpFallSpeed = 0.5,
    --// TRIMP + TELEPORT WALK (combinado)
    EnableTrimpTeleport = false,

    --// FIGURE-8 LOOP (Lemniscata / Movimiento en ∞)
    EnableFigure8  = false,
    Figure8Speed   = 1.5,   -- velocidad angular (rad/s). Rango: 0.2 – 3.0
    Figure8Radius  = 8,     -- radio de cada lóbulo en studs. Rango: 3 – 25
    --// LISSAJOUS 3:2 LOOP (figura de lazo con 6 cruzamientos)
    EnableLissajous  = false,
    LissajousSpeed   = 1.5,   -- velocidad angular (rad/s). Rango: 0.2 – 3.0
    LissajousRadius  = 8,     -- radio en studs. Rango: 3 – 25
    --// HIPOTROCOIDE (Spirograph)
    EnableSpiro      = false,
    SpiroSpeed       = 0.8,   -- velocidad angular (rad/s). Rango: 0.1 – 2.0
    SpiroRadius      = 12,    -- radio exterior R en studs. Rango: 5 – 30
    SpiroRatio       = 0.35,  -- ratio radio interno (r = R * ratio). Rango: 0.1 – 0.9
}

--// Frontal Jump Variables
local camera = Workspace.CurrentCamera
getgenv().FrontalJumpSpeed = 42
getgenv().RampMultiplier = 1.55
getgenv().RampMultiplierValue = 1.55 -- Ramp Multiplier independiente (no comparte valor con Frontal Jump)
local maxExtraSpeed = 80
local currentSpeed = getgenv().FrontalJumpSpeed
local airAccumulator = 0
local lastTick = tick()
local wasAir = false
local activeBV = nil
local lastJumpTime = tick()
local jumpInterval = 0.65
-- Próximo instante permitido para Auto Jump; se conserva fuera del Heartbeat.
local autoJumpNextTime = 0
local autoCrouchNextTime = 0
local rampLocked = false
--// Jump Acceleration comparte estado y motor con Frontal Jump.

--// Back Jump Variables
getgenv().BackJumpSpeed = 42
getgenv().BackRampMultiplier = 0
local backCurrentSpeed = getgenv().BackJumpSpeed
local backAirAccumulator = 0
local backLastTick = tick()
local backWasAir = false
local backActiveBV = nil
local backLastJumpTime = tick()
local backJumpInterval = 0.65
local backRampLocked = false


--// FullBright Variables
local Lighting = game:GetService("Lighting")

local fullBrightState = {
    Enabled = false,
    Connection = nil,
    Original = nil,
}

local fullBrightHooks = {
    SaveSettings = nil,
}

function captureFullBrightState()
    if fullBrightState.Original then
        return
    end

    local ok, snapshot = pcall(function()
        return {
            Brightness = Lighting.Brightness,
            Ambient = Lighting.Ambient,
            OutdoorAmbient = Lighting.OutdoorAmbient,
            ColorShift_Bottom = Lighting.ColorShift_Bottom,
            ColorShift_Top = Lighting.ColorShift_Top,
            GlobalShadows = Lighting.GlobalShadows,
            ClockTime = Lighting.ClockTime,
            ExposureCompensation = Lighting.ExposureCompensation,
            FogColor = Lighting.FogColor,
            FogEnd = Lighting.FogEnd,
            FogStart = Lighting.FogStart,
            EnvironmentDiffuseScale = Lighting.EnvironmentDiffuseScale,
            EnvironmentSpecularScale = Lighting.EnvironmentSpecularScale,
        }
    end)

    if ok and snapshot then
        fullBrightState.Original = snapshot
    end
end

function disconnectFullBrightConnection()
    if fullBrightState.Connection then
        pcall(function()
            fullBrightState.Connection:Disconnect()
        end)
        fullBrightState.Connection = nil
    end
end

function restoreFullBrightState()
    local original = fullBrightState.Original
    if not original then
        return
    end

    pcall(function()
        Lighting.Brightness = original.Brightness
        Lighting.Ambient = original.Ambient
        Lighting.OutdoorAmbient = original.OutdoorAmbient
        Lighting.ColorShift_Bottom = original.ColorShift_Bottom
        Lighting.ColorShift_Top = original.ColorShift_Top
        Lighting.GlobalShadows = original.GlobalShadows
        Lighting.ClockTime = original.ClockTime
        Lighting.ExposureCompensation = original.ExposureCompensation
        Lighting.FogColor = original.FogColor
        Lighting.FogEnd = original.FogEnd
        Lighting.FogStart = original.FogStart
        Lighting.EnvironmentDiffuseScale = original.EnvironmentDiffuseScale
        Lighting.EnvironmentSpecularScale = original.EnvironmentSpecularScale
    end)
end

function applyFullBright()
    pcall(function()
        if Lighting.GlobalShadows ~= false then
            Lighting.GlobalShadows = false
        end

        if Lighting.Brightness ~= 1 then
            Lighting.Brightness = 1
        end

        local white = Color3.new(1, 1, 1)
        if Lighting.Ambient ~= white then
            Lighting.Ambient = white
        end
        if Lighting.OutdoorAmbient ~= white then
            Lighting.OutdoorAmbient = white
        end
        if Lighting.ColorShift_Bottom ~= white then
            Lighting.ColorShift_Bottom = white
        end
        if Lighting.ColorShift_Top ~= white then
            Lighting.ColorShift_Top = white
        end

        if Lighting.ClockTime ~= 12 then
            Lighting.ClockTime = 12
        end

        if Lighting.ExposureCompensation ~= 0 then
            Lighting.ExposureCompensation = 0
        end

        if Lighting.FogColor ~= white then
            Lighting.FogColor = white
        end

        if Lighting.FogEnd ~= 1e10 then
            Lighting.FogEnd = 1e10
        end

        if Lighting.FogStart ~= 0 then
            Lighting.FogStart = 0
        end

        if Lighting.EnvironmentDiffuseScale ~= 1 then
            Lighting.EnvironmentDiffuseScale = 1
        end

        if Lighting.EnvironmentSpecularScale ~= 1 then
            Lighting.EnvironmentSpecularScale = 1
        end
    end)
end

function setFullBrightEnabled(state)
    local enabled = state and true or false

    fullBrightState.Enabled = enabled
    Config.EnableFullBright = enabled

    if enabled then
        captureFullBrightState()
        applyFullBright()

        if not fullBrightState.Connection then
            fullBrightState.Connection = RunService.Heartbeat:Connect(function()
                if not fullBrightState.Enabled then
                    return
                end

                applyFullBright()
            end)
        end
    else
        disconnectFullBrightConnection()
        restoreFullBrightState()
    end

    if fullBrightHooks.SaveSettings then
        pcall(function()
            fullBrightHooks.SaveSettings()
        end)
    end
end

--// Variables to save options state

local savedOptions = {
    EnableTeleportWalk = false,
    EnableEnhancedJump = false,
    EnableGravityMod = false,
}

--// ═════════════════════════════════════════════════════════════════════════════
--// SETTINGS SAVE/LOAD SYSTEM
--// ═════════════════════════════════════════════════════════════════════════════

local HttpService = game:GetService("HttpService")
local SettingsFile = "EVADE_V52_Settings.json"

function SaveSettings()
    pcall(function()
        local data = {
            EnableTeleportWalk = Config.EnableTeleportWalk,
            TeleportMovementSpeed = Config.TeleportMovementSpeed,
            EnableEnhancedJump = Config.EnableEnhancedJump,
            JumpHeight = Config.JumpHeight,
            EnableInfiniteSlide = Config.EnableInfiniteSlide,
            InfiniteSlideSpeed = Config.InfiniteSlideSpeed,
            AutoJump = Config.AutoJump,
            AutoJumpSpeed = Config.AutoJumpSpeed,
            AutoCrouch = Config.AutoCrouch,
            AutoCrouchSpeed = Config.AutoCrouchSpeed,
            EnableJumpAcceleration = Config.EnableJumpAcceleration,
            JumpAccelerationSpeed = Config.JumpAccelerationSpeed,
            EnableGravityMod = Config.EnableGravityMod,
            GravityScale = Config.GravityScale,
            EnableFrontalJump = Config.EnableFrontalJump,
            FrontalJumpSpeed = getgenv().FrontalJumpSpeed,
            RampMultiplier = getgenv().RampMultiplier,
            EnableRampMultiplier = Config.EnableRampMultiplier,
            RampMultiplierValue = getgenv().RampMultiplierValue,
            EnableFullBright = Config.EnableFullBright,
            EnableAutoTicket = Config.EnableAutoTicket,
            EnableVoidEscape = Config.EnableVoidEscape,
            EnableAfkMap     = Config.EnableAfkMap,
            EnableAntiAfk    = Config.EnableAntiAfk,
            EnableWhistleESP = Config.EnableWhistleESP,
            EnableReviveAura = Config.EnableReviveAura,
            EnableAutoCarry = Config.EnableAutoCarry,
            EnableGoodMode = Config.EnableGoodMode,
            EnableAutoTrimp = Config.EnableAutoTrimp,
            AutoTrimpMultiplier = Config.AutoTrimpMultiplier,
            AutoTrimpFallSpeed = Config.AutoTrimpFallSpeed,
            EnableTrimpTeleport = Config.EnableTrimpTeleport,
            EnableSpeedMultiplier = Config.EnableSpeedMultiplier,
            SpeedMultiplierValue = Config.SpeedMultiplierValue,
            EnableEmoteSpeed = Config.EnableEmoteSpeed,
            EmoteSpeedMultiplier = Config.EmoteSpeedMultiplier,
            EnableLayerJumps = Config.EnableLayerJumps,
            LayerJumpsCount = Config.LayerJumpsCount,
            EnableImpulseJump = Config.EnableImpulseJump,
            ImpulseJumpSpeed = Config.ImpulseJumpSpeed,
            EnableBackJump = Config.EnableBackJump,
            BackJumpSpeed = getgenv().BackJumpSpeed,
            BackRampMultiplier = getgenv().BackRampMultiplier,
            EnableFigure8  = Config.EnableFigure8,
            Figure8Speed   = Config.Figure8Speed,
            Figure8Radius  = Config.Figure8Radius,
            EnableLissajous  = Config.EnableLissajous,
            LissajousSpeed   = Config.LissajousSpeed,
            LissajousRadius  = Config.LissajousRadius,
            EnableSpiro      = Config.EnableSpiro,
            SpiroSpeed       = Config.SpiroSpeed,
            SpiroRadius      = Config.SpiroRadius,
            SpiroRatio       = Config.SpiroRatio,
        }
        writefile(SettingsFile, HttpService:JSONEncode(data))
    end)
end

function LoadSettings()
    local ok, result = pcall(function()
        if isfile and isfile(SettingsFile) then
            local content = readfile(SettingsFile)
            if content and content ~= "" then
                return HttpService:JSONDecode(content)
            end
        end
        return nil
    end)

    if ok and result then
        if result.EnableTeleportWalk ~= nil then Config.EnableTeleportWalk = result.EnableTeleportWalk end
        if result.TeleportMovementSpeed ~= nil then Config.TeleportMovementSpeed = result.TeleportMovementSpeed end
        if result.EnableEnhancedJump ~= nil then Config.EnableEnhancedJump = result.EnableEnhancedJump end
        if result.JumpHeight ~= nil then Config.JumpHeight = result.JumpHeight end
        if result.EnableInfiniteSlide ~= nil then Config.EnableInfiniteSlide = result.EnableInfiniteSlide end
        if result.InfiniteSlideSpeed ~= nil then Config.InfiniteSlideSpeed = math.clamp(result.InfiniteSlideSpeed, 15, 150) end
        if result.AutoJump ~= nil then Config.AutoJump = result.AutoJump end
        if result.AutoJumpSpeed ~= nil then Config.AutoJumpSpeed = math.clamp(tonumber(result.AutoJumpSpeed) or 1.5, 0.5, 8) end
        if result.AutoCrouch ~= nil then Config.AutoCrouch = result.AutoCrouch end
        if result.AutoCrouchSpeed ~= nil then Config.AutoCrouchSpeed = math.clamp(tonumber(result.AutoCrouchSpeed) or 1.5, 0.5, 24) end
        if result.EnableJumpAcceleration ~= nil then Config.EnableJumpAcceleration = result.EnableJumpAcceleration end
        if result.JumpAccelerationSpeed ~= nil then Config.JumpAccelerationSpeed = math.clamp(tonumber(result.JumpAccelerationSpeed) or 42, 30, 110) end
        if result.EnableGravityMod ~= nil then Config.EnableGravityMod = result.EnableGravityMod end
        if result.GravityScale ~= nil then Config.GravityScale = result.GravityScale end
        if result.EnableFrontalJump ~= nil then Config.EnableFrontalJump = result.EnableFrontalJump end
        if result.FrontalJumpSpeed ~= nil then getgenv().FrontalJumpSpeed = result.FrontalJumpSpeed end
        if result.RampMultiplier ~= nil then getgenv().RampMultiplier = result.RampMultiplier end
        if result.EnableRampMultiplier ~= nil then Config.EnableRampMultiplier = result.EnableRampMultiplier end
        if result.RampMultiplierValue ~= nil then getgenv().RampMultiplierValue = result.RampMultiplierValue end
        if result.EnableFullBright ~= nil then Config.EnableFullBright = result.EnableFullBright end
        if result.EnableAutoTicket ~= nil then Config.EnableAutoTicket = result.EnableAutoTicket end
        if result.EnableVoidEscape ~= nil then Config.EnableVoidEscape = result.EnableVoidEscape end
        if result.EnableAfkMap     ~= nil then Config.EnableAfkMap     = result.EnableAfkMap     end
        if result.EnableAntiAfk    ~= nil then Config.EnableAntiAfk    = result.EnableAntiAfk    end
        if result.EnableWhistleESP ~= nil then Config.EnableWhistleESP = result.EnableWhistleESP end
        if result.EnableReviveAura ~= nil then Config.EnableReviveAura = result.EnableReviveAura end
        if result.EnableAutoCarry ~= nil then Config.EnableAutoCarry = result.EnableAutoCarry end
        if result.EnableGoodMode ~= nil then Config.EnableGoodMode = result.EnableGoodMode end
        if result.EnableAutoTrimp ~= nil then Config.EnableAutoTrimp = result.EnableAutoTrimp end
        if result.AutoTrimpMultiplier ~= nil then Config.AutoTrimpMultiplier = result.AutoTrimpMultiplier end
        if result.AutoTrimpFallSpeed ~= nil then Config.AutoTrimpFallSpeed = result.AutoTrimpFallSpeed end
        if result.EnableTrimpTeleport ~= nil then Config.EnableTrimpTeleport = result.EnableTrimpTeleport end
        if result.EnableSpeedMultiplier ~= nil then Config.EnableSpeedMultiplier = result.EnableSpeedMultiplier end
        if result.SpeedMultiplierValue ~= nil then Config.SpeedMultiplierValue = result.SpeedMultiplierValue end
        if result.EnableEmoteSpeed ~= nil then Config.EnableEmoteSpeed = result.EnableEmoteSpeed end
        if result.EmoteSpeedMultiplier ~= nil then Config.EmoteSpeedMultiplier = result.EmoteSpeedMultiplier end
        if result.EnableLayerJumps ~= nil then Config.EnableLayerJumps = result.EnableLayerJumps end
        if result.LayerJumpsCount ~= nil then Config.LayerJumpsCount = result.LayerJumpsCount end
        if result.EnableImpulseJump ~= nil then Config.EnableImpulseJump = result.EnableImpulseJump end
        if result.ImpulseJumpSpeed ~= nil then Config.ImpulseJumpSpeed = math.clamp(tonumber(result.ImpulseJumpSpeed) or 42, 10, 120) end
        if result.EnableBackJump ~= nil then Config.EnableBackJump = result.EnableBackJump end
        if result.BackJumpSpeed ~= nil then getgenv().BackJumpSpeed = result.BackJumpSpeed end
        if result.BackRampMultiplier ~= nil then getgenv().BackRampMultiplier = result.BackRampMultiplier end
        if result.EnableFigure8  ~= nil then Config.EnableFigure8  = result.EnableFigure8 end
        if result.Figure8Speed   ~= nil then Config.Figure8Speed   = math.clamp(tonumber(result.Figure8Speed)  or 1.5, 0.2, 3.0) end
        if result.Figure8Radius  ~= nil then Config.Figure8Radius  = math.clamp(tonumber(result.Figure8Radius) or 8,   3,   25)  end
        if result.EnableLissajous  ~= nil then Config.EnableLissajous  = result.EnableLissajous end
        if result.LissajousSpeed   ~= nil then Config.LissajousSpeed   = math.clamp(tonumber(result.LissajousSpeed)  or 1.5, 0.2, 3.0) end
        if result.LissajousRadius  ~= nil then Config.LissajousRadius  = math.clamp(tonumber(result.LissajousRadius) or 8,   3,   25)  end
        if result.EnableSpiro  ~= nil then Config.EnableSpiro  = result.EnableSpiro end
        if result.SpiroSpeed   ~= nil then Config.SpiroSpeed   = math.clamp(tonumber(result.SpiroSpeed)  or 0.8, 0.1, 2.0)  end
        if result.SpiroRadius  ~= nil then Config.SpiroRadius  = math.clamp(tonumber(result.SpiroRadius) or 12,  5,   30)   end
        if result.SpiroRatio   ~= nil then Config.SpiroRatio   = math.clamp(tonumber(result.SpiroRatio)  or 0.35, 0.1, 0.9) end
        print("SETTINGS: Previous configuration loaded successfully")
    else
        print("SETTINGS: No saved configuration found, using defaults")
    end
end

LoadSettings()

fullBrightHooks.SaveSettings = SaveSettings

--// Resync currentSpeed with the loaded FrontalJumpSpeed value
currentSpeed = Config.EnableJumpAcceleration and Config.JumpAccelerationSpeed or getgenv().FrontalJumpSpeed
--// Resync backCurrentSpeed with the loaded BackJumpSpeed value
backCurrentSpeed = getgenv().BackJumpSpeed

print("CONFIGURATION: Initialized")

--// ═════════════════════════════════════════════════════════════════════════════
--// LOAD YIN YANG v33 FINAL
--// ═════════════════════════════════════════════════════════════════════════════

print("\nLoading Yin Yang v28 Final from GitHub...")

local success = pcall(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/ZINZEROX/Zin/refs/heads/main/Zin"))()
end)

if not success or not _G.YinYang then
    error("ERROR: Failed to load Yin Yang v28 Final")
    return
end

print("LIBRARY: Yin Yang v28 Final loaded successfully")
task.wait(0.5)

print("SYSTEM: Initialization completed")

--// ═════════════════════════════════════════════════════════════════════════════
--// CREATE UI
--// ═════════════════════════════════════════════════════════════════════════════

print("\nCreating EVADE v5.2 Beta interface...")

--// Read the theme saved by the Yin Yang library itself (Yin_Yang_Config.txt)
--// The library saves the theme correctly on change, but its own CreateWindow
--// never re-applies it on load - so we read the same file here and pass it in.
function GetSavedTheme()
    local theme = "Dark"
    pcall(function()
        if isfile and isfile("Yin_Yang_Config.txt") then
            local content = readfile("Yin_Yang_Config.txt")
            if content and content ~= "" then
                local value = content:match("theme:([^|]+)")
                if value then
                    theme = value
                end
            end
        end
    end)
    return theme
end

local UI = _G.YinYang:CreateWindow("Yin Yang", GetSavedTheme())

--// ═════════════════════════════════════════════════════════════════════════════
--// INVERSE RAINBOW EFFECT FOR TITLE
--// ═════════════════════════════════════════════════════════════════════════════

function setupRainbowInversedTitle()
    local PlayerGui = Players.LocalPlayer:WaitForChild("PlayerGui")
    
    task.wait(0.8)
    
    if PlayerGui:FindFirstChild("Yin") then
        local ScreenGui = PlayerGui.Yin
        local TitleLabel = ScreenGui:FindFirstChild("TitleLabel")
        
        if TitleLabel then
            print("Creating inverse rainbow effect with two TextLabels...")
            
            --// Get position and size of the original title
            local origText = TitleLabel.Text
            local origSize = TitleLabel.Size
            local origPos = TitleLabel.Position
            local origFont = TitleLabel.Font
            local origTextSize = TitleLabel.TextSize
            
            --// Make original title invisible
            TitleLabel.TextTransparency = 1
            
            --// Create TextLabel for "EVADE" (White → Black)
            local EvadeLabel = Instance.new("TextLabel")
            EvadeLabel.Name = "EvadeLabel"
            EvadeLabel.Text = "EVADE"
            EvadeLabel.Font = origFont
            EvadeLabel.TextSize = origTextSize
            EvadeLabel.BackgroundTransparency = 1
            EvadeLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
            EvadeLabel.Size = UDim2.new(0.6, 0, 1, 0)
            EvadeLabel.Position = UDim2.new(0, 0, 0, 0)
            EvadeLabel.TextXAlignment = Enum.TextXAlignment.Left
            EvadeLabel.ZIndex = TitleLabel.ZIndex + 1
            EvadeLabel.Parent = TitleLabel.Parent
            
            --// Create TextLabel for "Beta" (Black → White)
            local BetaLabel = Instance.new("TextLabel")
            BetaLabel.Name = "BetaLabel"
            BetaLabel.Text = "v5.2 Beta"
            BetaLabel.Font = origFont
            BetaLabel.TextSize = origTextSize
            BetaLabel.BackgroundTransparency = 1
            BetaLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
            BetaLabel.Size = UDim2.new(0.4, 0, 1, 0)
            BetaLabel.Position = UDim2.new(0.6, 0, 0, 0)
            BetaLabel.TextXAlignment = Enum.TextXAlignment.Left
            BetaLabel.ZIndex = TitleLabel.ZIndex + 1
            BetaLabel.Parent = TitleLabel.Parent
            
            print("✅ Labels created: EvadeLabel + BetaLabel")
            
            --// Animate INVERSE colors
            local cycleCount = 0
            local rainbow = RunService.RenderStepped:Connect(function()
                if not EvadeLabel or not EvadeLabel.Parent or not BetaLabel or not BetaLabel.Parent then
                    rainbow:Disconnect()
                    return
                end
                
                cycleCount = cycleCount + 1
                local progress = (cycleCount % 120) / 120
                
                if progress < 0.5 then
                    local t = progress * 2
                    
                    local evadeBrightness = 1 - t
                    EvadeLabel.TextColor3 = Color3.fromRGB(
                        math.floor(255 * evadeBrightness),
                        math.floor(255 * evadeBrightness),
                        math.floor(255 * evadeBrightness)
                    )
                    
                    local betaBrightness = t
                    BetaLabel.TextColor3 = Color3.fromRGB(
                        math.floor(255 * betaBrightness),
                        math.floor(255 * betaBrightness),
                        math.floor(255 * betaBrightness)
                    )
                else
                    local t = (progress - 0.5) * 2
                    
                    local evadeBrightness = t
                    EvadeLabel.TextColor3 = Color3.fromRGB(
                        math.floor(255 * evadeBrightness),
                        math.floor(255 * evadeBrightness),
                        math.floor(255 * evadeBrightness)
                    )
                    
                    local betaBrightness = 1 - t
                    BetaLabel.TextColor3 = Color3.fromRGB(
                        math.floor(255 * betaBrightness),
                        math.floor(255 * betaBrightness),
                        math.floor(255 * betaBrightness)
                    )
                end
            end)
            
            print("✅ Inverse rainbow effect activated")
        end
    end
end

task.delay(1, setupRainbowInversedTitle)

--// ════════════════════════════════════════════════════════════════════════════
--// ═════════════════════════════════════════════════════════════════════════════
--// SCRIPT UI LANGUAGE — detección automática del idioma del jugador
--// Usa LocalizationService/GetTranslatorForPlayerAsync, sin configuración manual.
--// Español e inglés usan los textos fuente del script; otros locales intentan
--// traducirse mediante el Translator disponible y usan inglés como fallback.
--// ═════════════════════════════════════════════════════════════════════════════
local ScriptLanguage = "en"
local ScriptTranslator = nil
local ScriptTranslationCache = {}

local function getScriptLanguage(localeId)
    local language = string.lower(tostring(localeId or "en")):match("^(%a+)") or "en"
    return language
end

local function scriptText(spanishText, englishText)
    spanishText = tostring(spanishText or "")
    englishText = tostring(englishText or spanishText)

    if ScriptLanguage == "es" then
        return spanishText
    end
    if ScriptLanguage == "en" then
        return englishText
    end

    local cacheKey = ScriptLanguage .. "\0" .. englishText
    if ScriptTranslationCache[cacheKey] ~= nil then
        return ScriptTranslationCache[cacheKey]
    end

    local translated = nil
    if ScriptTranslator then
        pcall(function()
            translated = ScriptTranslator:Translate(game, englishText)
        end)
    end

    if translated and translated ~= "" and translated ~= englishText then
        ScriptTranslationCache[cacheKey] = translated
        return translated
    end

    ScriptTranslationCache[cacheKey] = englishText
    return englishText
end

local function refreshScriptLanguage()
    local playerGui = LocalPlayer:FindFirstChildOfClass("PlayerGui")
    local screenGui = playerGui and playerGui:FindFirstChild("Yin")
    if not screenGui then
        return
    end

    for _, object in ipairs(screenGui:GetDescendants()) do
        if object:IsA("TextLabel") or object:IsA("TextButton") or object:IsA("TextBox") then
            local spanishText = object:GetAttribute("TextSpanish")
            local englishText = object:GetAttribute("TextEnglish")
            if spanishText ~= nil and englishText ~= nil then
                object.Text = scriptText(spanishText, englishText)
            end

            local placeholderSpanish = object:GetAttribute("PlaceholderSpanish")
            local placeholderEnglish = object:GetAttribute("PlaceholderEnglish")
            if placeholderSpanish ~= nil and placeholderEnglish ~= nil then
                object.PlaceholderText = scriptText(placeholderSpanish, placeholderEnglish)
            end
        end
    end
end

local function setScriptLanguageFromLocale(localeId)
    ScriptLanguage = getScriptLanguage(localeId)
    ScriptTranslationCache = {}
    refreshScriptLanguage()
end

--// Primero aplica el locale del sistema para que la UI tenga el idioma correcto
--// desde el inicio.
pcall(function()
    local systemLocale = LocalizationService.SystemLocaleId
    if systemLocale and systemLocale ~= "" then
        setScriptLanguageFromLocale(systemLocale)
    end
end)

--// Después usa el Translator del jugador y escucha cambios de LocaleId.
task.spawn(function()
    local ok, translator = pcall(function()
        return LocalizationService:GetTranslatorForPlayerAsync(LocalPlayer)
    end)

    if ok and translator then
        ScriptTranslator = translator

        local function updateScriptLanguage()
            setScriptLanguageFromLocale(translator.LocaleId)
        end

        updateScriptLanguage()
        pcall(function()
            translator:GetPropertyChangedSignal("LocaleId"):Connect(updateScriptLanguage)
        end)
    end
end)

--// CREATE TABS
--// ════════════════════════════════════════════════════════════════════════════

local TabMove = UI:CreateTab("Move", "rbxassetid://127654592832194")
local TabMap = UI:CreateTab("Map", "rbxassetid://108047640113959")
local TabExtra = UI:CreateTab("Extra", "rbxassetid://87314564821705")
local TabEmote = UI:CreateTab("Emote", "rbxassetid://120302862220435")
local TabAvatar = UI:CreateTab("Avatar", "Avatar", "rbxassetid://97892611931074")

--// ⚠️ NO ELIMINAR — Crea la pestaña Spotify en esta UI
--// Descarga el catálogo desde GitHub y renderiza las canciones automáticamente
UI:CreateSpotifyTab()

print("TABS: Created (Move + Map + Extra + Avatar)")

--// ═════════════════════════════════════════════════════════════════════════════
--// TAB: AVATAR — Cambio visual local, limpio y permanente
--// ═════════════════════════════════════════════════════════════════════════════

do
function createAvatarStroke(parent, transparency)
    local border = Instance.new("UIStroke")
    border.Color = Color3.fromRGB(220, 220, 225)
    border.Thickness = 1
    border.Transparency = transparency or 0.38
    border.ZIndex = 12
    border.Parent = parent
    border:SetAttribute("ThemeRole", "Stroke")
    return border
end

--// Encabezado compacto con icono Avatar2 y separador inspirado en la referencia.
local AvatarHeader = Instance.new("Frame")
AvatarHeader.Name = "AvatarHeader"
AvatarHeader.Size = UDim2.new(1, 0, 0, 58)
AvatarHeader.BackgroundColor3 = Color3.fromRGB(22, 22, 25)
AvatarHeader.BorderSizePixel = 0
AvatarHeader.ZIndex = 10
AvatarHeader.Parent = TabAvatar.Page
AvatarHeader:SetAttribute("ThemeRole", "Secondary")
local AvatarHeaderCorner = Instance.new("UICorner")
AvatarHeaderCorner.CornerRadius = UDim.new(0, 10)
AvatarHeaderCorner.Parent = AvatarHeader
createAvatarStroke(AvatarHeader, 0.44)

local AvatarHeaderIconBox = Instance.new("Frame")
AvatarHeaderIconBox.Size = UDim2.fromOffset(40, 40)
AvatarHeaderIconBox.Position = UDim2.fromOffset(9, 9)
AvatarHeaderIconBox.BackgroundColor3 = Color3.fromRGB(8, 8, 10)
AvatarHeaderIconBox.BorderSizePixel = 0
AvatarHeaderIconBox.ZIndex = 11
AvatarHeaderIconBox.Parent = AvatarHeader
local AvatarHeaderIconCorner = Instance.new("UICorner")
AvatarHeaderIconCorner.CornerRadius = UDim.new(0, 8)
AvatarHeaderIconCorner.Parent = AvatarHeaderIconBox
createAvatarStroke(AvatarHeaderIconBox, 0.2)

local AvatarHeaderIcon = Instance.new("ImageLabel")
AvatarHeaderIcon.Size = UDim2.fromOffset(26, 26)
AvatarHeaderIcon.Position = UDim2.fromOffset(7, 7)
AvatarHeaderIcon.BackgroundTransparency = 1
AvatarHeaderIcon.Image = "rbxassetid://125446874976286"
AvatarHeaderIcon.ImageColor3 = Color3.fromRGB(255, 255, 255)
AvatarHeaderIcon.ZIndex = 12
AvatarHeaderIcon.Parent = AvatarHeaderIconBox

local AvatarHeaderTitle = Instance.new("TextLabel")
AvatarHeaderTitle.Size = UDim2.new(1, -78, 0, 24)
AvatarHeaderTitle.Position = UDim2.fromOffset(61, 9)
AvatarHeaderTitle.BackgroundTransparency = 1
AvatarHeaderTitle.Text = scriptText("CAMBIO DE AVATAR", "AVATAR CHANGE")
AvatarHeaderTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
AvatarHeaderTitle.TextSize = 16
AvatarHeaderTitle.Font = Enum.Font.GothamBlack
AvatarHeaderTitle.TextXAlignment = Enum.TextXAlignment.Left
AvatarHeaderTitle.ZIndex = 11
AvatarHeaderTitle.Parent = AvatarHeader
AvatarHeaderTitle:SetAttribute("ThemeTextRole", "Text")
AvatarHeaderTitle:SetAttribute("TextSpanish", "CAMBIO DE AVATAR")
AvatarHeaderTitle:SetAttribute("TextEnglish", "AVATAR CHANGE")

local AvatarHeaderLine = Instance.new("Frame")
AvatarHeaderLine.Size = UDim2.new(1, -91, 0, 1)
AvatarHeaderLine.Position = UDim2.new(0, 61, 0, 41)
AvatarHeaderLine.BackgroundColor3 = Color3.fromRGB(205, 205, 210)
AvatarHeaderLine.BorderSizePixel = 0
AvatarHeaderLine.ZIndex = 11
AvatarHeaderLine.Parent = AvatarHeader
AvatarHeaderLine:SetAttribute("ThemeRole", "Stroke")

local AvatarHeaderSpark = Instance.new("TextLabel")
AvatarHeaderSpark.Size = UDim2.fromOffset(20, 20)
AvatarHeaderSpark.Position = UDim2.new(1, -28, 0, 31)
AvatarHeaderSpark.BackgroundTransparency = 1
AvatarHeaderSpark.Text = "✦"
AvatarHeaderSpark.TextColor3 = Color3.fromRGB(255, 255, 255)
AvatarHeaderSpark.TextSize = 17
AvatarHeaderSpark.Font = Enum.Font.GothamBold
AvatarHeaderSpark.ZIndex = 12
AvatarHeaderSpark.Parent = AvatarHeader
AvatarHeaderSpark:SetAttribute("ThemeTextRole", "Text")

local AvatarDescription = Instance.new("TextLabel")
AvatarDescription.Name = "AvatarDescription"
AvatarDescription.Size = UDim2.new(1, 0, 0, 34)
AvatarDescription.BackgroundTransparency = 1
AvatarDescription.Text = scriptText(
    "Escribe el nombre de un jugador para usar su avatar visual.",
    "Enter a player's username to use their visual avatar."
)
AvatarDescription.TextColor3 = Color3.fromRGB(225, 225, 230)
AvatarDescription.TextSize = 12
AvatarDescription.Font = Enum.Font.GothamBold
AvatarDescription.TextWrapped = true
AvatarDescription.TextXAlignment = Enum.TextXAlignment.Left
AvatarDescription.TextYAlignment = Enum.TextYAlignment.Center
AvatarDescription.ZIndex = 10
AvatarDescription.Parent = TabAvatar.Page
AvatarDescription:SetAttribute("ThemeTextRole", "Text")
AvatarDescription:SetAttribute("TextSpanish", "Escribe el nombre de un jugador para usar su avatar visual.")
AvatarDescription:SetAttribute("TextEnglish", "Enter a player's username to use their visual avatar.")

--// Input alto con icono Avatar2 y contraste fijo para que siempre sea legible.
local AvatarInputHolder = Instance.new("Frame")
AvatarInputHolder.Name = "AvatarUsernameInputHolder"
AvatarInputHolder.Size = UDim2.new(1, 0, 0, 52)
AvatarInputHolder.BackgroundColor3 = Color3.fromRGB(24, 24, 28)
AvatarInputHolder.BorderSizePixel = 0
AvatarInputHolder.ZIndex = 10
AvatarInputHolder.Parent = TabAvatar.Page
AvatarInputHolder:SetAttribute("ThemeRole", "Secondary")
local AvatarInputCorner = Instance.new("UICorner")
AvatarInputCorner.CornerRadius = UDim.new(0, 10)
AvatarInputCorner.Parent = AvatarInputHolder
createAvatarStroke(AvatarInputHolder, 0.2)

local AvatarInputIcon = Instance.new("ImageLabel")
AvatarInputIcon.Size = UDim2.fromOffset(24, 24)
AvatarInputIcon.Position = UDim2.fromOffset(15, 14)
AvatarInputIcon.BackgroundTransparency = 1
AvatarInputIcon.Image = "rbxassetid://125446874976286"
AvatarInputIcon.ImageColor3 = Color3.fromRGB(240, 240, 245)
AvatarInputIcon.ZIndex = 12
AvatarInputIcon.Parent = AvatarInputHolder

local AvatarUsernameInput = Instance.new("TextBox")
AvatarUsernameInput.Name = "AvatarUsernameInput"
AvatarUsernameInput.Size = UDim2.new(1, -67, 1, 0)
AvatarUsernameInput.Position = UDim2.fromOffset(52, 0)
AvatarUsernameInput.BackgroundTransparency = 1
AvatarUsernameInput.BorderSizePixel = 0
AvatarUsernameInput.ZIndex = 12
AvatarUsernameInput.ClearTextOnFocus = false
AvatarUsernameInput.PlaceholderText = scriptText("Nombre de usuario", "Username")
AvatarUsernameInput.PlaceholderColor3 = Color3.fromRGB(155, 155, 165)
AvatarUsernameInput.Text = ""
AvatarUsernameInput.TextColor3 = Color3.fromRGB(255, 255, 255)
AvatarUsernameInput.TextSize = 15
AvatarUsernameInput.Font = Enum.Font.GothamBold
AvatarUsernameInput.TextXAlignment = Enum.TextXAlignment.Left
AvatarUsernameInput.Parent = AvatarInputHolder
AvatarUsernameInput:SetAttribute("ThemeTextRole", "Text")
AvatarUsernameInput:SetAttribute("PlaceholderSpanish", "Nombre de usuario")
AvatarUsernameInput:SetAttribute("PlaceholderEnglish", "Username")

--// Botón propio con flecha, para conservar el lenguaje visual de la referencia.
local AvatarApplyHolder = Instance.new("Frame")
AvatarApplyHolder.Name = "AvatarApplyHolder"
AvatarApplyHolder.Size = UDim2.new(1, 0, 0, 48)
AvatarApplyHolder.BackgroundColor3 = Color3.fromRGB(20, 20, 23)
AvatarApplyHolder.BorderSizePixel = 0
AvatarApplyHolder.ZIndex = 10
AvatarApplyHolder.Parent = TabAvatar.Page
AvatarApplyHolder:SetAttribute("ThemeRole", "Secondary")
local AvatarApplyCorner = Instance.new("UICorner")
AvatarApplyCorner.CornerRadius = UDim.new(0, 10)
AvatarApplyCorner.Parent = AvatarApplyHolder
createAvatarStroke(AvatarApplyHolder, 0.16)

local AvatarApplyButton = Instance.new("TextButton")
AvatarApplyButton.Name = "AvatarApplyButton"
AvatarApplyButton.Size = UDim2.new(1, 0, 1, 0)
AvatarApplyButton.BackgroundTransparency = 1
AvatarApplyButton.BorderSizePixel = 0
AvatarApplyButton.AutoButtonColor = false
AvatarApplyButton.Text = ""
AvatarApplyButton.ZIndex = 11
AvatarApplyButton.Parent = AvatarApplyHolder

local AvatarApplyLabel = Instance.new("TextLabel")
AvatarApplyLabel.Size = UDim2.new(1, -62, 1, 0)
AvatarApplyLabel.Position = UDim2.fromOffset(31, 0)
AvatarApplyLabel.BackgroundTransparency = 1
AvatarApplyLabel.Text = scriptText("APLICAR AVATAR", "APPLY AVATAR")
AvatarApplyLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
AvatarApplyLabel.TextSize = 14
AvatarApplyLabel.Font = Enum.Font.GothamBlack
AvatarApplyLabel.TextXAlignment = Enum.TextXAlignment.Center
AvatarApplyLabel.ZIndex = 12
AvatarApplyLabel.Parent = AvatarApplyHolder
AvatarApplyLabel:SetAttribute("ThemeTextRole", "Text")
AvatarApplyLabel:SetAttribute("TextSpanish", "APLICAR AVATAR")
AvatarApplyLabel:SetAttribute("TextEnglish", "APPLY AVATAR")

local AvatarApplyArrow = Instance.new("TextLabel")
AvatarApplyArrow.Size = UDim2.fromOffset(28, 28)
AvatarApplyArrow.Position = UDim2.new(1, -42, 0.5, -14)
AvatarApplyArrow.BackgroundTransparency = 1
AvatarApplyArrow.Text = "›"
AvatarApplyArrow.TextColor3 = Color3.fromRGB(255, 255, 255)
AvatarApplyArrow.TextSize = 31
AvatarApplyArrow.Font = Enum.Font.GothamBold
AvatarApplyArrow.ZIndex = 12
AvatarApplyArrow.Parent = AvatarApplyHolder
AvatarApplyArrow:SetAttribute("ThemeTextRole", "Text")

--// Tarjeta de estado: se muestra al ejecutar y usa el icono Correcto entregado.
local AvatarStatusHolder = Instance.new("Frame")
AvatarStatusHolder.Name = "AvatarStatusHolder"
AvatarStatusHolder.Size = UDim2.new(1, 0, 0, 52)
AvatarStatusHolder.LayoutOrder = 10
AvatarStatusHolder.BackgroundColor3 = Color3.fromRGB(18, 18, 21)
AvatarStatusHolder.BorderSizePixel = 0
AvatarStatusHolder.Visible = false
AvatarStatusHolder.ZIndex = 10
AvatarStatusHolder.Parent = TabAvatar.Page
AvatarStatusHolder:SetAttribute("ThemeRole", "Secondary")
local AvatarStatusCorner = Instance.new("UICorner")
AvatarStatusCorner.CornerRadius = UDim.new(0, 10)
AvatarStatusCorner.Parent = AvatarStatusHolder
createAvatarStroke(AvatarStatusHolder, 0.35)

local AvatarStatusIcon = Instance.new("ImageLabel")
AvatarStatusIcon.Size = UDim2.fromOffset(27, 27)
AvatarStatusIcon.Position = UDim2.fromOffset(14, 12)
AvatarStatusIcon.BackgroundTransparency = 1
AvatarStatusIcon.Image = "rbxassetid://88658875299906"
AvatarStatusIcon.ImageColor3 = Color3.fromRGB(145, 255, 180)
AvatarStatusIcon.ZIndex = 12
AvatarStatusIcon.Parent = AvatarStatusHolder

local AvatarStatus = Instance.new("TextLabel")
AvatarStatus.Name = "AvatarStatus"
AvatarStatus.Size = UDim2.new(1, -62, 1, -12)
AvatarStatus.Position = UDim2.fromOffset(50, 6)
AvatarStatus.BackgroundTransparency = 1
AvatarStatus.Text = ""
AvatarStatus.TextColor3 = Color3.fromRGB(180, 180, 190)
AvatarStatus.TextSize = 11
AvatarStatus.Font = Enum.Font.Gotham
AvatarStatus.ZIndex = 12
AvatarStatus.TextWrapped = true
AvatarStatus.TextXAlignment = Enum.TextXAlignment.Left
AvatarStatus.TextYAlignment = Enum.TextYAlignment.Center
AvatarStatus.Parent = AvatarStatusHolder
AvatarStatus:SetAttribute("TextSpanish", "")
AvatarStatus:SetAttribute("TextEnglish", "")

local avatarStatusSpanish, avatarStatusEnglish = "", ""
local avatarStatusColor = Color3.fromRGB(180, 180, 190)
function SetAvatarStatus(spanish, english, color)
    avatarStatusSpanish = spanish
    avatarStatusEnglish = english
    avatarStatusColor = color or Color3.fromRGB(180, 180, 190)
    AvatarStatusHolder.Visible = true
    AvatarStatus:SetAttribute("TextSpanish", spanish)
    AvatarStatus:SetAttribute("TextEnglish", english)
    AvatarStatus.Text = scriptText(spanish, english)
    AvatarStatus.TextColor3 = avatarStatusColor
    AvatarStatusIcon.ImageColor3 = avatarStatusColor
end

function GetAvatarVisualHumanoid()
    local rigsFolder = workspace:FindFirstChild("Rigs")
    local visualRig = rigsFolder and rigsFolder:FindFirstChild(Players.LocalPlayer.Name)
    local humanoid = visualRig and visualRig:FindFirstChildOfClass("Humanoid")
    return visualRig, humanoid
end

function ApplyAvatarByUsername()
    local username = tostring(AvatarUsernameInput.Text or ""):match("^%s*(.-)%s*$")
    if username == "" then
        SetAvatarStatus("Escribe un nombre de usuario.", "Enter a username.", Color3.fromRGB(255, 135, 135))
        return
    end

    local visualRig, visualHumanoid = GetAvatarVisualHumanoid()
    if not visualRig or not visualHumanoid then
        SetAvatarStatus("No se encontró el rig visual R6.", "The visual R6 rig was not found.", Color3.fromRGB(255, 135, 135))
        return
    end
    if visualHumanoid.RigType ~= Enum.HumanoidRigType.R6 then
        SetAvatarStatus("El rig visual no es R6.", "The visual rig is not R6.", Color3.fromRGB(255, 135, 135))
        return
    end

    SetAvatarStatus("Cargando avatar de " .. username .. "...", "Loading " .. username .. "'s avatar...", Color3.fromRGB(195, 215, 255))
    local okUser, userIdOrError = pcall(function()
        return Players:GetUserIdFromNameAsync(username)
    end)
    if not okUser then
        SetAvatarStatus("No se encontró ese usuario.", "That user was not found.", Color3.fromRGB(255, 135, 135))
        return
    end

    local okDescription, descriptionOrError = pcall(function()
        return Players:GetHumanoidDescriptionFromUserId(userIdOrError)
    end)
    if not okDescription then
        SetAvatarStatus("No se pudo cargar el avatar.", "The avatar could not be loaded.", Color3.fromRGB(255, 135, 135))
        return
    end

    local okApply, applyError = pcall(function()
        -- Reset completo: impide que ropa, cabello o accesorios previos se acumulen.
        visualHumanoid:ApplyDescriptionReset(descriptionOrError)
    end)
    if not okApply then
        SetAvatarStatus("No se pudo aplicar el avatar.", "The avatar could not be applied.", Color3.fromRGB(255, 135, 135))
        warn("[Avatar] ApplyDescriptionReset error:", applyError)
        return
    end

    SetAvatarStatus(
        "Avatar de " .. username .. " aplicado. Escribe otro nombre para cambiarlo.",
        username .. "'s avatar was applied. Enter another name to change it.",
        Color3.fromRGB(145, 255, 180)
    )
end

AvatarApplyButton.MouseButton1Click:Connect(ApplyAvatarByUsername)
AvatarUsernameInput.FocusLost:Connect(function(enterPressed)
    if enterPressed then
        ApplyAvatarByUsername()
    end
end)

--// Los iconos usan el mismo color de texto del tema activo para conservar
--// contraste en temas claros y oscuros sin alterar el icono de estado dinámico.
function RefreshAvatarThemeIcons()
    local textColor = AvatarHeaderTitle.TextColor3
    AvatarHeaderIcon.ImageColor3 = textColor
    AvatarInputIcon.ImageColor3 = textColor
end
RefreshAvatarThemeIcons()
AvatarHeaderTitle:GetPropertyChangedSignal("TextColor3"):Connect(RefreshAvatarThemeIcons)

--// ═════════════════════════════════════════════════════════════════════════════
--// CUSTOM BODY PART TOGGLES — standard toggles only
--// Inserted below the existing Avatar Change UI.
--// No CreateFloatingToggle, detach arrow, pin button, or lock is used here.
--// ═════════════════════════════════════════════════════════════════════════════
TabAvatar:CreateDivider()
TabAvatar:CreateLabel("PARTES CORPORALES", "CUSTOM BODY PARTS", 12)

local CustomBodyPartState = {
    Saved = {},
    HeadlessFallback = {
        Active = false,
        HeadTransparency = nil,
        FaceTransparency = nil,
    },
}

local CustomBodyParts = {
    Korblox = {
        Spanish = "Pierna derecha Korblox",
        English = "Korblox Right Leg",
        Property = "RightLeg",
        AssetId = 139607718,
    },
    Headless = {
        Spanish = "Cabeza invisible Headless",
        English = "Headless Invisible Head",
        Property = "Head",
        AssetId = 15093053680,
    },
}

function SetHeadlessFallback(visualRig, enabled)
    local head = visualRig and visualRig:FindFirstChild("Head")
    if not head or not head:IsA("BasePart") then
        return false
    end

    local face = head:FindFirstChild("face")
    if enabled then
        if not CustomBodyPartState.HeadlessFallback.Active then
            CustomBodyPartState.HeadlessFallback.HeadTransparency = head.LocalTransparencyModifier
            CustomBodyPartState.HeadlessFallback.FaceTransparency = face and face:IsA("Decal") and face.Transparency or nil
        end
        head.LocalTransparencyModifier = 1
        if face and face:IsA("Decal") then
            face.Transparency = 1
        end
        CustomBodyPartState.HeadlessFallback.Active = true
    else
        head.LocalTransparencyModifier = CustomBodyPartState.HeadlessFallback.HeadTransparency or 0
        if face and face:IsA("Decal") and CustomBodyPartState.HeadlessFallback.FaceTransparency ~= nil then
            face.Transparency = CustomBodyPartState.HeadlessFallback.FaceTransparency
        end
        CustomBodyPartState.HeadlessFallback = {
            Active = false,
            HeadTransparency = nil,
            FaceTransparency = nil,
        }
    end
    return true
end

function ApplyCustomBodyPart(partInfo, enabled)
    local visualRig, visualHumanoid = GetAvatarVisualHumanoid()
    if not visualRig or not visualHumanoid then
        SetAvatarStatus("No se encontró el rig visual R6.", "The visual R6 rig was not found.", Color3.fromRGB(255, 135, 135))
        return false
    end
    if visualHumanoid.RigType ~= Enum.HumanoidRigType.R6 then
        SetAvatarStatus("Las piezas requieren un rig visual R6.", "Custom parts require the visual R6 rig.", Color3.fromRGB(255, 135, 135))
        return false
    end

    local okDescription, descriptionOrError = pcall(function()
        return visualHumanoid:GetAppliedDescription()
    end)
    if not okDescription or not descriptionOrError then
        SetAvatarStatus("No se pudo leer el avatar actual.", "The current avatar description could not be read.", Color3.fromRGB(255, 135, 135))
        warn("[Avatar] GetAppliedDescription error:", descriptionOrError)
        return false
    end

    if enabled then
        if CustomBodyPartState.Saved[partInfo.Property] == nil then
            CustomBodyPartState.Saved[partInfo.Property] = descriptionOrError[partInfo.Property]
        end
        descriptionOrError[partInfo.Property] = partInfo.AssetId
        SetAvatarStatus("Aplicando " .. partInfo.Spanish .. "...", "Applying " .. partInfo.English .. "...", Color3.fromRGB(195, 215, 255))
    else
        descriptionOrError[partInfo.Property] = CustomBodyPartState.Saved[partInfo.Property] or 0
        CustomBodyPartState.Saved[partInfo.Property] = nil
        if partInfo.Property == "Head" and CustomBodyPartState.HeadlessFallback.Active then
            SetHeadlessFallback(visualRig, false)
        end
    end

    local okApply, applyError = pcall(function()
        visualHumanoid:ApplyDescriptionReset(descriptionOrError)
    end)
    if not okApply then
        if enabled and partInfo.Property == "Head" and SetHeadlessFallback(visualRig, true) then
            SetAvatarStatus("Cabeza invisible aplicada localmente.", "Headless fallback applied locally.", Color3.fromRGB(145, 255, 180))
            warn("[Avatar] Headless Dynamic Head fallback used:", applyError)
            return true
        end
        SetAvatarStatus("No se pudo aplicar la pieza.", "The body part could not be applied.", Color3.fromRGB(255, 135, 135))
        warn("[Avatar] ApplyDescriptionReset error:", applyError)
        return false
    end

    SetAvatarStatus(
        enabled and "Pieza aplicada al rig visual." or "Pieza restaurada.",
        enabled and "Body part applied to the visual rig." or "Body part restored.",
        enabled and Color3.fromRGB(145, 255, 180) or Color3.fromRGB(180, 180, 190)
    )
    return true
end

local KorbloxRightLegToggle
local HeadlessHeadToggle

KorbloxRightLegToggle = TabAvatar:CreateToggle(
    CustomBodyParts.Korblox.Spanish,
    CustomBodyParts.Korblox.English,
    false,
    function(enabled)
        local ok = ApplyCustomBodyPart(CustomBodyParts.Korblox, enabled)
        if not ok and enabled then
            KorbloxRightLegToggle.SetValue(false)
        end
    end
)

HeadlessHeadToggle = TabAvatar:CreateToggle(
    CustomBodyParts.Headless.Spanish,
    CustomBodyParts.Headless.English,
    false,
    function(enabled)
        local ok = ApplyCustomBodyPart(CustomBodyParts.Headless, enabled)
        if not ok and enabled then
            HeadlessHeadToggle.SetValue(false)
        end
    end
)
end

--// ═════════════════════════════════════════════════════════════════════════════
--// TAB: MOVEMENT
--// ═════════════════════════════════════════════════════════════════════════════

print("\nConfiguring Move tab...")

--// Última llamada real de SpeedChange (para poder resincronizar al desactivar
--// el Multiplicador de Velocidad, en vez de esperar al próximo evento natural)
local lastSpeedSelf, lastSpeedState, lastSpeedValue = nil, nil, nil
getgenv().YYSpeedRestoreRequested = false
getgenv().YYRestoreSpeedMultiplier = nil

--// PRUEBA: toggle combinado, primero de todo en la pestaña Move
TabMove:CreateCombinedToggle("Combinado (prueba)", "Combined (test)")
TabMove:CreateDivider()

TabMove:CreateLabel("MULTIPLICADOR DE VELOCIDAD (WalkSpeed)", "SPEED MULTIPLIER (WalkSpeed)", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Velocidad", "Speed", Config.EnableSpeedMultiplier, function(state)
    Config.EnableSpeedMultiplier = state
    print(state and "✅ Speed Multiplier ENABLED" or "❌ Speed Multiplier DISABLED")
    if state then
        getgenv().YYSpeedRestoreRequested = false
        getgenv().YYSpeedApplyRequested = true
        if getgenv().YYApplySpeedMultiplier then
            getgenv().YYApplySpeedMultiplier()
        end
    else
        getgenv().YYSpeedRestoreRequested = true
        if getgenv().YYRestoreSpeedMultiplier then
            getgenv().YYRestoreSpeedMultiplier()
        end
    end
    SaveSettings()
end)

TabMove:CreateSlider(
    "Speed Value",
    1, 5, Config.SpeedMultiplierValue,
    function(value)
        Config.SpeedMultiplierValue = value
        if Config.EnableSpeedMultiplier and getgenv().YYApplySpeedMultiplier then
            getgenv().YYSpeedApplyRequested = true
            getgenv().YYApplySpeedMultiplier()
        end
        print("✓ Speed Multiplier value: " .. value)
        SaveSettings()
    end,
    nil, 1.3
)

TabMove:CreateDivider()

TabMove:CreateLabel("MOVIMIENTO BÁSICO", "BASIC MOVEMENT", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Caminata", "Walk", Config.EnableTeleportWalk, function(state)
    Config.EnableTeleportWalk = state
    print(state and "✅ Teleport Walk ENABLED" or "❌ Teleport Walk DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Teleport Speed",
    1, 50, Config.TeleportMovementSpeed,
    function(value)
        Config.TeleportMovementSpeed = value
        print("✓ Teleport speed: " .. value)
        SaveSettings()
    end,
    nil, 3
)

TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Altura de Salto", "Jump Height", Config.EnableEnhancedJump, function(state)
    Config.EnableEnhancedJump = state
    print(state and "✅ Enhanced Jump ENABLED" or "❌ Enhanced Jump DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Jump Height",
    20, 300, Config.JumpHeight,
    function(value)
        Config.JumpHeight = value
        print("✓ Jump height: " .. value)
        SaveSettings()
    end,
    nil, 30
)

TabMove:CreateDivider()
TabMove:CreateLabel("SLIDE INFINITO", "INFINITE SLIDE", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Slide Inf", "Inf Slide", Config.EnableInfiniteSlide, function(state)
    Config.EnableInfiniteSlide = state
    print(state and "✅ Infinite Slide ENABLED" or "❌ Infinite Slide DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Slide Speed",
    15, 150, Config.InfiniteSlideSpeed,
    function(value)
        Config.InfiniteSlideSpeed = math.clamp(math.floor(value), 15, 150)
        print("✓ Infinite Slide speed: " .. Config.InfiniteSlideSpeed)
        SaveSettings()
    end,
    nil, 30
)

TabMove:CreateLabel(
    "Mantiene el deslizamiento mientras mantienes la entrada de slide",
    "Maintains the slide while the slide input is held",
    11
)

TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Auto Salto", "Auto Jump", Config.AutoJump, function(state)
    Config.AutoJump = state
    if not state then
        autoJumpNextTime = 0
    end
    print(state and "✅ Auto Jump ENABLED" or "❌ Auto Jump DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Auto Jump Speed",
    0.5, 8, Config.AutoJumpSpeed,
    function(value)
        Config.AutoJumpSpeed = math.clamp(tonumber(value) or 1.5, 0.5, 8)
        print("✓ Auto Jump speed: " .. string.format("%.2f", Config.AutoJumpSpeed) .. " jumps/s")
        SaveSettings()
    end,
    nil, 0.5
)

TabMove:CreateLabel(
    "Saltos por segundo; un valor mayor hace que salte más rápido",
    "Jumps per second; a higher value makes it jump faster",
    11
)

TabMove:CreateFloatingToggle("Agachado", "Crouch", Config.AutoCrouch, function(state)
    Config.AutoCrouch = state
    if not state then
        autoCrouchNextTime = 0
    end
    print(state and "✅ Auto Crouch ENABLED" or "❌ Auto Crouch DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Crouch Speed",
    0.5, 24, Config.AutoCrouchSpeed,
    function(value)
        Config.AutoCrouchSpeed = math.clamp(tonumber(value) or 1.5, 0.5, 24)
        print("✓ Auto Crouch speed: " .. string.format("%.2f", Config.AutoCrouchSpeed) .. " toggles/s")
        SaveSettings()
    end,
    nil, 0.5
)

TabMove:CreateFloatingToggle("Jump Aceleración", "Jump Acceleration", Config.EnableJumpAcceleration, function(state)
    Config.EnableJumpAcceleration = state
    lastJumpTime = tick()
    currentSpeed = Config.JumpAccelerationSpeed
    print(state and "✅ Jump Acceleration ENABLED" or "❌ Jump Acceleration DISABLED")
    SaveSettings()
end)

local JumpAccelerationSpeedSlider = TabMove:CreateSlider(
    "Jump Aceleración",
    30, 110, Config.JumpAccelerationSpeed,
    function(value)
        Config.JumpAccelerationSpeed = math.clamp(tonumber(value) or 42, 30, 110)
        if Config.EnableJumpAcceleration then currentSpeed = Config.JumpAccelerationSpeed end
        print("✓ Jump Acceleration Speed: " .. Config.JumpAccelerationSpeed)
        SaveSettings()
    end,
    nil, 1
)

TabMove:CreateLabel("Sin rampas", "No ramps", 11)
TabMove:CreateDivider()

--// LAYER JUMPS
TabMove:CreateLabel("SALTOS EN CAPAS (Multi-Salto)", "LAYER JUMPS (Multi-Jump)", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Saltos en Capas", "Layer Jumps", Config.EnableLayerJumps, function(state)
    Config.EnableLayerJumps = state
    print(state and "✅ Layer Jumps ENABLED" or "❌ Layer Jumps DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Jump Count",
    2, 20, Config.LayerJumpsCount,
    function(value)
        Config.LayerJumpsCount = math.floor(value)
        print("✓ Layer Jumps count: " .. Config.LayerJumpsCount)
        SaveSettings()
    end,
    nil, 20
)

TabMove:CreateDivider()

--// IMPULSE JUMP
TabMove:CreateLabel("SALTO CON IMPULSO", "IMPULSE JUMP", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Salto con Impulso", "Impulse Jump", Config.EnableImpulseJump, function(state)
    Config.EnableImpulseJump = state
    print(state and "✅ Impulse Jump ENABLED" or "❌ Impulse Jump DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Impulse Speed",
    10, 120, Config.ImpulseJumpSpeed,
    function(value)
        Config.ImpulseJumpSpeed = value
        print("✓ Impulse Jump speed: " .. string.format("%.0f", value))
        SaveSettings()
    end,
    nil, 120
)

TabMove:CreateDivider()

--// FRONTAL JUMP WITH BETA NEW RAINBOW
TabMove:CreateLabel("SALTO FRONTAL", "FRONTAL JUMP", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Salto Frontal", "Frontal Jump", Config.EnableFrontalJump, function(state)
    Config.EnableFrontalJump = state
    if state then
        --// SAVE CURRENT OPTIONS STATE
        savedOptions.EnableTeleportWalk = Config.EnableTeleportWalk
        savedOptions.EnableEnhancedJump = Config.EnableEnhancedJump
        savedOptions.EnableGravityMod = Config.EnableGravityMod
        
        --// DISABLE CONFLICTING OPTIONS
        Config.EnableTeleportWalk = false
        Config.EnableEnhancedJump = false
        Config.EnableGravityMod = false
        
        --// INITIALIZE FRONTAL JUMP
        lastJumpTime = tick()
        currentSpeed = getgenv().FrontalJumpSpeed
        airAccumulator = 0
        
        print("✅ Frontal Jump ENABLED - Beta")
        print("   - Teleport Walk disabled")
        print("   - Enhanced Jump disabled")
        print("   - Gravity Mod disabled")
    else
        --// RESTORE SAVED OPTIONS
        Config.EnableTeleportWalk = savedOptions.EnableTeleportWalk
        Config.EnableEnhancedJump = savedOptions.EnableEnhancedJump
        Config.EnableGravityMod = savedOptions.EnableGravityMod
        
        if activeBV then activeBV:Destroy() end
        activeBV = nil
        currentSpeed = getgenv().FrontalJumpSpeed
        
        print("❌ Frontal Jump DISABLED")
        print("   - Options restored")
    end
    SaveSettings()
end)

TabMove:CreateSlider(
    "Movement Speed",
    30, 110, getgenv().FrontalJumpSpeed,
    function(value)
        getgenv().FrontalJumpSpeed = value
        currentSpeed = value
        print("✓ Frontal Jump Speed: " .. value)
        SaveSettings()
    end,
    nil, 32
)

TabMove:CreateSlider(
    "Ramp Multiplier",
    0.10, 5.0, getgenv().RampMultiplier,
    function(value)
        getgenv().RampMultiplier = value
        print("✓ Ramp Multiplier: " .. value)
        SaveSettings()
    end,
    nil, 1.5
)

TabMove:CreateDivider()

--// RAMP MULTIPLIER (independiente) — mismo efecto de rampa que Frontal
--// Jump, pero sin su motor de auto-salto/auto-movimiento. Solo actúa
--// cuando VOS te movés hacia una rampa detectada; el resto del tiempo
--// no toca nada de tu personaje.
TabMove:CreateLabel("MULTIPLICADOR DE RAMPA", "RAMP MULTIPLIER", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Impulso Rampa", "Ramp Boost", Config.EnableRampMultiplier, function(state)
    Config.EnableRampMultiplier = state
    print(state and "✅ Ramp Multiplier ENABLED (standalone)" or "❌ Ramp Multiplier DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Ramp Multiplier",
    0.10, 5.0, getgenv().RampMultiplierValue,
    function(value)
        getgenv().RampMultiplierValue = value
        print("✓ Ramp Multiplier (standalone): " .. value)
        SaveSettings()
    end,
    nil, 1.5
)

TabMove:CreateDivider()

--// BACK JUMP
TabMove:CreateLabel("SALTO HACIA ATRÁS", "BACK JUMP", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Salto Hacia Atrás", "Back Jump", Config.EnableBackJump, function(state)
    Config.EnableBackJump = state
    if state then
        backLastJumpTime = tick()
        backCurrentSpeed = getgenv().BackJumpSpeed
        backAirAccumulator = 0
        print("✅ Back Jump ENABLED")
    else
        if backActiveBV then backActiveBV:Destroy() end
        backActiveBV = nil
        backCurrentSpeed = getgenv().BackJumpSpeed
        print("❌ Back Jump DISABLED")
    end
    SaveSettings()
end)

TabMove:CreateSlider(
    "Back Speed",
    30, 110, getgenv().BackJumpSpeed,
    function(value)
        getgenv().BackJumpSpeed = value
        backCurrentSpeed = value
        print("✓ Back Jump Speed: " .. value)
        SaveSettings()
    end,
    nil, 32
)

TabMove:CreateSlider(
    "Back Ramp Multiplier",
    0, 5.0, getgenv().BackRampMultiplier,
    function(value)
        getgenv().BackRampMultiplier = value
        print("✓ Back Ramp Multiplier: " .. value)
        SaveSettings()
    end,
    nil, 0
)

TabMove:CreateDivider()
TabMove:CreateLabel("MODIFICACIÓN DE GRAVEDAD", "GRAVITY MODIFICATION", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Gravedad", "Gravity", Config.EnableGravityMod, function(state)
    Config.EnableGravityMod = state
    print(state and "✅ Gravity Mod ENABLED" or "❌ Gravity Mod DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Gravity Scale",
    0.1, 2, Config.GravityScale,
    function(value)
        Config.GravityScale = value
        print("✓ Gravity Scale: " .. value)
        SaveSettings()
    end,
    nil, 1
)

TabMove:CreateDivider()
TabMove:CreateLabel("AUTO TRIMP", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Auto Trimp", Config.EnableAutoTrimp, function(state)
    Config.EnableAutoTrimp = state
    print(state and "✅ Auto Trimp ENABLED" or "❌ Auto Trimp DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Trimp Multiplier",
    1.0, 10.0, Config.AutoTrimpMultiplier,
    function(value)
        Config.AutoTrimpMultiplier = value
        print("✓ Trimp Multiplier: " .. value)
        SaveSettings()
    end
)

TabMove:CreateSlider(
    "Fall Speed",
    0.1, 1.0, Config.AutoTrimpFallSpeed,
    function(value)
        Config.AutoTrimpFallSpeed = value
        print("✓ Trimp Fall Speed: " .. value)
        SaveSettings()
    end
)

TabMove:CreateLabel("Rebota en el suelo con velocidad acumulada", "Bounces off the ground with accumulated speed", 11)

TabMove:CreateDivider()
TabMove:CreateLabel("TRIMP + TELEPORT WALK", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Trimp + Walk", Config.EnableTrimpTeleport, function(state)
    Config.EnableTrimpTeleport = state
    print(state and "✅ Trimp + Teleport Walk ENABLED" or "❌ Trimp + Teleport Walk DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Trimp Multiplier",
    1.0, 10.0, Config.AutoTrimpMultiplier,
    function(value)
        Config.AutoTrimpMultiplier = value
        print("✓ Trimp Multiplier: " .. value)
        SaveSettings()
    end
)

TabMove:CreateSlider(
    "Fall Speed",
    0.1, 1.0, Config.AutoTrimpFallSpeed,
    function(value)
        Config.AutoTrimpFallSpeed = value
        print("✓ Trimp Fall Speed: " .. value)
        SaveSettings()
    end
)

TabMove:CreateSlider(
    "Teleport Speed",
    1, 50, Config.TeleportMovementSpeed,
    function(value)
        Config.TeleportMovementSpeed = value
        print("✓ Teleport speed: " .. value)
        SaveSettings()
    end,
    nil, 3
)

TabMove:CreateLabel("Combina rebote y caminata por teletransporte", "Combines trimp bounce and teleport walk", 11)

TabMove:CreateDivider()
TabMove:CreateLabel("MOVIMIENTO EN 8  ∞", "FIGURE-8 MOVEMENT  ∞", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Bucle en 8", "Figure-8 Loop", Config.EnableFigure8, function(state)
    Config.EnableFigure8 = state
    if state then
        StartFigure8Loop()
    else
        StopFigure8Loop()
    end
    print(state and "✅ Figure-8 Loop ENABLED" or "❌ Figure-8 Loop DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Velocidad / Speed",
    0.2, 3.0, Config.Figure8Speed,
    function(value)
        Config.Figure8Speed = value
        print("✓ Figure-8 Speed: " .. value)
        SaveSettings()
    end
)

TabMove:CreateSlider(
    "Radio / Radius (studs)",
    3, 25, Config.Figure8Radius,
    function(value)
        Config.Figure8Radius = value
        print("✓ Figure-8 Radius: " .. value)
        SaveSettings()
    end
)

TabMove:CreateLabel(
    "Traza un ∞ alrededor del punto de activación  •  Velocidad y radio ajustables en tiempo real",
    "Traces an ∞ around the activation point  •  Speed and radius adjustable in real-time",
    11
)

TabMove:CreateDivider()
TabMove:CreateLabel("CURVA DE LISSAJOUS 3:2  〰️", "LISSAJOUS 3:2 CURVE  〰️", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Lissajous 3:2", "Lissajous 3:2", Config.EnableLissajous, function(state)
    Config.EnableLissajous = state
    if state then
        StartLissajousLoop()
    else
        StopLissajousLoop()
    end
    print(state and "✅ Lissajous Loop ENABLED" or "❌ Lissajous Loop DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Velocidad / Speed",
    0.2, 3.0, Config.LissajousSpeed,
    function(value)
        Config.LissajousSpeed = value
        print("✓ Lissajous Speed: " .. value)
        SaveSettings()
    end
)

TabMove:CreateSlider(
    "Radio / Radius (studs)",
    3, 25, Config.LissajousRadius,
    function(value)
        Config.LissajousRadius = value
        print("✓ Lissajous Radius: " .. value)
        SaveSettings()
    end
)

TabMove:CreateLabel(
    "Figura de lazo con 6 cruzamientos  •  Patrón osciloscópico clásico",
    "6-crossing loop figure  •  Classic oscilloscope pattern",
    11
)

TabMove:CreateDivider()
TabMove:CreateLabel("HIPOTROCOIDE (Spirograph)  🌀", "HYPOTROCHOID (Spirograph)  🌀", 14)
TabMove:CreateDivider()

TabMove:CreateFloatingToggle("Spirograph", "Spirograph", Config.EnableSpiro, function(state)
    Config.EnableSpiro = state
    if state then
        StartSpiroLoop()
    else
        StopSpiroLoop()
    end
    print(state and "✅ Spirograph Loop ENABLED" or "❌ Spirograph Loop DISABLED")
    SaveSettings()
end)

TabMove:CreateSlider(
    "Velocidad / Speed",
    0.1, 2.0, Config.SpiroSpeed,
    function(value)
        Config.SpiroSpeed = value
        print("✓ Spiro Speed: " .. value)
        SaveSettings()
    end
)

TabMove:CreateSlider(
    "Radio Exterior / Outer Radius (studs)",
    5, 30, Config.SpiroRadius,
    function(value)
        Config.SpiroRadius = value
        print("✓ Spiro Radius: " .. value)
        SaveSettings()
    end
)

TabMove:CreateSlider(
    "Radio Interno / Inner Ratio (0.1 – 0.9)",
    0.1, 0.9, Config.SpiroRatio,
    function(value)
        Config.SpiroRatio = value
        print("✓ Spiro Ratio: " .. value)
        SaveSettings()
    end
)

TabMove:CreateLabel(
    "Figura tipo Spirograph  •  Cambia el ratio para estrellas, rosas y más formas",
    "Spirograph-style figure  •  Change ratio for stars, roses and more shapes",
    11
)

print("✅ MOVE: Tab completed")

--// CREATE YELLOW-BLACK RAINBOW EFFECT FOR "Beta New"
function setupBetaNewRainbow()
    local PlayerGui = Players.LocalPlayer:WaitForChild("PlayerGui")
    task.wait(1.2)
    
    if PlayerGui:FindFirstChild("Yin") then
        local ScreenGui = PlayerGui.Yin
        
        --// Find the "Frontal Jump" toggle in TabMovement
        for _, child in pairs(ScreenGui:GetDescendants()) do
            if child:IsA("TextLabel") and child.Text:find("Frontal Jump") then
                local parentFrame = child.Parent
                
                if parentFrame then
                    --// Create label for "Beta" with rainbow
                    local BetaNewLabel = Instance.new("TextLabel")
                    BetaNewLabel.Name = "BetaLabel"
                    BetaNewLabel.Text = "Beta"
                    BetaNewLabel.Font = Enum.Font.GothamBold
                    BetaNewLabel.TextSize = 12
                    BetaNewLabel.BackgroundTransparency = 1
                    BetaNewLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
                    BetaNewLabel.Size = UDim2.new(0.25, 0, 1, 0)
                    BetaNewLabel.Position = UDim2.new(0.7, 0, 0, 0)
                    BetaNewLabel.TextXAlignment = Enum.TextXAlignment.Right
                    BetaNewLabel.ZIndex = child.ZIndex + 2
                    BetaNewLabel.Parent = parentFrame
                    
                    print("✨ Creating Yellow-Black rainbow effect for Beta...")
                    
                    --// Animate Yellow ↔ Black colors
                    local cycleCount = 0
                    local rainbowBetaNew = RunService.RenderStepped:Connect(function()
                        if not BetaNewLabel or not BetaNewLabel.Parent then
                            rainbowBetaNew:Disconnect()
                            return
                        end
                        
                        cycleCount = cycleCount + 1
                        local progress = (cycleCount % 120) / 120
                        
                        if progress < 0.5 then
                            --// Yellow (255, 255, 0) → Black (0, 0, 0)
                            local t = progress * 2
                            local brightness = 1 - t
                            
                            BetaNewLabel.TextColor3 = Color3.fromRGB(
                                math.floor(255 * brightness),
                                math.floor(255 * brightness),
                                0
                            )
                        else
                            --// Black (0, 0, 0) → Yellow (255, 255, 0)
                            local t = (progress - 0.5) * 2
                            local brightness = t
                            
                            BetaNewLabel.TextColor3 = Color3.fromRGB(
                                math.floor(255 * brightness),
                                math.floor(255 * brightness),
                                0
                            )
                        end
                    end)
                    
                    print("✅ Yellow-Black rainbow effect activated for Beta")
                    return
                end
            end
        end
    end
end

task.delay(1.5, setupBetaNewRainbow)

--// ═════════════════════════════════════════════════════════════════════════════
--// TAB: MAP - FULLBRIGHT SYSTEM
--// ═════════════════════════════════════════════════════════════════════════════

--// AFK MAP SYSTEM
local AfkMapSpawnCFrame  = nil
local AfkMapRespawnConn  = nil
local AntiAfkConn        = nil
local AFk_MAP_FILE       = "yin_yang_map.json"
local AFK_MAP_URL        = "https://raw.githubusercontent.com/Yinyangzx/yin_map/refs/heads/main/yin_yang_map.json"

function loadAfkMap()
    -- Siempre intenta traer la última versión del repo y sobrescribir la local.
    -- Si falla (sin internet, repo caído), sigue con la copia local que ya haya.
    if isfile then
        local ok, raw = pcall(game.HttpGet, game, AFK_MAP_URL)
        if ok and raw and raw ~= "" then
            pcall(writefile, AFk_MAP_FILE, raw)
        end
    end
    if not isfile or not isfile(AFk_MAP_FILE) then return false, "Archivo no encontrado" end
    local ok, raw = pcall(readfile, AFk_MAP_FILE)
    if not ok then return false, "Error leyendo archivo" end
    local ok2, data = pcall(function() return game:GetService("HttpService"):JSONDecode(raw) end)
    if not ok2 or not data or not data.parts then return false, "Archivo inválido" end
    -- Limpiar mapa anterior
    local old = workspace:FindFirstChild("YinYangAfkMap")
    if old then old:Destroy() end
    local folder = Instance.new("Folder")
    folder.Name = "YinYangAfkMap"
    folder.Parent = workspace
    local spawnCFrame = nil
    for _, p in ipairs(data.parts) do
        pcall(function()
            local part = Instance.new(p.t or "Part")
            part.Size = Vector3.new(p.sx, p.sy, p.sz)
            part.CFrame = CFrame.new(p.px, p.py, p.pz) * CFrame.Angles(math.rad(p.rx), math.rad(p.ry), math.rad(p.rz))
            part.Color = Color3.fromRGB(p.cr, p.cg, p.cb)
            pcall(function() part.Material = Enum.Material:GetEnumItems()[p.m] or Enum.Material.SmoothPlastic end)
            part.Transparency = p.tr or 0
            part.Anchored = p.an == 1
            part.CanCollide = p.ca == 1
            part.CastShadow = false
            part.Parent = folder
            if part:IsA("SpawnLocation") then
                spawnCFrame = part.CFrame
            end
        end)
    end
    -- Spawn: usar el SpawnLocation propio del mapa (el JSON trae uno), no la
    -- posición del jugador en Evade — son coordenadas de mundos distintos.
    AfkMapSpawnCFrame = (spawnCFrame or CFrame.new(0, 10, 0)) + Vector3.new(0, 5, 0)
    return true, data.count or #data.parts
end

function setAfkMapEnabled(state)
    Config.EnableAfkMap = state
    if state then
        -- Teletransportar ya mismo al spawn del mapa cargado (lo calculó loadAfkMap)
        if AfkMapSpawnCFrame then
            local char = LocalPlayer.Character
            local hrp  = char and char:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.CFrame = AfkMapSpawnCFrame end
        end
        -- Reconectar en cada respawn
        if AfkMapRespawnConn then AfkMapRespawnConn:Disconnect() end
        AfkMapRespawnConn = LocalPlayer.CharacterAdded:Connect(function()
            task.wait(0.5)
            if Config.EnableAfkMap and AfkMapSpawnCFrame then
                local c = LocalPlayer.Character
                local h = c and c:FindFirstChild("HumanoidRootPart")
                if h then h.CFrame = AfkMapSpawnCFrame end
            end
        end)
    else
        if AfkMapRespawnConn then AfkMapRespawnConn:Disconnect() AfkMapRespawnConn = nil end
        AfkMapSpawnCFrame = nil
        local old = workspace:FindFirstChild("YinYangAfkMap")
        if old then old:Destroy() end
    end
    SaveSettings()
end

function setAntiAfkEnabled(state)
    Config.EnableAntiAfk = state
    if state then
        if AntiAfkConn then return end
        AntiAfkConn = game:GetService("RunService").Heartbeat:Connect(function()
            if math.floor(os.clock()) % 60 == 0 then
                pcall(function()
                    game:GetService("VirtualInputManager"):SendMouseWheelEvent(0, 0, true, game)
                end)
            end
        end)
    else
        if AntiAfkConn then AntiAfkConn:Disconnect() AntiAfkConn = nil end
    end
    SaveSettings()
end

--// PLAYER ESP (WhistleHighlight nativo de Evade)
local playerEspRigService = nil
local playerEspWhistleTemplate = nil
local playerEspEntries = {}
local playerEspConnections = {}
local playerEspAccumulator = 0
local PLAYER_ESP_WHITE = Color3.fromRGB(255, 255, 255)
local PLAYER_ESP_RED = Color3.fromRGB(255, 70, 70)
local PLAYER_ESP_REFRESH = 0.10

pcall(function()
    local services = ReplicatedStorage:WaitForChild("Services", 5)
    local asset = services and services:WaitForChild("Asset", 5)
    local rigModule = asset and asset:WaitForChild("RigService", 5)
    if rigModule then
        playerEspRigService = require(rigModule)
    end
end)

pcall(function()
    local objects = ReplicatedStorage:WaitForChild("Objects", 5)
    local gameFolder = objects and objects:WaitForChild("Game", 5)
    local characterFolder = gameFolder and gameFolder:WaitForChild("Character", 5)
    local actionVisuals = characterFolder and characterFolder:WaitForChild("ActionVisuals", 5)
    local actions = actionVisuals and actionVisuals:WaitForChild("Actions", 5)
    local whistle = actions and actions:WaitForChild("Whistle", 5)
    playerEspWhistleTemplate = whistle and whistle:FindFirstChild("WhistleHighlight")
end)

function playerEspDisconnect(connection)
    if connection then
        pcall(function()
            connection:Disconnect()
        end)
    end
end

function playerEspSetVisible(entry, visible)
    if entry.highlight then
        entry.highlight.Enabled = visible and Config.EnableWhistleESP
    end
    if entry.billboard then
        entry.billboard.Enabled = visible and Config.EnableWhistleESP
    end
end

function playerEspApplyColor(entry)
    if not entry or not entry.highlight then
        return
    end

    local color = entry.downed and PLAYER_ESP_RED or PLAYER_ESP_WHITE
    pcall(function()
        entry.highlight.FillColor = color
        entry.highlight.OutlineColor = color
    end)
end

function playerEspClearVisual(entry)
    if not entry then
        return
    end

    playerEspDisconnect(entry.died)
    entry.died = nil

    if entry.highlight then
        pcall(function()
            entry.highlight:Destroy()
        end)
        entry.highlight = nil
    end
    if entry.billboard then
        pcall(function()
            entry.billboard:Destroy()
        end)
        entry.billboard = nil
    end

    entry.label = nil
    entry.root = nil
    entry.visualRoot = nil
    entry.visualModel = nil
    entry.rigData = nil
    entry.downed = false
    entry.humanoid = nil
    entry.character = nil
end

function playerEspDestroyEntry(player)
    local entry = playerEspEntries[player]
    if not entry then
        return
    end

    playerEspDisconnect(entry.characterAdded)
    playerEspDisconnect(entry.characterRemoving)
    playerEspClearVisual(entry)
    playerEspEntries[player] = nil
end

function playerEspGetRigData(character)
    if not playerEspRigService or not character then
        return nil
    end

    local tag = character:GetAttribute("Tag")
    if not tag then
        return nil
    end

    local ok, rig = pcall(function()
        return playerEspRigService:GetRigFromTag(tag)
    end)
    if ok and rig and rig.Model and rig.Model:IsA("Model") then
        return rig
    end

    return nil
end

function playerEspResolveVisual(player, character)
    local rigData = playerEspGetRigData(character)
    local visualModel = rigData and rigData.Model or nil
    if visualModel then
        local visualRoot = visualModel:FindFirstChild("HumanoidRootPart", true)
            or visualModel.PrimaryPart
            or visualModel:FindFirstChildWhichIsA("BasePart", true)
        if visualRoot then
            return visualModel, visualRoot, rigData
        end
    end

    local playersFolder = Workspace:FindFirstChild("Players")
    visualModel = playersFolder and playersFolder:FindFirstChild(player.Name)
    if visualModel and visualModel:IsA("Model") then
        local visualRoot = visualModel:FindFirstChild("HumanoidRootPart", true)
            or visualModel.PrimaryPart
            or visualModel:FindFirstChildWhichIsA("BasePart", true)
        if visualRoot then
            return visualModel, visualRoot, nil
        end
    end

    local characterRoot = character and character:FindFirstChild("HumanoidRootPart")
    return character, characterRoot, nil
end

function playerEspIsDowned(rigData, character)
    if rigData and rigData.DataRegistry then
        local okDowned, downed = pcall(function()
            return rigData.DataRegistry:Get("Downed")
        end)
        if okDowned and downed == true then
            return true
        end

        local okState, state = pcall(function()
            return rigData.DataRegistry:Get("State")
        end)
        if okState and state == "Downed" then
            return true
        end
    end

    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    return humanoid ~= nil and humanoid.Health <= 0
end

function playerEspCreateEntry(player, character)
    if player == LocalPlayer then
        return
    end

    local entry = playerEspEntries[player]
    if not entry then
        entry = {}
        playerEspEntries[player] = entry
    else
        playerEspClearVisual(entry)
    end

    local characterRoot = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    if not character or not characterRoot or not humanoid then
        return
    end

    local visualModel, visualRoot, rigData = playerEspResolveVisual(player, character)
    if not visualModel or not visualRoot then
        return
    end

    local downed = playerEspIsDowned(rigData, character)
    local oldHighlight = character:FindFirstChild("Beta_PlayerESP_WhistleHighlight")
    if oldHighlight then
        oldHighlight:Destroy()
    end
    local oldBillboard = visualRoot:FindFirstChild("Beta_PlayerESP_Label")
    if oldBillboard then
        oldBillboard:Destroy()
    end

    local highlight
    if playerEspWhistleTemplate then
        highlight = playerEspWhistleTemplate:Clone()
        highlight.Name = "Beta_PlayerESP_WhistleHighlight"
        highlight.Adornee = visualModel
        highlight.Enabled = Config.EnableWhistleESP
        highlight.Parent = character
    else
        highlight = Instance.new("Highlight")
        highlight.Name = "Beta_PlayerESP_WhistleHighlight_Fallback"
        highlight.Adornee = visualModel
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        highlight.FillTransparency = 1
        highlight.OutlineTransparency = 0
        highlight.Enabled = Config.EnableWhistleESP
        highlight.Parent = character
    end

    local billboard = Instance.new("BillboardGui")
    billboard.Name = "Beta_PlayerESP_Label"
    billboard.Adornee = visualRoot
    billboard.AlwaysOnTop = true
    billboard.LightInfluence = 0
    pcall(function()
        billboard.MaxDistance = math.huge
    end)
    billboard.Size = UDim2.new(0, 360, 0, 42)
    billboard.StudsOffset = Vector3.new(0, 3.8, 0)
    billboard.Enabled = Config.EnableWhistleESP
    billboard.Parent = visualRoot

    local label = Instance.new("TextLabel")
    label.Name = "Info"
    label.BackgroundTransparency = 1
    label.Size = UDim2.new(1, 0, 1, 0)
    label.Font = Enum.Font.GothamBold
    label.TextColor3 = PLAYER_ESP_WHITE
    label.TextStrokeColor3 = Color3.new(0, 0, 0)
    label.TextStrokeTransparency = 0
    label.TextSize = 18
    label.TextXAlignment = Enum.TextXAlignment.Center
    label.TextYAlignment = Enum.TextYAlignment.Center
    label.Text = player.DisplayName
    label.Parent = billboard

    entry.character = character
    entry.root = characterRoot
    entry.visualModel = visualModel
    entry.visualRoot = visualRoot
    entry.rigData = rigData
    entry.downed = downed
    entry.humanoid = humanoid
    entry.highlight = highlight
    entry.billboard = billboard
    entry.label = label
    playerEspApplyColor(entry)

    entry.died = humanoid.Died:Connect(function()
        if playerEspEntries[player] == entry then
            entry.downed = true
            playerEspApplyColor(entry)
        end
    end)
end

function playerEspAttachPlayer(player)
    if player == LocalPlayer then
        return
    end

    playerEspDestroyEntry(player)
    local entry = {
        characterAdded = player.CharacterAdded:Connect(function(character)
            task.defer(function()
                if Config.EnableWhistleESP then
                    local root = character:WaitForChild("HumanoidRootPart", 5)
                    local humanoid = character:FindFirstChildOfClass("Humanoid")
                    if root and humanoid then
                        playerEspCreateEntry(player, character)
                    end
                end
            end)
        end),
        characterRemoving = player.CharacterRemoving:Connect(function()
            local current = playerEspEntries[player]
            if current then
                playerEspClearVisual(current)
            end
        end),
    }
    playerEspEntries[player] = entry

    if player.Character then
        playerEspCreateEntry(player, player.Character)
    end
end

function playerEspAttachAll()
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            playerEspAttachPlayer(player)
        end
    end
end

function playerEspRefresh()
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and not playerEspEntries[player] then
            playerEspAttachPlayer(player)
        end
    end

    local localCharacter = LocalPlayer.Character
    local localRoot = localCharacter and localCharacter:FindFirstChild("HumanoidRootPart")
    local camera = Workspace.CurrentCamera
    local observerPosition = localRoot and localRoot.Position or (camera and camera.CFrame.Position)
    if not observerPosition then
        return
    end

    for player, entry in pairs(playerEspEntries) do
        if not player.Parent then
            playerEspDestroyEntry(player)
        elseif entry.character and entry.character.Parent and entry.root and entry.root.Parent then
            local visualModel, visualRoot, rigData = playerEspResolveVisual(player, entry.character)
            if visualModel ~= entry.visualModel or visualRoot ~= entry.visualRoot then
                playerEspCreateEntry(player, entry.character)
                entry = playerEspEntries[player]
            elseif entry then
                entry.rigData = rigData or entry.rigData
            end

            if entry and entry.visualRoot and entry.visualRoot.Parent then
                entry.downed = playerEspIsDowned(entry.rigData, entry.character)
                playerEspApplyColor(entry)
                local distance = (entry.visualRoot.Position - observerPosition).Magnitude
                playerEspSetVisible(entry, Config.EnableWhistleESP and distance <= math.huge)
                if entry.label then
                    entry.label.Text = string.format("%s  |  %d m", player.DisplayName, math.floor(distance + 0.5))
                end
            end
        else
            playerEspSetVisible(entry, false)
        end
    end
end

function playerEspSetEnabled(state)
    Config.EnableWhistleESP = state and true or false
    if Config.EnableWhistleESP then
        playerEspAttachAll()
    else
        for player in pairs(playerEspEntries) do
            playerEspDestroyEntry(player)
        end
    end
    SaveSettings()
end

table.insert(playerEspConnections, Players.PlayerAdded:Connect(function(player)
    if Config.EnableWhistleESP then
        playerEspAttachPlayer(player)
    end
end))
table.insert(playerEspConnections, Players.PlayerRemoving:Connect(function(player)
    playerEspDestroyEntry(player)
end))
table.insert(playerEspConnections, RunService.Heartbeat:Connect(function(deltaTime)
    playerEspAccumulator = playerEspAccumulator + deltaTime
    if playerEspAccumulator >= PLAYER_ESP_REFRESH then
        playerEspAccumulator = 0
        if Config.EnableWhistleESP then
            playerEspRefresh()
        end
    end
end))

print("\nConfiguring Map tab...")

TabMap:CreateLabel("CARACTERÍSTICAS DEL MAPA", "MAP FEATURES", 14)
TabMap:CreateDivider()

TabMap:CreateLabel("FullBright - Sistema de Iluminación", "FullBright - Illumination System", 12)
TabMap:CreateDivider()


TabMap:CreateFloatingToggle("FullBright", Config.EnableFullBright, function(state)
    setFullBrightEnabled(state)
    print(state and "✅ FullBright ENABLED" or "❌ FullBright DISABLED")
end)

TabMap:CreateLabel("Elimina la oscuridad y mejora la visibilidad", "Removes darkness and improves visibility", 11)

TabMap:CreateDivider()
TabMap:CreateLabel("Ticket Automático", "Auto Ticket", 12)
TabMap:CreateDivider()

TabMap:CreateFloatingToggle("Ticket Automático", "Auto Ticket", Config.EnableAutoTicket, function(state)
    Config.EnableAutoTicket = state
    print(state and "✅ Auto Ticket ENABLED" or "❌ Auto Ticket DISABLED")
    SaveSettings()
end)

TabMap:CreateLabel("Te teletransporta a cada ticket disponible en el mapa", "Teleports you to every available ticket on the map", 11)

TabMap:CreateDivider()
TabMap:CreateLabel("Escape del Vacío", "Void Escape", 12)
TabMap:CreateDivider()

TabMap:CreateFloatingToggle("Escape del Vacío", "Void Escape", Config.EnableVoidEscape, function(state)
    Config.EnableVoidEscape = state
    print(state and "✅ Void Escape ENABLED" or "❌ Void Escape DISABLED")
    SaveSettings()
end)

TabMap:CreateLabel("Te manda muy alto en el aire, lejos de todo peligro", "Sends you high into the air, away from all danger", 11)

TabMap:CreateDivider()
TabMap:CreateLabel("AFK Map", "AFK Map", 12)
TabMap:CreateDivider()

TabMap:CreateFloatingToggle("AFK Map", "AFK Map", Config.EnableAfkMap, function(state)
    local ok, result = loadAfkMap()
    if not ok then
        print("[AfkMap] ❌ " .. tostring(result))
        return
    end
    setAfkMapEnabled(state)
    print(state and ("✅ AFK Map ENABLED — " .. tostring(result) .. " partes cargadas") or "❌ AFK Map DISABLED")
end)

TabMap:CreateLabel("Spawnea en tu mini mapa personal al activar", "Spawns you in your personal mini map when enabled", 11)

TabMap:CreateDivider()
TabMap:CreateLabel("Anti AFK", "Anti AFK", 12)
TabMap:CreateDivider()

TabMap:CreateFloatingToggle("Anti AFK", "Anti AFK", Config.EnableAntiAfk, function(state)
    setAntiAfkEnabled(state)
    print(state and "✅ Anti AFK ENABLED" or "❌ Anti AFK DISABLED")
end)

TabMap:CreateLabel("Evita que el juego te expulse por inactividad", "Prevents the game from kicking you for inactivity", 11)

TabMap:CreateDivider()
TabMap:CreateLabel("ESP DE JUGADORES", "PLAYER ESP", 12)
TabMap:CreateDivider()

TabMap:CreateFloatingToggle("ESP de Jugadores", "Player ESP", Config.EnableWhistleESP, function(state)
    playerEspSetEnabled(state)
    print(state and "✅ Player ESP ENABLED" or "❌ Player ESP DISABLED")
end)

TabMap:CreateLabel("Contorno blanco, nombre y distancia; rojo al ser derribado", "White outline, name and distance; red when downed", 11)

print("✅ MAP: Tab completed")

if Config.EnableWhistleESP then
    task.defer(function()
        playerEspSetEnabled(true)
    end)
end

if Config.EnableFullBright then
    task.defer(function()
        setFullBrightEnabled(true)
    end)
end

--// ═════════════════════════════════════════════════════════════════════════════
--// TAB: EXTRA
--// ═════════════════════════════════════════════════════════════════════════════

print("\nConfiguring Extra tab...")

TabExtra:CreateLabel("AURA DE REVIVIR", "REVIVE AURA", 14)
TabExtra:CreateDivider()

TabExtra:CreateFloatingToggle("Revivir", "Revive", Config.EnableReviveAura, function(state)
    Config.EnableReviveAura = state
    print(state and "✅ Revive Aura ENABLED" or "❌ Revive Aura DISABLED")
    SaveSettings()
end)

TabExtra:CreateLabel("Revive automáticamente a jugadores derribados cerca tuyo", "Automatically revives downed players near you", 11)

TabExtra:CreateDivider()
TabExtra:CreateLabel("AUTO CARGAR", "AUTO CARRY", 14)
TabExtra:CreateDivider()

TabExtra:CreateFloatingToggle("Cargar", "Carry", Config.EnableAutoCarry, function(state)
    Config.EnableAutoCarry = state
    print(state and "✅ Auto Carry ENABLED" or "❌ Auto Carry DISABLED")
    SaveSettings()
end)

TabExtra:CreateLabel("Levanta automaticamente a jugadores derribados cerca tuyo", "Automatically picks up downed players near you", 11)

TabExtra:CreateDivider()
TabExtra:CreateLabel("VELOCIDAD DE EMOTE", "EMOTE SPEED", 14)
TabExtra:CreateDivider()

TabExtra:CreateFloatingToggle("Velocidad de Emote", "Emote Speed", Config.EnableEmoteSpeed, function(state)
    Config.EnableEmoteSpeed = state
    SaveSettings()
end)

TabExtra:CreateSlider(
    "Emote Speed Multiplier",
    1.0, 4.0, Config.EmoteSpeedMultiplier,
    function(value)
        Config.EmoteSpeedMultiplier = value
        print("✓ Emote Speed Multiplier: " .. value)
        SaveSettings()
    end,
    nil, 2.5
)

TabExtra:CreateLabel("Velocidad normal mientras haces emote", "Normal speed while performing an emote", 11)

TabExtra:CreateDivider()
TabExtra:CreateLabel("FORZAR SPAWN", "FORCE SPAWN", 14)
TabExtra:CreateDivider()

TabExtra:CreateFloatingButton("Forzar Spawn", "Force Spawn", function()
    local SetPlayerMode = game:GetService("ReplicatedStorage")
        :WaitForChild("Events")
        :WaitForChild("SetPlayerMode")
    pcall(function()
        SetPlayerMode:FireServer(true)
    end)
    print("[ForceSpawn] ✅ Fired")
end)

TabExtra:CreateLabel("Presiona para intentar spawnear inmediatamente", "Press to try to spawn immediately", 11)

TabExtra:CreateDivider()
TabExtra:CreateLabel("INTERRUPTOR DE LAG", "LAG SWITCH", 14)
TabExtra:CreateDivider()

TabExtra:CreateFloatingButton("Interruptor de Lag", "Lag Switch", function()
    -- Aplicar boost de salto 1.5x
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if hum and hum.Health > 0 then
        pcall(function()
            hum.JumpPower = Config.JumpHeight * 1.5
            hum.UseJumpPower = true
        end)
    end

    -- Inducir 1 segundo de lag (busy wait)
    local start = os.clock()
    while os.clock() - start < 1.0 do end

    -- Resetear jump power a valor base después de 5 segundos
    task.delay(5, function()
        local c = LocalPlayer.Character
        local h = c and c:FindFirstChildOfClass("Humanoid")
        if h then
            pcall(function()
                h.JumpPower = Config.JumpHeight
                h.UseJumpPower = true
            end)
        end
        print("[LagSwitch] Jump reset")
    end)

    print("[LagSwitch] Fired - 1.5x jump activo por 5s")
end)

TabExtra:CreateLabel("Induce 1s de lag y activa salto 1.5x por 5 segundos", "Induces 1s of lag and activates 1.5x jump for 5 seconds", 11)

TabExtra:CreateDivider()

TabExtra:CreateToggle("Farm de Dinero", "Farm of money", Config.EnableGoodMode, function(state)
    Config.EnableGoodMode = state
    print(state and "✅ Farm of money ENABLED" or "❌ Farm of money DISABLED")
    SaveSettings()
end, nil, {
    imageId     = "rbxassetid://118509384908055",
    name        = "NhatMinh",
    description = "Option given by NhatMinh, he is thanked too much for this contribution to yin yang"
})

print("OK EXTRA: Tab completed")

--// ═════════════════════════════════════════════════════════════════════════════
--// TAB: EMOTE
--// ═════════════════════════════════════════════════════════════════════════════

print("\nConfiguring Emote tab...")

--// ── EMOTES VISUALES: datos (animId/soundId/emoteName) ──────────────────
local EMOTES = {
    { label = "RockinStride",       animId = "rbxassetid://111570274714712", soundId = "rbxassetid://114846968069031", emoteName = "RockinStride" },
    { label = "ZombieStride 1",     animId = "rbxassetid://125497596837433", soundId = "", emoteName = "ZombieStride", emoteKey = "ZombieStride1" },
    { label = "ZombieStride 2",     animId = "rbxassetid://84248734120911",  soundId = "", emoteName = "ZombieStride", emoteKey = "ZombieStride2" },
    { label = "ZombieStride 3",     animId = "rbxassetid://73383479205643",  soundId = "", emoteName = "ZombieStride", emoteKey = "ZombieStride3" },
    { label = "Ghoulish Galleon",   animId = "rbxassetid://119226805386886", soundId = "", emoteName = "GhoulishGalleon", emoteKey = "GhoulishGalleon" },
    { label = "Solar Slayer 1",      animId = "rbxassetid://117293869950361", soundId = "", emoteName = "SolarSlayer", emoteKey = "SolarSlayer1", selectionVariant = "1" },
    { label = "Solar Slayer 2",      animId = "rbxassetid://111930618562488", soundId = "", emoteName = "SolarSlayer", emoteKey = "SolarSlayer2", selectionVariant = "2" },
    { label = "Solar Combine",       animId = "rbxassetid://117293869950361", soundId = "", emoteName = "SolarSlayer", emoteKey = "SolarCombine", selectionVariants = {"1", "2"} },
    { label = "Griddy",             animId = "rbxassetid://82579103193493",  soundId = "", emoteName = "Griddy" },
    { label = "BananaDance",  animId = "rbxassetid://76664554569994",  soundId = "", emoteName = "BananaDance" },
    { label = "OiiaOiia",     animId = "rbxassetid://82698079573413",  soundId = "", emoteName = "OiiaOiia" },
    { label = "Writhing",     animId = "rbxassetid://118337616190939", soundId = "", emoteName = "Writhing" },
    { label = "AstroSlide",   animId = "rbxassetid://103095884226031", soundId = "", emoteName = "AstroSlide" },
    { label = "Popular",      animId = "rbxassetid://126396646825215", soundId = "", emoteName = "Popular" },
    { label = "Moonwalk",     animId = "rbxassetid://97074392214454",  soundId = "", emoteName = "Moonwalk" },
    { label = "Golpeador",    animId = "",                             soundId = "", emoteName = "Golpeador" },
    { label = "Headless Baller", animId = "rbxassetid://113685905137377", soundId = "", emoteName = "HeadlessBaller" },
    { label = "Banger",          animId = "rbxassetid://89805417778763",  soundId = "", emoteName = "Banger"         },
    { label = "Broom of Doom",   animId = "rbxassetid://112692937680187", soundId = "", emoteName = "Broom"          },
    { label = "Bold March",      animId = "rbxassetid://89755710041366",  soundId = "", emoteName = "BoldMarch"      },
}

-- IDs del idle/loop base del Rig — nunca sustituir estos
local IDLE_IDS = {
    ["rbxassetid://17418770762"] = true,
    ["rbxassetid://17418484618"] = true,
    ["rbxassetid://17418691532"] = true,
    ["rbxassetid://17418712872"] = true,
    ["rbxassetid://17418485963"] = true,
    ["rbxassetid://17418788540"] = true,
    ["rbxassetid://17418483149"] = true,
    ["rbxassetid://17524391617"] = true,
}

local EmoteState = {
    enabled  = true,
    emoteIdx = nil,   -- índice del emote seleccionado en EMOTES
}

local activeVisualTrack = nil
local activeProps       = {}
local activeSound       = nil
local activeSoundKillerConn = nil
local substituting      = false
local emoteGeneration  = 0 -- invalida creaciones de pistas que llegan tarde durante una transición de ronda
local emoteConnections  = {}
local emoteLifecycleConnections = {}
local roundCleanupConnections = {}
local inEvadeEmote      = false  -- true solo cuando Evade está en estado "Emote" real (se setea desde el hook SpeedChange más abajo)

function disconnectEmoteConnections(list)
    for _, connection in ipairs(list) do
        pcall(function() connection:Disconnect() end)
    end
    table.clear(list)
end

local emoteModuleCache = {}
function buildEmoteCache()
    local items = ReplicatedStorage:FindFirstChild("Items")
    if not items then return end

    local roots = {}
    local packs = items:FindFirstChild("ItemPacks")
    if packs then table.insert(roots, packs) end
    local baseItems = items:FindFirstChild("BaseItems")
    if baseItems then table.insert(roots, baseItems) end

    for _, emote in ipairs(EMOTES) do
        local acceptedNames = {[emote.emoteName] = true}
        if emote.emoteName == "Broom" then
            acceptedNames.BroomOfDoom = true
            acceptedNames["Broom of Doom"] = true
        end

        for _, root in ipairs(roots) do
            local found = false
            for _, inst in ipairs(root:GetDescendants()) do
                if inst.ClassName == "ModuleScript" and acceptedNames[inst.Name] then
                    local hasEmoteModel = inst:FindFirstChild("CharacterClassic", true)
                        or inst:FindFirstChild("EmoteModel", true)
                    if emote.emoteName ~= "Broom" or hasEmoteModel then
                        emoteModuleCache[emote.emoteKey or emote.emoteName] = inst
                        found = true
                        break
                    end
                end
            end
            if found then break end
        end
    end

    if not emoteModuleCache.Broom then
        for _, inst in ipairs(ReplicatedStorage:GetDescendants()) do
            if inst.ClassName == "ModuleScript"
            and (inst.Name == "Broom" or inst.Name == "BroomOfDoom" or inst.Name == "Broom of Doom") then
                local hasEmoteModel = inst:FindFirstChild("CharacterClassic", true)
                    or inst:FindFirstChild("EmoteModel", true)
                if hasEmoteModel then
                    emoteModuleCache.Broom = inst
                    break
                end
            end
        end
    end
end

function cleanupActiveEmote()
    emoteGeneration = emoteGeneration + 1
    if activeVisualTrack then
        pcall(function() activeVisualTrack:Stop(0) end)
        activeVisualTrack = nil
    end
    for _, obj in ipairs(activeProps) do
        pcall(function() obj:Destroy() end)
    end
    activeProps = {}
    if activeSound then
        pcall(function() activeSound:Stop(); activeSound:Destroy() end)
        activeSound = nil
    end
    if activeSoundKillerConn then
        pcall(function() activeSoundKillerConn:Disconnect() end)
        activeSoundKillerConn = nil
    end
end

function resetEmoteRuntime()
    cleanupActiveEmote()
    inEvadeEmote = false
    substituting = false
end

function bindEmoteCharacterLifecycle(char)
    disconnectEmoteConnections(emoteLifecycleConnections)
    if not char then return end

    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if humanoid then
        table.insert(emoteLifecycleConnections, humanoid.Died:Connect(resetEmoteRuntime))
    end

    table.insert(emoteLifecycleConnections, char.AncestryChanged:Connect(function(_, parent)
        if not parent then
            resetEmoteRuntime()
        end
    end))
end

function setupRoundCleanupWatchers()
    disconnectEmoteConnections(roundCleanupConnections)
    local updateMapBound = false
    local stateBound = false

    task.spawn(function()
        -- Evade puede crear estos objetos después de la interfaz. Reintentar evita
        -- que una única carrera de carga deje el emote visual sin limpieza de ronda.
        for _ = 1, 90 do
            local events = ReplicatedStorage:FindFirstChild("Events")
            local updateMap = events and events:FindFirstChild("UpdateMap")
            if not updateMapBound and updateMap and updateMap:IsA("RemoteEvent") then
                updateMapBound = true
                table.insert(roundCleanupConnections, updateMap.OnClientEvent:Connect(resetEmoteRuntime))
            end

            local services = ReplicatedStorage:FindFirstChild("Services")
            local data = services and services:FindFirstChild("Data")
            local registry = data and data:FindFirstChild("ServerStateRegistryService")
            local changedEvent = registry and registry:FindFirstChild("ChangedEvent")
            if not stateBound and changedEvent and changedEvent:IsA("BindableEvent") then
                stateBound = true
                table.insert(roundCleanupConnections, changedEvent.Event:Connect(function(key, value)
                    if (key == "MapLoading" and value == true)
                    or (key == "Voting" and value == true) then
                        resetEmoteRuntime()
                    end
                end))
            end

            if updateMapBound and stateBound then break end
            task.wait(1)
        end
    end)
end

function rebindRockinExternalJointPart(part, clonedEM, rig)
    if not part or clonedEM:IsAncestorOf(part) then
        return part
    end

    local rigPart = rig:FindFirstChild(part.Name, true)
    if rigPart and not clonedEM:IsAncestorOf(rigPart) then
        return rigPart
    end

    return part
end

function getRelativeInstancePath(inst, root)
    if not inst or not root then return nil end

    local ok, instFull = pcall(function() return inst:GetFullName() end)
    local okRoot, rootFull = pcall(function() return root:GetFullName() end)
    if not ok or not okRoot then return nil end

    local prefix = rootFull .. "."
    if instFull:sub(1, #prefix) ~= prefix then return nil end
    return instFull:sub(#prefix + 1)
end

function findGhoulishJointPart(part, sourceModel, clonedModel, rig)
    if not part then return nil end
    if clonedModel:IsAncestorOf(part) then return part end

    local relativePath = getRelativeInstancePath(part, sourceModel)
    if relativePath then
        local clonePart = clonedModel
        for segment in string.gmatch(relativePath, "[^%.]+") do
            clonePart = clonePart:FindFirstChild(segment)
            if not clonePart then break end
        end
        if clonePart and clonePart:IsA("BasePart") then
            return clonePart
        end
    end

    local rigPart = rig:FindFirstChild(part.Name, true)
    if rigPart and not clonedModel:IsAncestorOf(rigPart) then
        return rigPart
    end

    return part
end

-- Misma transformación que la versión funcional de EmotesSeleccionados:
-- Ghoulish queda con el barco abajo y hacia delante, siguiendo al Rig.
local GHOULISH_ROOT_OFFSET = CFrame.new(0, -4.1, 3)

function spawnEmoteProps(emote, rig)
    local cacheKey = emote.emoteKey or emote.emoteName
    local mod = emoteModuleCache[cacheKey]
    if not mod and emote.emoteName == "Broom" then
        buildEmoteCache()
        mod = emoteModuleCache[cacheKey]
    end
    if not mod then return end

    local cc, emoteModel
    if emote.selectionVariant then
        local selection = mod:FindFirstChild("Selection")
        local variant = selection and selection:FindFirstChild(emote.selectionVariant)
        cc = variant and variant:FindFirstChild("CharacterClassic")
        emoteModel = cc and cc:FindFirstChild("EmoteModel")
    end
    if not emoteModel then
        cc = mod:FindFirstChild("CharacterClassic")
        emoteModel = cc and cc:FindFirstChild("EmoteModel")
    end
    if not emoteModel and emote.emoteName == "Broom" then
        for _, inst in ipairs(mod:GetDescendants()) do
            if inst.Name == "EmoteModel" then
                emoteModel = inst
                break
            end
        end
    end
    if not emoteModel and not emote.selectionVariants then return end

    local propsParent = rig
    if emote.emoteName == "SolarSlayer" then
        -- Mantener las espadas fuera de los hijos Model directos del Rig.
        -- Así no quedan expuestas a la limpieza de props del emote real ni
        -- a la aparición de otro objeto 3D de espalda.
        local solarFolder = rig:FindFirstChild("__YY_SolarSlayerProps")
        if not solarFolder then
            solarFolder = Instance.new("Folder")
            solarFolder.Name = "__YY_SolarSlayerProps"
            solarFolder.Parent = rig
        end
        propsParent = solarFolder
    end

    if emote.selectionVariants and emote.emoteName == "SolarSlayer" then
        local selection = mod:FindFirstChild("Selection")
        if selection then
            for _, variantId in ipairs(emote.selectionVariants) do
                local variant = selection:FindFirstChild(variantId)
                local variantCC = variant and variant:FindFirstChild("CharacterClassic")
                local variantModel = variantCC and variantCC:FindFirstChild("EmoteModel")
                if variantModel then
                    local combinedProp = variantModel:Clone()
                    combinedProp.Parent = propsParent
                    table.insert(activeProps, combinedProp)
                    for _, prop in ipairs(combinedProp:GetDescendants()) do
                        if prop:IsA("BasePart") then
                            pcall(function()
                                prop.CanCollide = false
                                prop.CanTouch = false
                                prop.CanQuery = false
                                prop.Massless = true
                            end)
                        end
                    end
                    for _, motor in ipairs(combinedProp:GetDescendants()) do
                        if motor:IsA("Motor6D") or motor:IsA("Weld") or motor:IsA("WeldConstraint") then
                            local sourcePart0 = motor.Part0
                            if sourcePart0 and not combinedProp:IsAncestorOf(sourcePart0) then
                                local rigPart = rig:FindFirstChild(sourcePart0.Name)
                                if rigPart and rigPart:IsA("BasePart") then motor.Part0 = rigPart end
                            end
                            local sourcePart1 = motor.Part1
                            if sourcePart1 and not combinedProp:IsAncestorOf(sourcePart1) then
                                local rigPart = rig:FindFirstChild(sourcePart1.Name)
                                if rigPart and rigPart:IsA("BasePart") then motor.Part1 = rigPart end
                            end
                        end
                    end
                end
            end
        end
        return
    end

    local clonedEM = emoteModel:Clone()
    clonedEM.Parent = propsParent
    table.insert(activeProps, clonedEM)

    if emote.emoteName == "SolarSlayer"
    or emote.emoteName == "GhoulishGalleon"
    or emote.emoteName == "Broom" then
        for _, prop in ipairs(clonedEM:GetDescendants()) do
            if prop:IsA("BasePart") then
                pcall(function() prop.CanCollide = false end)
                pcall(function() prop.CanTouch = false end)
                pcall(function() prop.CanQuery = false end)
                pcall(function() prop.Massless = true end)
            end
        end
    end

    if emote.emoteName == "GhoulishGalleon" then
        local rigRoot = rig:FindFirstChild("HumanoidRootPart", true)
            or rig:FindFirstChild("Torso", true)
        local rootMotor = nil
        local hull = nil

        -- Motor6D raíz real detectado en el escaneo: EmoteModel.Hull.
        for _, joint in ipairs(clonedEM:GetDescendants()) do
            if joint:IsA("Motor6D")
            and joint.Name == "Hull"
            and joint.Parent == clonedEM then
                rootMotor = joint
            end
            if joint:IsA("BasePart") and joint.Name == "Hull" then
                hull = hull or joint
            end
        end

        -- C0/C1 conservan la orientación y el offset original del barco.
        local targetCFrame = hull and hull.CFrame or nil
        if rootMotor and rigRoot then
            local okC0, c0 = pcall(function() return rootMotor.C0 end)
            local okC1, c1 = pcall(function() return rootMotor.C1 end)
            if okC0 and okC1 and typeof(c0) == "CFrame" and typeof(c1) == "CFrame" then
                targetCFrame = rigRoot.CFrame
                    * GHOULISH_ROOT_OFFSET
                    * c0
                    * c1:Inverse()
            end
        end
        if hull and targetCFrame then
            pcall(function() hull.CFrame = targetCFrame end)
        end

        -- Rebindar todos los joints internos por ruta; no el raíz.
        for _, joint in ipairs(clonedEM:GetDescendants()) do
            if joint ~= rootMotor
            and (joint:IsA("Motor6D") or joint:IsA("Weld")) then
                joint.Part0 = findGhoulishJointPart(joint.Part0, emoteModel, clonedEM, rig)
                joint.Part1 = findGhoulishJointPart(joint.Part1, emoteModel, clonedEM, rig)
            end
        end

        if rootMotor then
            rootMotor:Destroy()
        end

        -- Unión persistente para que el barco siga al jugador.
        if rigRoot and hull then
            local follow = Instance.new("WeldConstraint")
            follow.Name = "__YY_GhoulishFollow"
            follow.Part0 = rigRoot
            follow.Part1 = hull
            follow.Parent = rigRoot
            table.insert(activeProps, follow)
        end
        return
    end

    -- Solar Slayer variante 1 usa Weld y variante 2 usa Motor6D.
    -- El Part0 externo se resuelve solo contra una parte corporal directa del
    -- Rig. No se usa búsqueda recursiva: un accesorio 3D de espalda puede
    -- contener otra pieza con el mismo nombre y robar el enlace de la espada.
    for _, motor in ipairs(clonedEM:GetDescendants()) do
        if motor:IsA("Motor6D")
        or ((emote.emoteName == "SolarSlayer" or emote.emoteName == "Broom")
        and (motor:IsA("Weld") or motor:IsA("WeldConstraint"))) then
            local sourcePart0 = motor.Part0
            if sourcePart0 and not clonedEM:IsAncestorOf(sourcePart0) then
                local rigPart = rig:FindFirstChild(sourcePart0.Name)
                if rigPart and rigPart:IsA("BasePart") then
                    motor.Part0 = rigPart
                end
            end

            -- Part1 debe permanecer en la espada cuando ya pertenece al clon.
            -- Solo se remapea si el enlace quedó apuntando fuera del prop y
            -- existe una parte corporal directa con ese mismo nombre.
            local sourcePart1 = motor.Part1
            if sourcePart1 and not clonedEM:IsAncestorOf(sourcePart1) then
                local rigPart = rig:FindFirstChild(sourcePart1.Name)
                if rigPart and rigPart:IsA("BasePart") then
                    motor.Part1 = rigPart
                end
            end
        end
    end
end

function playEmoteSound(emote, rig)
    if emote.soundId == "" then return end
    local snd = Instance.new("Sound")
    snd.SoundId = emote.soundId
    snd.Volume  = 0.5
    snd.Looped  = false
    snd.Parent  = rig
    snd:Play()
    activeSound = snd
end

function onEmoteAnimPlayed(anim, track)
    if not EmoteState.enabled or not EmoteState.emoteIdx then return end
    if substituting then return end

    local id = track.Animation and track.Animation.AnimationId
    if not id then return end

    -- Si el idle vuelve → el emote real terminó, parar visual.
    -- Ghoulish puede recibir idle mientras su track visual sigue activo.
    if IDLE_IDS[id] then
        local selectedEmote = EmoteState.emoteIdx and EMOTES[EmoteState.emoteIdx]
        if selectedEmote
        and selectedEmote.emoteName == "GhoulishGalleon"
        and inEvadeEmote then
            return
        end
        if activeVisualTrack then task.spawn(cleanupActiveEmote) end
        return
    end

    -- Solo sustituir cuando Evade está en estado Emote real
    if not inEvadeEmote then return end

    local emote = EMOTES[EmoteState.emoteIdx]
    if not emote or emote.animId == "" then return end
    if emote.animId == id then return end  -- ya es el correcto

    substituting = true
    cleanupActiveEmote()
    local visualEpoch = emoteGeneration

    -- Silenciar sonidos del emote real en el Rig (props 3D)
    pcall(function()
        local rig = anim.Parent.Parent
        for _, snd in ipairs(rig:GetDescendants()) do
            if snd:IsA("Sound") and snd.IsPlaying then snd:Stop() end
        end
    end)

    -- EmoteSound vive en Workspace.Players.<n>.HumanoidRootPart.SoundPos, NO en el Rig.
    pcall(function()
        if activeSoundKillerConn then
            activeSoundKillerConn:Disconnect()
            activeSoundKillerConn = nil
        end
        local playersFolder = workspace:FindFirstChild("Players")
        local charModel = playersFolder and playersFolder:FindFirstChild(LocalPlayer.Name)
        if charModel then
            local hrp = charModel:FindFirstChild("HumanoidRootPart")
            local soundPos = hrp and hrp:FindFirstChild("SoundPos")
            if soundPos then
                for _, snd in ipairs(soundPos:GetChildren()) do
                    if snd:IsA("Sound") and snd.Name == "EmoteSound" then
                        pcall(function() snd:Stop() end)
                    end
                end
            end
            activeSoundKillerConn = charModel.DescendantAdded:Connect(function(inst)
                if inst:IsA("Sound") and inst.Name == "EmoteSound" then
                    task.defer(function()
                        pcall(function() inst:Stop() end)
                    end)
                end
            end)
        end
    end)

    -- Destruir props 3D del emote real para que no queden flotando
    pcall(function()
        local rig = anim.Parent.Parent
        for _, obj in ipairs(rig:GetChildren()) do
            if obj:IsA("Model") and obj ~= rig:FindFirstChildOfClass("Humanoid") then
                obj:Destroy()
            end
        end
    end)

    pcall(function() track:Stop(0) end)

    local fake = Instance.new("Animation")
    fake.AnimationId = emote.animId
    local ok, visualTrack = pcall(function()
        return anim:LoadAnimation(fake)
    end)

    if ok and visualTrack then
        if visualEpoch ~= emoteGeneration or not inEvadeEmote then
            pcall(function() visualTrack:Stop(0); fake:Destroy() end)
            task.defer(function() substituting = false end)
            return
        end
        activeVisualTrack = visualTrack
        visualTrack.Priority = Enum.AnimationPriority.Action
        if emote.emoteName == "GhoulishGalleon" then
            visualTrack.Looped = true
        end
        visualTrack:Play()
        if visualEpoch ~= emoteGeneration or not inEvadeEmote then
            if activeVisualTrack == visualTrack then activeVisualTrack = nil end
            pcall(function() visualTrack:Stop(0); fake:Destroy() end)
            task.defer(function() substituting = false end)
            return
        end

        pcall(function()
            local rig = anim.Parent.Parent
            spawnEmoteProps(emote, rig)
        end)

        pcall(function()
            local rig = anim.Parent.Parent
            playEmoteSound(emote, rig)
        end)

        visualTrack.Stopped:Connect(function()
            -- Una pista anterior puede detenerse después de que una nueva ya fue
            -- guardada. Solo la propia pista puede limpiar su sesión global.
            pcall(function() fake:Destroy() end)
            if activeVisualTrack ~= visualTrack then return end
            activeVisualTrack = nil
            for _, obj in ipairs(activeProps) do
                pcall(function() obj:Destroy() end)
            end
            activeProps = {}
            if activeSound then
                pcall(function() activeSound:Stop(); activeSound:Destroy() end)
                activeSound = nil
            end
        end)
    end

    task.defer(function() substituting = false end)
end

function connectEmoteRig(humAnim, acAnim)
    for _, c in ipairs(emoteConnections) do pcall(function() c:Disconnect() end) end
    emoteConnections = {}
    if humAnim then
        emoteConnections[#emoteConnections+1] = humAnim.AnimationPlayed:Connect(function(track)
            onEmoteAnimPlayed(humAnim, track)
        end)
    end
    if acAnim then
        emoteConnections[#emoteConnections+1] = acAnim.AnimationPlayed:Connect(function(track)
            onEmoteAnimPlayed(acAnim, track)
        end)
    end
end

function getEmoteRigAnimators()
    local rigs = workspace:FindFirstChild("Rigs")
    if not rigs then return nil, nil end
    local rig = rigs:FindFirstChild(LocalPlayer.Name)
    if not rig then return nil, nil end
    local hum     = rig:FindFirstChildOfClass("Humanoid")
    local humAnim = hum and hum:FindFirstChildOfClass("Animator")
    local ac      = rig:FindFirstChildOfClass("AnimationController")
    local acAnim  = ac and ac:FindFirstChildOfClass("Animator")
    return humAnim, acAnim
end

buildEmoteCache()

task.spawn(function()
    local humAnim, acAnim = getEmoteRigAnimators()
    if not humAnim and not acAnim then
        local rigs = workspace:WaitForChild("Rigs", 10)
        if not rigs then return end
        local rig = rigs:WaitForChild(LocalPlayer.Name, 10)
        if not rig then return end
        local hum = rig:WaitForChild("Humanoid", 5)
        humAnim = hum and hum:WaitForChild("Animator", 5)
        local ac = rig:WaitForChild("AnimationController", 5)
        acAnim  = ac and ac:WaitForChild("Animator", 5)
    end
    pcall(connectEmoteRig, humAnim, acAnim)
end)

LocalPlayer.CharacterAdded:Connect(function(char)
    bindEmoteCharacterLifecycle(char)
    task.wait(1)
    local humAnim, acAnim = getEmoteRigAnimators()
    if humAnim or acAnim then pcall(connectEmoteRig, humAnim, acAnim) end
end)

LocalPlayer.CharacterRemoving:Connect(function()
    resetEmoteRuntime()
    disconnectEmoteConnections(emoteLifecycleConnections)
end)

bindEmoteCharacterLifecycle(LocalPlayer.Character)
setupRoundCleanupWatchers()

--// ── GRID VISUAL: 9 emotes con thumbnail, orden pedido (Golpeador sin thumbnail, queda fuera) ──
local EmoteGridItems = {
    { Image = "rbxassetid://74660035371902",  LabelES = "Broom of Doom",   LabelEN = "Broom of Doom",   emoteKey = "Broom"          },
    { Image = "rbxassetid://73174831119162",  LabelES = "Griddy",         LabelEN = "Griddy",         emoteKey = "Griddy" },
    { Image = "rbxassetid://98064200423688",  LabelES = "Popular",        LabelEN = "Popular",        emoteKey = "Popular" },
    { Image = "rbxassetid://127215087541402", LabelES = "Writhing",       LabelEN = "Writhing",       emoteKey = "Writhing" },
    { Image = "rbxassetid://84918625639293",  LabelES = "Astro Slide",    LabelEN = "Astro Slide",    emoteKey = "AstroSlide" },
    { Image = "rbxassetid://84060840420307",  LabelES = "Moonwalk",       LabelEN = "Moonwalk",       emoteKey = "Moonwalk" },
    { Image = "rbxassetid://71947616607317",  LabelES = "Banana Dance",   LabelEN = "Banana Dance",   emoteKey = "BananaDance" },
    { Image = "rbxassetid://110443286307594", LabelES = "OiiaOiia",       LabelEN = "OiiaOiia",       emoteKey = "OiiaOiia" },
    { Image = "rbxassetid://86302242319867",  LabelES = "Zombie Stride 1", LabelEN = "Zombie Stride 1", emoteKey = "ZombieStride1" },
    { Image = "rbxassetid://86302242319867",  LabelES = "Zombie Stride 2", LabelEN = "Zombie Stride 2", emoteKey = "ZombieStride2" },
    { Image = "rbxassetid://86302242319867",  LabelES = "Zombie Stride 3", LabelEN = "Zombie Stride 3", emoteKey = "ZombieStride3" },
    { Image = "rbxassetid://136281024873093", LabelES = "Ghoulish Galleon", LabelEN = "Ghoulish Galleon", emoteKey = "GhoulishGalleon" },
    { Image = "rbxassetid://124775667511401", LabelES = "Solar Slayer 1",  LabelEN = "Solar Slayer 1",  emoteKey = "SolarSlayer1" },
    { Image = "rbxassetid://119932397286210", LabelES = "Solar Slayer 2",  LabelEN = "Solar Slayer 2",  emoteKey = "SolarSlayer2" },
    { Image = "rbxassetid://124775667511401", LabelES = "Solar Combine",   LabelEN = "Solar Combine",   emoteKey = "SolarCombine" },
    { Image = "rbxassetid://88980462354208",  LabelES = "Rockin' Stride",  LabelEN = "Rockin' Stride",  emoteKey = "RockinStride"   },
    { Image = "rbxassetid://120404811560579", LabelES = "Headless Baller", LabelEN = "Headless Baller", emoteKey = "HeadlessBaller" },
    { Image = "rbxassetid://88586004283343",  LabelES = "Banger",          LabelEN = "Banger",          emoteKey = "Banger"         },
    { Image = "rbxassetid://88448996681393",  LabelES = "Bold March",      LabelEN = "Bold March",      emoteKey = "BoldMarch"      },
}

local emoteIndexByKey = {}
for i, e in ipairs(EMOTES) do
    emoteIndexByKey[e.emoteKey or e.emoteName] = i
end

getgenv().YYBuildPCEmoteChanger = function()
    --// Modo PC: reemplaza el grid táctil por un cambiador de emote visual.
    --// La lógica de clonación se mantiene aislada dentro de esta pestaña.
    EmoteState.enabled = false

    TabEmote:CreateLabel("CAMBIADOR VISUAL DE EMOTES", "VISUAL EMOTE CHANGER", 16)
    TabEmote:CreateLabel(
        "Escribe el nombre del emote actual y el emote visual que quieres usar.",
        "Enter the current emote name and the visual emote you want to use.",
        11
    )
    TabEmote:CreateDivider()

    local targetEmoteName = ""
    local sourceEmoteName = ""
    local statusLabel

    function trimName(value)
        value = tostring(value or "")
        return value:gsub("^%s+", ""):gsub("%s+$", "")
    end

    function setStatus(text, color)
        if statusLabel and statusLabel.Parent then
            statusLabel.Text = text
            statusLabel.TextColor3 = color
        end
    end

    function createNameBox(titleSpanish, titleEnglish, placeholderSpanish, placeholderEnglish, setter)
        TabEmote:CreateLabel(titleSpanish, titleEnglish, 12)

        local input = Instance.new("TextBox")
        input.Name = "PCEmoteNameInput"
        input.Parent = TabEmote.Page
        input.Size = UDim2.new(1, 0, 0, 42)
        input.BackgroundColor3 = TabEmote.Button.BackgroundColor3
        input.BackgroundTransparency = 0.08
        input.BorderSizePixel = 0
        input.ClearTextOnFocus = false
        input.Font = Enum.Font.Gotham
        input.PlaceholderText = placeholderSpanish
        input.PlaceholderColor3 = Color3.fromRGB(165, 170, 185)
        input.Text = ""
        input.TextColor3 = Color3.fromRGB(245, 245, 250)
        input.TextSize = 14
        input.TextXAlignment = Enum.TextXAlignment.Left
        input.TextTruncate = Enum.TextTruncate.AtEnd
        input.ZIndex = 10

        local padding = Instance.new("UIPadding")
        padding.PaddingLeft = UDim.new(0, 14)
        padding.PaddingRight = UDim.new(0, 14)
        padding.Parent = input

        local inputCorner = Instance.new("UICorner")
        inputCorner.CornerRadius = UDim.new(0, 8)
        inputCorner.Parent = input

        local inputStroke = Instance.new("UIStroke")
        inputStroke.Color = Color3.fromRGB(120, 125, 145)
        inputStroke.Thickness = 1
        inputStroke.Transparency = 0.42
        inputStroke.Parent = input

        input:SetAttribute("ThemeRole", "Secondary")
        input:SetAttribute("ThemeTextRole", "Text")
        input:SetAttribute("PlaceholderSpanish", placeholderSpanish)
        input:SetAttribute("PlaceholderEnglish", placeholderEnglish)
        inputStroke:SetAttribute("ThemeRole", "Stroke")

        input.Focused:Connect(function()
            inputStroke.Transparency = 0.05
            inputStroke.Thickness = 1.5
        end)
        input.FocusLost:Connect(function()
            inputStroke.Transparency = 0.42
            inputStroke.Thickness = 1
        end)
        input:GetPropertyChangedSignal("Text"):Connect(function()
            setter(trimName(input.Text))
        end)
        return input
    end

    createNameBox(
        "EMOTE ACTUAL / TARGET",
        "CURRENT EMOTE / TARGET",
        "Ejemplo: Popular",
        "Example: Popular",
        function(value) targetEmoteName = value end
    )

    createNameBox(
        "EMOTE VISUAL / SOURCE",
        "VISUAL EMOTE / SOURCE",
        "Ejemplo: Broom of Doom",
        "Example: Broom of Doom",
        function(value) sourceEmoteName = value end
    )

    TabEmote:CreateDivider()

    function findEmoteFolder(name)
        local rep = game:GetService("ReplicatedStorage")
        local cleanName = trimName(name)
        if cleanName == "" then return nil end

        local folder = rep:FindFirstChild(cleanName, true)
        if folder then return folder end

        local compactName = cleanName:gsub("%s+", "")
        if compactName ~= cleanName then
            folder = rep:FindFirstChild(compactName, true)
        end
        return folder
    end

    function applyVisualEmote()
        local targetName = trimName(targetEmoteName)
        local sourceName = trimName(sourceEmoteName)
        if targetName == "" or sourceName == "" then
            setStatus("Faltan los dos nombres de emote.", Color3.fromRGB(255, 185, 115))
            return
        end

        local targetFolder = findEmoteFolder(targetName)
        local sourceFolder = findEmoteFolder(sourceName)
        if not targetFolder then
            setStatus("No se encontró el emote actual: " .. targetName, Color3.fromRGB(255, 125, 125))
            return
        end
        if not sourceFolder then
            setStatus("No se encontró el emote visual: " .. sourceName, Color3.fromRGB(255, 125, 125))
            return
        end
        if targetFolder == sourceFolder then
            setStatus("El emote actual y el visual deben ser diferentes.", Color3.fromRGB(255, 185, 115))
            return
        end

        --// Preparar clones antes de destruir el target: evita dejarlo vacío si
        --// algún elemento no puede clonarse en el cliente actual.
        local clones = {}
        for _, item in ipairs(sourceFolder:GetChildren()) do
            local ok, clonedItem = pcall(function()
                return item:Clone()
            end)
            if not ok or not clonedItem then
                for _, pending in ipairs(clones) do
                    pcall(function() pending:Destroy() end)
                end
                setStatus("No se pudo preparar el emote visual.", Color3.fromRGB(255, 125, 125))
                return
            end
            table.insert(clones, clonedItem)
        end

        for _, item in ipairs(targetFolder:GetChildren()) do
            pcall(function() item:Destroy() end)
        end
        for _, clonedItem in ipairs(clones) do
            clonedItem.Parent = targetFolder
        end

        setStatus("Aplicado: " .. targetName .. " → " .. sourceName, Color3.fromRGB(150, 255, 190))
        print("✅ PC VISUAL EMOTE: " .. targetName .. " → " .. sourceName)
    end

    TabEmote:CreateButton(
        "APLICAR EMOTE VISUAL",
        "APPLY VISUAL EMOTE",
        applyVisualEmote
    )

    statusLabel = TabEmote:CreateLabel(
        "Listo para aplicar un cambio visual.",
        "Ready to apply a visual change.",
        11
    )
    statusLabel.TextColor3 = Color3.fromRGB(175, 185, 205)
    statusLabel.TextWrapped = true
    statusLabel.TextXAlignment = Enum.TextXAlignment.Left
    statusLabel:SetAttribute("ThemeTextRole", "TextDim")

    UI.PCEmoteChanger = true
end

getgenv().YYIsPCEmotePlatform = function()
    local detector = UI and UI.DetectPlatformType
    if type(detector) == "function" then
        local ok, result = pcall(detector)
        if ok then
            return result == "PC"
        end
    end

    local isPC = false
    pcall(function()
        local userInputService = game:GetService("UserInputService")
        local preferredInput = userInputService.PreferredInput
        if preferredInput == Enum.PreferredInput.KeyboardAndMouse then
            isPC = true
            return
        elseif preferredInput == Enum.PreferredInput.Touch then
            return
        end

        local hasKeyboard = userInputService.KeyboardEnabled
        local hasMouse = userInputService.MouseEnabled
        local hasTouch = userInputService.TouchEnabled
        if hasKeyboard and hasMouse and not hasTouch then
            isPC = true
            return
        end

        local camera = workspace.CurrentCamera
        local viewportWidth = camera and camera.ViewportSize.X or 0
        isPC = viewportWidth >= 1080
    end)
    return isPC
end

if getgenv().YYIsPCEmotePlatform() then
    getgenv().YYBuildPCEmoteChanger()
    print("✅ EMOTE: PC visual changer loaded")
else
TabEmote:CreateImageGrid("Emotes", "Emotes", EmoteGridItems, function(item, index, isSelected)
    if isSelected then
        EmoteState.emoteIdx = emoteIndexByKey[item.emoteKey]
    else
        EmoteState.emoteIdx = nil
        cleanupActiveEmote()
    end
end, true)
end

print("✅ EMOTE: Tab completed")

print("\n✅ OK: INTERFACE FULLY CONFIGURED")

--// ═════════════════════════════════════════════════════════════════════════════
--// MOVEMENT FUNCTIONS
--// ═════════════════════════════════════════════════════════════════════════════

print("\nLoading movement functions...")

function applyTeleportWalk()
    if not Config.EnableTeleportWalk and not Config.EnableTrimpTeleport then return end
    
    local char = LocalPlayer.Character
    if not char or not char.PrimaryPart then return end
    
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum or hum.Health <= 0 then return end
    
    if hum.MoveDirection.Magnitude > 0 then
        pcall(function()
            char:TranslateBy(hum.MoveDirection * Config.TeleportMovementSpeed * 0.1)
        end)
    end
end

local InfiniteSlideState = {
    inSlide = false,
    slideDir = Vector3.new(0, 0, 1),
}

local InfiniteSlideKeys = {
    Enum.KeyCode.LeftControl,
    Enum.KeyCode.C,
}

function getInfiniteSlideInputHeld()
    if UserInputService.KeyboardEnabled then
        for _, keyCode in ipairs(InfiniteSlideKeys) do
            if UserInputService:IsKeyDown(keyCode) then
                return true
            end
        end
        return false
    end

    if UserInputService.GamepadEnabled then
        local ok, held = pcall(function()
            return UserInputService:IsGamepadButtonDown(
                Enum.UserInputType.Gamepad1,
                Enum.KeyCode.ButtonR2
            )
        end)
        if ok then
            return held
        end
    end

    -- En touch, el sonido es el indicador disponible del slide real.
    return nil
end

function getInfiniteSlideDirection(char, root)
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local move = humanoid.MoveDirection
        local moveHorizontal = Vector3.new(move.X, 0, move.Z)
        if moveHorizontal.Magnitude > 0.1 then
            return moveHorizontal.Unit
        end
    end

    local currentCamera = Workspace.CurrentCamera
    if currentCamera then
        local look = currentCamera.CFrame.LookVector
        local cameraHorizontal = Vector3.new(look.X, 0, look.Z)
        if cameraHorizontal.Magnitude > 0.1 then
            return cameraHorizontal.Unit
        end
    end

    local velocity = root.AssemblyLinearVelocity
    local horizontal = Vector3.new(velocity.X, 0, velocity.Z)
    if horizontal.Magnitude > 0.1 then
        return horizontal.Unit
    end

    return InfiniteSlideState.slideDir
end

function applyEnhancedJump()
    if not Config.EnableEnhancedJump then return end
    
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("Humanoid") then return end
    
    local hum = char.Humanoid
    if hum.Health <= 0 then return end
    
    if hum:GetState() ~= Enum.HumanoidStateType.Jumping then
        pcall(function()
            hum.JumpPower = Config.JumpHeight
            hum.UseJumpPower = true
        end)
    end
end

function applyInfiniteSlide()
    if not Config.EnableInfiniteSlide then
        InfiniteSlideState.inSlide = false
        return
    end

    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    local slideAttachment = root and root:FindFirstChild("Slide")
    local slideSound = slideAttachment and slideAttachment:FindFirstChild("SlideSound")
    if not char or not root or not slideSound then
        InfiniteSlideState.inSlide = false
        return
    end

    if slideSound.Playing then
        if not InfiniteSlideState.inSlide then
            InfiniteSlideState.inSlide = true
            local velocity = root.AssemblyLinearVelocity
            local horizontal = Vector3.new(velocity.X, 0, velocity.Z)
            if horizontal.Magnitude > 0.1 then
                InfiniteSlideState.slideDir = horizontal.Unit
            end
        end
    elseif not InfiniteSlideState.inSlide then
        return
    end

    local inputHeld = getInfiniteSlideInputHeld()
    if inputHeld == false or (inputHeld == nil and not slideSound.Playing) then
        InfiniteSlideState.inSlide = false
        return
    end

    local velocity = root.AssemblyLinearVelocity
    InfiniteSlideState.slideDir = getInfiniteSlideDirection(char, root)
    root.AssemblyLinearVelocity = Vector3.new(
        InfiniteSlideState.slideDir.X * Config.InfiniteSlideSpeed,
        velocity.Y,
        InfiniteSlideState.slideDir.Z * Config.InfiniteSlideSpeed
    )
end

function applyAutoJump()
    if not Config.AutoJump then return end

    local now = tick()
    if now < autoJumpNextTime then return end

    local char = LocalPlayer.Character
    if not char then return end

    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum or hum.Health <= 0 then return end

    if hum:GetState() == Enum.HumanoidStateType.Running then
        pcall(function()
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
            local jumpsPerSecond = math.clamp(tonumber(Config.AutoJumpSpeed) or 1.5, 0.5, 8)
            autoJumpNextTime = now + (1 / jumpsPerSecond)
        end)
    else
        -- Si todavía está en el aire, no consume el siguiente salto.
        autoJumpNextTime = math.min(autoJumpNextTime, now)
    end
end

--// ═════════════════════════════════════════════════════════════════════════════
--// AUTO CROUCH — getconnections directo al CrouchButton, igual técnica que
--// Auto Jump: repite el toggle (agachar/parar) a un intervalo configurable.
--//
--// IMPORTANTE: el WaitForChild con timeout de 5s por cada nivel de la
--// jerarquía puede tardar hasta varios segundos si el HUD no cargó
--// todavía. Llamarlo directo dentro del Heartbeat congelaría el juego
--// entero mientras dura (el mismo problema que ya se corrigió en otras
--// partes de la librería). Por eso se resuelve UNA sola vez, en su
--// propia coroutine (task.spawn), y el Heartbeat solo hace un chequeo
--// barato de si ya está lista.
--// ═════════════════════════════════════════════════════════════════════════════
local crouchToggleFn  = nil
local crouchResolving = false

local function resolveCrouchToggleAsync()
    if crouchToggleFn or crouchResolving then return end
    crouchResolving = true

    task.spawn(function()
        local ok, btn = pcall(function()
            return LocalPlayer.PlayerGui
                :WaitForChild("Game",        5)
                :WaitForChild("HUD",         5)
                :WaitForChild("Mobile",      5)
                :WaitForChild("Right",       5)
                :WaitForChild("Mobile",      5)
                :WaitForChild("CrouchButton",5)
        end)

        if ok and btn then
            local conns = getconnections(btn.Activated)
            if not conns or #conns == 0 then
                conns = getconnections(btn.MouseButton1Click)
            end

            if conns and #conns > 0 and conns[1].Function then
                crouchToggleFn = conns[1].Function
                print("✅ Auto Crouch: CrouchButton resuelto correctamente")
            else
                warn("[AutoCrouch] sin conexiones accesibles en CrouchButton")
            end
        else
            warn("[AutoCrouch] CrouchButton no encontrado")
        end

        crouchResolving = false
    end)
end

function applyAutoCrouch()
    if not Config.AutoCrouch then return end

    if not crouchToggleFn then
        resolveCrouchToggleAsync() -- no bloquea; corre en su propia coroutine
        return
    end

    local now = tick()
    if now < autoCrouchNextTime then return end

    local ok = pcall(crouchToggleFn)
    if ok then
        local togglesPerSecond = math.clamp(tonumber(Config.AutoCrouchSpeed) or 1.5, 0.5, 24)
        autoCrouchNextTime = now + (1 / togglesPerSecond)
    end
end

--// ═════════════════════════════════════════════════════════════════════════════
--// LAYER JUMPS (Multi-Jump via JumpCap interno de Evade)
--// ═════════════════════════════════════════════════════════════════════════════
local layerJumpsInstance = nil
local originalJumpCap = nil
local layerJumpsRespawned = false  -- true tras CharacterAdded, hasta confirmar instancia nueva

function FindMoveStatsInstance()
    -- Si acabamos de respawnear, nunca retornar la instancia cacheada:
    -- podría ser la tabla vieja de la ronda anterior con JumpCap=nuestro valor
    if layerJumpsInstance and not layerJumpsRespawned then return layerJumpsInstance end

    -- Re-escanear GC completo buscando una tabla con MoveStats.JumpCap
    -- que NO sea nuestro valor seteado (señal de que es la tabla nueva de Evade)
    local ok, gcTable = pcall(getgc, true)
    if not ok or not gcTable then return layerJumpsInstance end

    local candidate = nil
    for i = 1, #gcTable do
        local v = gcTable[i]
        if type(v) == "table" then
            local ok2, ms = pcall(rawget, v, "MoveStats")
            if ok2 and type(ms) == "table" then
                local ok3, jc = pcall(rawget, ms, "JumpCap")
                if ok3 and type(jc) == "number" then
                    -- Si venimos de respawn, solo aceptar tabla cuyo JumpCap
                    -- es distinto al que nosotros escribimos (es la nueva de Evade)
                    if layerJumpsRespawned then
                        if jc ~= Config.LayerJumpsCount then
                            candidate = v
                            break
                        end
                        -- Si encontramos JumpCap == nuestro valor tras respawn,
                        -- puede ser tabla vieja; seguir buscando
                    else
                        candidate = v
                        break
                    end
                end
            end
        end
    end

    if candidate then
        layerJumpsInstance = candidate
        layerJumpsRespawned = false  -- instancia nueva confirmada
    end
    return layerJumpsInstance
end

LocalPlayer.CharacterAdded:Connect(function()
    layerJumpsInstance = nil
    originalJumpCap = nil
    layerJumpsRespawned = true  -- forzar re-escaneo sin caché en la próxima búsqueda
    lastSpeedSelf, lastSpeedState, lastSpeedValue = nil, nil, nil
    getgenv().YYSpeedRestoreRequested = false
    if Config.EnableSpeedMultiplier then
        getgenv().YYSpeedApplyRequested = true
        task.delay(0.75, function()
            if getgenv().YYApplySpeedMultiplier then getgenv().YYApplySpeedMultiplier() end
        end)
    end
end)

function applyLayerJumps()
    if not Config.EnableLayerJumps then
        if layerJumpsInstance and originalJumpCap then
            pcall(function()
                local ms = rawget(layerJumpsInstance, "MoveStats")
                if type(ms) == "table" then
                    ms.JumpCap = originalJumpCap
                end
            end)
            originalJumpCap = nil
        end
        return
    end

    local inst = FindMoveStatsInstance()
    if not inst then return end

    pcall(function()
        local ms = rawget(inst, "MoveStats")
        if type(ms) == "table" and type(ms.JumpCap) == "number" then
            if not originalJumpCap then
                originalJumpCap = (ms.JumpCap ~= Config.LayerJumpsCount) and ms.JumpCap or 1
            end
            ms.JumpCap = Config.LayerJumpsCount
        end
    end)
end

--// IMPULSE JUMP — misma lógica que applyFrontalJump, slider controla la velocidad directamente
local impulseCurrentSpeed = 42
local impulseAirAccumulator = 0
local impulseLastTick = tick()
local impulseWasAir = false
local activeImpulseBV = nil

function applyImpulseJump()
    if not Config.EnableImpulseJump then
        impulseWasAir = false
        impulseAirAccumulator = 0
        impulseCurrentSpeed = Config.ImpulseJumpSpeed
        if activeImpulseBV then activeImpulseBV:Destroy() activeImpulseBV = nil end
        return
    end

    local deltaTime = tick() - impulseLastTick
    impulseLastTick = tick()

    local baseSpeed = Config.ImpulseJumpSpeed

    local char = LocalPlayer.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    local humanoid = char:FindFirstChild("Humanoid")
    if not root or not humanoid then return end
    if humanoid.Health <= 0 then return end

    local isAir = humanoid.FloorMaterial == Enum.Material.Air
    local humanoidState = humanoid:GetState()
    local isOnGround = (humanoidState == Enum.HumanoidStateType.Landed
                    or  humanoidState == Enum.HumanoidStateType.Running) and not isAir

    if impulseWasAir and isOnGround then
        impulseCurrentSpeed = baseSpeed
        impulseAirAccumulator = 0
    end
    impulseWasAir = isAir

    local lookDir = camera.CFrame.LookVector
    lookDir = Vector3.new(lookDir.X, 0, lookDir.Z)
    if lookDir.Magnitude ~= 0 then lookDir = lookDir.Unit end

    if isAir then
        if activeImpulseBV then activeImpulseBV:Destroy() end
        local bv = Instance.new("BodyVelocity")
        bv.Velocity = lookDir * impulseCurrentSpeed
        bv.MaxForce = Vector3.new(4e5, 0, 4e5)
        bv.P = 1250
        bv.Parent = root
        Debris:AddItem(bv, 0.1)
        activeImpulseBV = bv
    else
        impulseAirAccumulator = impulseAirAccumulator + deltaTime
        while impulseAirAccumulator >= 0.04 do
            impulseAirAccumulator = impulseAirAccumulator - 0.04
            impulseCurrentSpeed = math.min(baseSpeed + 10, impulseCurrentSpeed + 0.3)
        end

        if activeImpulseBV then activeImpulseBV:Destroy() end
        local bv = Instance.new("BodyVelocity")
        bv.Velocity = lookDir * impulseCurrentSpeed
        bv.MaxForce = Vector3.new(4e5, 0, 4e5)
        bv.P = 1250
        bv.Parent = root
        Debris:AddItem(bv, 0.1)
        activeImpulseBV = bv

        impulseCurrentSpeed = math.max(baseSpeed, impulseCurrentSpeed - 2.5 * deltaTime)
    end
end

function applyGravityModification()
    if not Config.EnableGravityMod then return end
    
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("Humanoid") or not char:FindFirstChild("HumanoidRootPart") then return end
    
    local hum = char.Humanoid
    local rootPart = char.HumanoidRootPart
    
    if hum.Health <= 0 then return end
    
    pcall(function()
        local velocity = rootPart.AssemblyLinearVelocity
        
        if velocity.Y < 0 then
            rootPart.AssemblyLinearVelocity = Vector3.new(
                velocity.X,
                velocity.Y * Config.GravityScale,
                velocity.Z
            )
        end
    end)
end

--// FRONTAL JUMP FUNCTION
function ActivateFrontalJump()
    if inEmote then return end
    local char = LocalPlayer.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart")
        local humanoid = char:FindFirstChild("Humanoid")
        
        if root and humanoid then
            if Config.EnableJumpAcceleration then
                humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                lastJumpTime = tick()
                return
            end
            local humanoidState = humanoid:GetState()
            local isOnGround = (humanoidState == Enum.HumanoidStateType.Landed or humanoidState == Enum.HumanoidStateType.Running)
            
            if isOnGround then
                --// Detect if there's a structure ahead to apply vertical impulse
                local lookDir = camera.CFrame.LookVector
                lookDir = Vector3.new(lookDir.X, 0, lookDir.Z)
                if lookDir.Magnitude ~= 0 then
                    lookDir = lookDir.Unit
                end
                
                local rayOriginFront = root.Position
                local rayDirectionFront = lookDir * 10
                local raycastParamsFront = RaycastParams.new()
                raycastParamsFront.FilterType = Enum.RaycastFilterType.Blacklist
                raycastParamsFront.FilterDescendantsInstances = {char}
                
                local raycastResultFront = Workspace:Raycast(rayOriginFront, rayDirectionFront, raycastParamsFront)
                
                if raycastResultFront and not Config.EnableJumpAcceleration then
                    --// Solo Frontal Jump puede usar el boost vertical de rampa.
                    rampLocked = true
                    humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    
                    --// Apply vertical impulse after a small delay
                    task.delay(0.1, function()
                        if char and root then
                            local jumpBoost = getgenv().FrontalJumpSpeed * getgenv().RampMultiplier * 0.8
                            pcall(function()
                                root.AssemblyLinearVelocity = Vector3.new(
                                    root.AssemblyLinearVelocity.X,
                                    jumpBoost,
                                    root.AssemblyLinearVelocity.Z
                                )
                            end)
                        end
                    end)
                else
                    --// No structure ahead - normal auto jump only if not coming from a ramp
                    if not rampLocked then
                        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    end
                end
                
                lastJumpTime = tick()
            end
        end
    end
end

function applyFrontalJump()
    if not Config.EnableFrontalJump and not Config.EnableJumpAcceleration then return end
    
    local deltaTime = tick() - lastTick
    lastTick = tick()
    local baseSpeed = Config.EnableJumpAcceleration and math.clamp(tonumber(Config.JumpAccelerationSpeed) or 42, 30, 110) or getgenv().FrontalJumpSpeed
    
    local char = LocalPlayer.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart")
        local humanoid = char:FindFirstChild("Humanoid")
        
        if root and humanoid then
            local isAir = humanoid.FloorMaterial == Enum.Material.Air
            local humanoidState = humanoid:GetState()
            local isOnGround = (humanoidState == Enum.HumanoidStateType.Landed or humanoidState == Enum.HumanoidStateType.Running) and not isAir
            
            local hitObject = false
            if wasAir and isOnGround then
                hitObject = true
                currentSpeed = baseSpeed
                airAccumulator = 0
                rampLocked = false
            end
            wasAir = isAir
            
            if isAir then
                if activeBV then activeBV:Destroy() end
                
                local lookDir = camera.CFrame.LookVector
                lookDir = Vector3.new(lookDir.X, 0, lookDir.Z)
                
                if lookDir.Magnitude ~= 0 then
                    lookDir = lookDir.Unit
                end
                
                --// In air: only maintain horizontal momentum, no Y force, no wall detection
                local bv = Instance.new("BodyVelocity")
                bv.Velocity = lookDir * currentSpeed
                bv.MaxForce = Vector3.new(4e5, 0, 4e5)
                bv.P = 1250
                bv.Parent = root
                
                Debris:AddItem(bv, 0.1)
                activeBV = bv
            else
                --// On ground: Apply impulse AND accelerate to gain power
                airAccumulator = airAccumulator + deltaTime
                while airAccumulator >= 0.04 do
                    airAccumulator = airAccumulator - 0.04
                    --// Accelerate slowly but CAPPED at base speed
                    currentSpeed = math.min(baseSpeed + 10, currentSpeed + 0.3)
                end
                
                if activeBV then activeBV:Destroy() end
                
                local lookDir = camera.CFrame.LookVector
                lookDir = Vector3.new(lookDir.X, 0, lookDir.Z)
                
                if lookDir.Magnitude ~= 0 then
                    lookDir = lookDir.Unit
                end
                
                local impulsePower = currentSpeed
                if not Config.EnableJumpAcceleration then
                    --// Frontal Jump conserva su raycast y multiplicador de rampas.
                    local rayOriginFront = root.Position
                    local rayDirectionFront = lookDir * 8
                    local raycastParamsFront = RaycastParams.new()
                    raycastParamsFront.FilterType = Enum.RaycastFilterType.Blacklist
                    raycastParamsFront.FilterDescendantsInstances = {char}
                    local raycastResultFront = Workspace:Raycast(rayOriginFront, rayDirectionFront, raycastParamsFront)
                    if raycastResultFront then
                        impulsePower = currentSpeed * getgenv().RampMultiplier
                    end
                end
                
                local bv = Instance.new("BodyVelocity")
                bv.Velocity = lookDir * impulsePower
                bv.MaxForce = Vector3.new(4e5, 0, 4e5)
                bv.P = 1250
                bv.Parent = root
                
                Debris:AddItem(bv, 0.1)
                activeBV = bv
                
                currentSpeed = math.max(baseSpeed, currentSpeed - 2.5 * deltaTime)
                
                if tick() - lastJumpTime >= jumpInterval then
                    ActivateFrontalJump()
                end
            end
        end
    end
end

--// ═════════════════════════════════════════════════════════════════════════════
--// RAMP MULTIPLIER FUNCTION (standalone — sin la lógica de Frontal Jump)
--// ═════════════════════════════════════════════════════════════════════════════
--// Mismo efecto ("detectar rampa adelante -> multiplicar velocidad") que
--// usa Frontal Jump, pero acá NO hay auto-salto periódico, NO hay
--// BodyVelocity constante empujándote, NO hay acumulador de velocidad.
--// Es puramente reactivo: solo actúa el instante en que VOS te movés (o
--// saltás) hacia una rampa detectada; el resto del tiempo tu personaje
--// se mueve 100% normal, como si esta opción no existiera.
local rampMultPrevState = nil

function applyRampMultiplier()
    if not Config.EnableRampMultiplier then
        rampMultPrevState = nil
        return
    end

    local char = LocalPlayer.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    local humanoid = char:FindFirstChild("Humanoid")
    if not root or not humanoid then return end

    local state = humanoid:GetState()
    local justJumped = (state == Enum.HumanoidStateType.Jumping and rampMultPrevState ~= Enum.HumanoidStateType.Jumping)
    rampMultPrevState = state

    local moveDir = humanoid.MoveDirection
    if moveDir.Magnitude < 0.05 then return end -- no te estás moviendo, no hace nada

    local rayParams = RaycastParams.new()
    rayParams.FilterType = Enum.RaycastFilterType.Blacklist
    rayParams.FilterDescendantsInstances = {char}
    local hit = Workspace:Raycast(root.Position, moveDir.Unit * 8, rayParams)
    if not hit then return end -- no hay rampa/estructura adelante, no hace nada

    if justJumped then
        --// Acabás de saltar hacia una rampa detectada: empujón vertical.
        local boost = humanoid.WalkSpeed * getgenv().RampMultiplierValue * 0.8
        pcall(function()
            root.AssemblyLinearVelocity = Vector3.new(
                root.AssemblyLinearVelocity.X,
                boost,
                root.AssemblyLinearVelocity.Z
            )
        end)
    elseif humanoid.FloorMaterial ~= Enum.Material.Air then
        --// En el piso, moviéndote hacia una rampa: multiplica tu velocidad horizontal actual.
        local currentVel = root.AssemblyLinearVelocity
        local boosted = moveDir.Unit * humanoid.WalkSpeed * getgenv().RampMultiplierValue
        pcall(function()
            root.AssemblyLinearVelocity = Vector3.new(boosted.X, currentVel.Y, boosted.Z)
        end)
    end
end

--// BACK JUMP FUNCTION
function ActivateBackJump()
    if inEmote then return end
    local char = LocalPlayer.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart")
        local humanoid = char:FindFirstChild("Humanoid")

        if root and humanoid then
            local humanoidState = humanoid:GetState()
            local isOnGround = (humanoidState == Enum.HumanoidStateType.Landed or humanoidState == Enum.HumanoidStateType.Running)

            if isOnGround then
                local lookDir = camera.CFrame.LookVector
                local backDir = Vector3.new(-lookDir.X, 0, -lookDir.Z)
                if backDir.Magnitude ~= 0 then
                    backDir = backDir.Unit
                end

                local rayOriginBack = root.Position
                local rayDirectionBack = backDir * 10
                local raycastParamsBack = RaycastParams.new()
                raycastParamsBack.FilterType = Enum.RaycastFilterType.Blacklist
                raycastParamsBack.FilterDescendantsInstances = {char}

                local raycastResultBack = Workspace:Raycast(rayOriginBack, rayDirectionBack, raycastParamsBack)

                if raycastResultBack then
                    --// Structure behind - jump with boost
                    backRampLocked = true
                    humanoid:ChangeState(Enum.HumanoidStateType.Jumping)

                    task.delay(0.1, function()
                        if char and root then
                            local jumpBoost = getgenv().BackJumpSpeed * getgenv().BackRampMultiplier * 0.8
                            pcall(function()
                                root.AssemblyLinearVelocity = Vector3.new(
                                    root.AssemblyLinearVelocity.X,
                                    jumpBoost,
                                    root.AssemblyLinearVelocity.Z
                                )
                            end)
                        end
                    end)
                else
                    --// No structure behind - normal auto jump only if not coming from a ramp
                    if not backRampLocked then
                        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    end
                end

                backLastJumpTime = tick()
            end
        end
    end
end

function applyBackJump()
    if not Config.EnableBackJump then return end

    local deltaTime = tick() - backLastTick
    backLastTick = tick()

    local char = LocalPlayer.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart")
        local humanoid = char:FindFirstChild("Humanoid")

        if root and humanoid then
            local isAir = humanoid.FloorMaterial == Enum.Material.Air
            local humanoidState = humanoid:GetState()
            local isOnGround = (humanoidState == Enum.HumanoidStateType.Landed or humanoidState == Enum.HumanoidStateType.Running) and not isAir

            if backWasAir and isOnGround then
                backCurrentSpeed = getgenv().BackJumpSpeed
                backAirAccumulator = 0
                backRampLocked = false
            end
            backWasAir = isAir

            local lookDir = camera.CFrame.LookVector
            local backDir = Vector3.new(-lookDir.X, 0, -lookDir.Z)
            if backDir.Magnitude ~= 0 then
                backDir = backDir.Unit
            end

            if isAir then
                if backActiveBV then backActiveBV:Destroy() end

                --// In air: only maintain horizontal momentum backwards, no Y force
                local bv = Instance.new("BodyVelocity")
                bv.Velocity = backDir * backCurrentSpeed
                bv.MaxForce = Vector3.new(4e5, 0, 4e5)
                bv.P = 1250
                bv.Parent = root

                Debris:AddItem(bv, 0.1)
                backActiveBV = bv
            else
                --// On ground: apply backwards impulse
                backAirAccumulator = backAirAccumulator + deltaTime
                while backAirAccumulator >= 0.04 do
                    backAirAccumulator = backAirAccumulator - 0.04
                    backCurrentSpeed = math.min(getgenv().BackJumpSpeed + 10, backCurrentSpeed + 0.3)
                end

                if backActiveBV then backActiveBV:Destroy() end

                --// Raycast behind to detect structures
                local rayOriginBack = root.Position
                local rayDirectionBack = backDir * 8
                local raycastParamsBack = RaycastParams.new()
                raycastParamsBack.FilterType = Enum.RaycastFilterType.Blacklist
                raycastParamsBack.FilterDescendantsInstances = {char}

                local raycastResultBack = Workspace:Raycast(rayOriginBack, rayDirectionBack, raycastParamsBack)

                local impulsePower = backCurrentSpeed
                if raycastResultBack then
                    impulsePower = backCurrentSpeed * getgenv().BackRampMultiplier
                end

                local bv = Instance.new("BodyVelocity")
                bv.Velocity = backDir * impulsePower
                bv.MaxForce = Vector3.new(4e5, 0, 4e5)
                bv.P = 1250
                bv.Parent = root

                Debris:AddItem(bv, 0.1)
                backActiveBV = bv

                backCurrentSpeed = math.max(getgenv().BackJumpSpeed, backCurrentSpeed - 2.5 * deltaTime)

                if tick() - backLastJumpTime >= backJumpInterval then
                    ActivateBackJump()
                end
            end
        end
    end
end

print("✅ MOVEMENT: Functions loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════

--// FULLBRIGHT FUNCTION
--// ═════════════════════════════════════════════════════════════════════════════

--// FullBright now uses setFullBrightEnabled(state)

print("✅ FULLBRIGHT: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// AUTO TICKET
--// ═════════════════════════════════════════════════════════════════════════════
--// Busca la tabla interna del juego que guarda los tickets vivos en el mapa
--// (usa el mismo método que scripts conocidos: escanear la memoria (getgc)
--// buscando la tabla "Contexts" que tiene .CollectableIDs, sin tocar remotes
--// ni módulos del juego directamente)

local AutoTicketContexts = nil

function FindAutoTicketContexts()
    if AutoTicketContexts then return AutoTicketContexts end
    local ok, gcTable = pcall(getgc, true)
    if not ok or not gcTable then return nil end

    for i = 1, #gcTable do
        local v = gcTable[i]
        if type(v) == "table" then
            local ok2, first = pcall(function() return v[1] end)
            if ok2 and type(first) == "table" then
                local ok3, hasIds = pcall(rawget, first, "CollectableIDs")
                if ok3 and hasIds then
                    AutoTicketContexts = v
                    break
                end
            end
        end
    end

    return AutoTicketContexts
end

function GetServerPing()
    local ok, ping = pcall(function()
        return game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue() / 1000
    end)
    return (ok and ping) or 0.1
end

--// Teletransporte suave (lerp), igual de rápido pero sin salto brusco
function SmoothTp(part, targetCFrame, duration)
    if not part or not part.Parent then return end
    duration = duration or 0.3
    local startCFrame = part.CFrame
    local startTime = tick()

    while tick() - startTime < duration do
        if not Config.EnableAutoTicket then return end
        if not part or not part.Parent then return end
        local alpha = math.clamp((tick() - startTime) / duration, 0, 1)
        pcall(function() part.CFrame = startCFrame:Lerp(targetCFrame, alpha) end)
        RunService.Heartbeat:Wait()
    end

    if part and part.Parent then
        pcall(function() part.CFrame = targetCFrame end)
    end
end

function applyAutoTicket()
    local contexts = FindAutoTicketContexts()
    if not contexts then return end

    local duration = 0.5 + math.clamp(GetServerPing(), 0, 0.5)

    for i = 1, #contexts do
        if not Config.EnableAutoTicket then return end
        local ctx = contexts[i]

        if type(ctx) == "table" and ctx.CollectableIDs then
            for _, collectable in pairs(ctx.CollectableIDs) do
                if not Config.EnableAutoTicket then return end

                local ok, position = pcall(function()
                    return collectable.CollectableData.Position
                end)

                if ok and position then
                    local char = LocalPlayer.Character
                    local root = char and char:FindFirstChild("HumanoidRootPart")
                    if root then
                        SmoothTp(root, CFrame.new(position), duration)
                        task.wait(2.5)
                    end
                end
            end
        end
    end
end

local autoTicketLoopStarted = false
function StartAutoTicketLoop()
    if autoTicketLoopStarted then return end
    autoTicketLoopStarted = true

    task.spawn(function()
        while true do
            if Config.EnableAutoTicket then
                pcall(applyAutoTicket)
            end
            task.wait(0.5)
        end
    end)
end

StartAutoTicketLoop()

print("✅ AUTO TICKET: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// VOID ESCAPE
--// ═════════════════════════════════════════════════════════════════════════════
--// Te manda muy alto en el cielo (0, 9e9, 0), lejos de cualquier peligro del mapa

function applyVoidEscape()
    if not Config.EnableVoidEscape then return end

    local char = LocalPlayer.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    pcall(function()
        root.CFrame = CFrame.new(0, 9e9, 0)
    end)
end

print("✅ VOID ESCAPE: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// REVIVE AURA
--// ═════════════════════════════════════════════════════════════════════════════
--// Revive automático a jugadores derribados cerca tuyo (30 studs).
--// Usa el mismo módulo interno del juego (CharService) que ya usa Auto Ticket
--// para encontrar Contexts, buscado con el mismo método (getgc)

local CharServiceModule = nil
local InteractRemote = nil

function FindCharService()
    if CharServiceModule then return CharServiceModule end
    local ok, gcTable = pcall(getgc, true)
    if not ok or not gcTable then return nil end

    for i = 1, #gcTable do
        local v = gcTable[i]
        if type(v) == "table" then
            local ok2, hasGetCharacters = pcall(rawget, v, "GetCharacters")
            if ok2 and hasGetCharacters then
                CharServiceModule = v
                break
            end
        end
    end

    return CharServiceModule
end

function GetInteractRemote()
    if InteractRemote then return InteractRemote end
    local ok, remote = pcall(function()
        return ReplicatedStorage:WaitForChild("Events", 10):WaitForChild("Interact", 5)
    end)
    if ok and remote then
        InteractRemote = remote
    end
    return InteractRemote
end

--// ═════════════════════════════════════════════════════════════════════════════
--// REVIVE AURA
--// ═════════════════════════════════════════════════════════════════════════════

function applyReviveAura()
    if not Config.EnableReviveAura then return end

    local charService = FindCharService()
    local interact = GetInteractRemote()
    if not charService or not interact then return end

    local char = LocalPlayer.Character
    local myRoot = char and char:FindFirstChild("HumanoidRootPart")
    if not myRoot then return end

    local ok, chars = pcall(function() return charService:GetCharacters() end)
    if not ok or not chars then return end

    for i = 1, #chars do
        if not Config.EnableReviveAura then return end
        local v = chars[i]

        if type(v) == "table" and v.Done and not v.Local then
            local okState, isDowned = pcall(function() return v.State.State == "Downed" end)

            if okState and isDowned and v.PrimaryPart then
                local okDist, distance = pcall(function()
                    return (v.PrimaryPart.Position - myRoot.Position).Magnitude
                end)

                if okDist and distance < 30 then
                    pcall(function()
                        interact:FireServer("Revive", v.Tag, true)
                    end)

                    local reviveLength = 1.5
                    pcall(function()
                        reviveLength = v.DataRegistry:Get("ReviveLength") or 1.5
                    end)

                    task.wait(reviveLength)

                    local stillOk, stillDowned = pcall(function()
                        return v.State.State == "Downed" and v.PrimaryPart ~= nil
                    end)

                    if Config.EnableReviveAura and stillOk and stillDowned then
                        local okDist2, distance2 = pcall(function()
                            return (v.PrimaryPart.Position - myRoot.Position).Magnitude
                        end)
                        if okDist2 and distance2 < 30 then
                            pcall(function()
                                interact:FireServer("Revive", v.Tag)
                            end)
                        end
                    end
                end
            end
        end
    end
end

local reviveAuraLoopStarted = false
function StartReviveAuraLoop()
    if reviveAuraLoopStarted then return end
    reviveAuraLoopStarted = true

    task.spawn(function()
        while true do
            if Config.EnableReviveAura then
                pcall(applyReviveAura)
            end
            task.wait(0.5)
        end
    end)
end

StartReviveAuraLoop()

print("OK REVIVE AURA: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// FARM OF MONEY (Auto-Revive propio) — aporte de NhatMinh
--// Misma lógica que Goodmode.lua: cada ~2s, si el personaje local tiene el
--// atributo "Tag", dispara Revive sobre sí mismo (sin chequear distancia ni
--// estado, igual que el original). Reutiliza GetInteractRemote() ya existente
--// en vez de duplicar la búsqueda del remote.
--// ═════════════════════════════════════════════════════════════════════════════

local goodModeLoopStarted = false
function StartGoodModeLoop()
    if goodModeLoopStarted then return end
    goodModeLoopStarted = true

    task.spawn(function()
        while true do
            task.wait(2)
            if Config.EnableGoodMode then
                local interact = GetInteractRemote()
                local char = LocalPlayer.Character
                local tag = char and char:GetAttribute("Tag")
                if interact and tag then
                    pcall(function() interact:FireServer("Revive", tag) end)
                    pcall(function() interact:FireServer("Revive", tag, true) end)
                end
            end
        end
    end)
end

StartGoodModeLoop()

print("OK FARM OF MONEY: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// AUTO CARRY
--// ═════════════════════════════════════════════════════════════════════════════

local autoCarryLoopStarted = false
local function applyAutoCarry()
    if not Config.EnableAutoCarry then return end

    local charService = FindCharService()
    local interact = GetInteractRemote()
    if not charService or not interact then return end

    local char = LocalPlayer.Character
    local myRoot = char and char:FindFirstChild("HumanoidRootPart")
    if not myRoot then return end

    local ok, chars = pcall(function() return charService:GetCharacters() end)
    if not ok or not chars then return end

    for i = 1, #chars do
        if not Config.EnableAutoCarry then return end
        local v = chars[i]

        if type(v) == "table" and v.Done and not v.Local then
            local okState, isDowned = pcall(function()
                return v.State and v.State.State == "Downed"
            end)

            if okState and isDowned and v.PrimaryPart then
                local okDist, distance = pcall(function()
                    return (v.PrimaryPart.Position - myRoot.Position).Magnitude
                end)

                if okDist and distance < 30 then
                    local tag = v.Tag
                    if tag then
                        pcall(function()
                            interact:FireServer("Carry", tag)
                        end)
                        task.wait(0.3)
                    end
                end
            end
        end
    end
end

local function StartAutoCarryLoop()
    if autoCarryLoopStarted then return end
    autoCarryLoopStarted = true

    task.spawn(function()
        while true do
            if Config.EnableAutoCarry then
                pcall(applyAutoCarry)
            end
            task.wait(0.2)
        end
    end)
end

StartAutoCarryLoop()

print("OK AUTO CARRY: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// FIGURE-8 LOOP (Lemniscata — Movimiento en bucle ∞)
--// Traza una lemniscata de Bernoulli (símbolo ∞) alrededor del punto exacto
--// donde el toggle fue activado. El personaje queda anclado en Y y sigue
--// siempre la tangente de la curva, mirando en la dirección del movimiento.
--// Velocidad y radio son configurables en tiempo real vía sliders en TabMove.
--// ═════════════════════════════════════════════════════════════════════════════

local figure8T            = 0      -- Parámetro de la curva (radianes, crece indefinidamente)
local figure8CenterCF     = nil    -- CFrame congelado del punto de activación
local figure8Conn         = nil    -- Conexión Heartbeat activa
local figure8PrevAnchor   = false  -- Estado Anchored previo del HRP
local figure8PrevWalkSpd  = 16     -- WalkSpeed previo del Humanoid

function StopFigure8Loop()
    -- Desconectar el loop si existe
    if figure8Conn then
        figure8Conn:Disconnect()
        figure8Conn = nil
    end
    -- Restaurar estado previo del personaje
    local char = LocalPlayer.Character
    if char then
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = figure8PrevWalkSpd end
    end
    figure8CenterCF = nil
    print("❌ Figure-8 Loop STOPPED")
end

function StartFigure8Loop()
    StopFigure8Loop()  -- Garantiza que no haya loop previo colgado

    local char = LocalPlayer.Character
    if not char then
        warn("[Figure-8] No character found")
        return
    end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hrp or not hum then
        warn("[Figure-8] HumanoidRootPart or Humanoid not found")
        return
    end

    -- Guardar estado previo antes de modificar nada
    figure8PrevAnchor  = hrp.Anchored
    figure8PrevWalkSpd = hum.WalkSpeed

    -- Inmovilizar sin anclar — el CFrame se replica a todos los jugadores
    hum.WalkSpeed  = 0
    figure8T       = 0

    -- Capturar el CFrame de origen:
    -- posición actual del personaje + orientación de su cara (solo eje Y)
    local lookVec = hrp.CFrame.LookVector
    figure8CenterCF = CFrame.new(hrp.Position)
        * CFrame.Angles(0, math.atan2(lookVec.X, lookVec.Z), 0)

    print("✅ Figure-8 Loop STARTED at " .. tostring(hrp.Position))

    figure8Conn = RunService.Heartbeat:Connect(function(dt)
        if not Config.EnableFigure8 then return end

        local char2 = LocalPlayer.Character
        if not char2 then return end
        local hrp2 = char2:FindFirstChild("HumanoidRootPart")
        if not hrp2 then return end

        -- ──────────────────────────────────────────────────────────────────────
        -- Lemniscata de Bernoulli parametrizada (plano XZ local):
        --   x(t) = R · cos(t)            → eje lateral   (RightVector)
        --   z(t) = (R/2) · sin(2·t)      → eje frontal   (LookVector)
        -- Derivadas → dirección del movimiento (tangente):
        --   dx/dt = −R · sin(t)
        --   dz/dt =  R · cos(2·t)
        -- ──────────────────────────────────────────────────────────────────────
        figure8T = figure8T + Config.Figure8Speed * dt

        local R  = Config.Figure8Radius
        local t  = figure8T

        local localX =  R       * math.cos(t)
        local localZ = (R * 0.5)* math.sin(2 * t)
        local tanX   = -R       * math.sin(t)
        local tanZ   =  R       * math.cos(2 * t)

        -- Transformar al espacio mundial usando los vectores del punto de activación
        local right   = figure8CenterCF.RightVector
        local forward = figure8CenterCF.LookVector
        local origin  = figure8CenterCF.Position

        local worldPos = origin + right * localX + forward * localZ
        -- Mantener la altura Y exacta del punto de activación (sin caída)
        worldPos = Vector3.new(worldPos.X, origin.Y, worldPos.Z)

        -- Dirección hacia la que mira el personaje = tangente de la curva en espacio mundial
        local worldTan = right * tanX + forward * tanZ
        local tanMag   = worldTan.Magnitude

        local newCF
        if tanMag > 0.01 then
            newCF = CFrame.new(worldPos, worldPos + worldTan)
        else
            -- Singularidad (velocidad tangente cero): mantener dirección del origen
            newCF = CFrame.new(worldPos, worldPos + forward)
        end

        hrp2.CFrame = newCF
        -- Zeroing de velocidad: evita que la física acumule momentum entre frames
        hrp2.AssemblyLinearVelocity  = Vector3.zero
        hrp2.AssemblyAngularVelocity = Vector3.zero
    end)
end

-- Si el personaje reaparece mientras el loop está activo, desactivarlo para
-- evitar que intente mover el nuevo personaje desde coordenadas del mapa anterior.
LocalPlayer.CharacterAdded:Connect(function()
    if Config.EnableFigure8 then
        Config.EnableFigure8 = false
        StopFigure8Loop()
        print("[Figure-8] Character respawned — loop deactivated automatically")
    end
end)

print("OK FIGURE-8 LOOP: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// LISSAJOUS 3:2 LOOP
--//   x(t) = R · sin(3t)
--//   z(t) = R · sin(2t + π/4)
--//   dx/dt = 3R · cos(3t)   /   dz/dt = 2R · cos(2t + π/4)
--// ═════════════════════════════════════════════════════════════════════════════

local lissajousT            = 0
local lissajousCenterCF     = nil
local lissajousConn         = nil
local lissajousPrevAnchor   = false
local lissajousPrevWalkSpd  = 16

function StopLissajousLoop()
    if lissajousConn then
        lissajousConn:Disconnect()
        lissajousConn = nil
    end
    local char = LocalPlayer.Character
    if char then
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = lissajousPrevWalkSpd end
    end
    lissajousCenterCF = nil
    print("❌ Lissajous Loop STOPPED")
end

function StartLissajousLoop()
    StopLissajousLoop()

    local char = LocalPlayer.Character
    if not char then
        warn("[Lissajous] No character found")
        return
    end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hrp or not hum then
        warn("[Lissajous] HumanoidRootPart or Humanoid not found")
        return
    end

    lissajousPrevAnchor  = hrp.Anchored
    lissajousPrevWalkSpd = hum.WalkSpeed

    -- Inmovilizar sin anclar — el CFrame se replica a todos los jugadores
    hum.WalkSpeed = 0
    lissajousT    = 0

    local lookVec = hrp.CFrame.LookVector
    lissajousCenterCF = CFrame.new(hrp.Position)
        * CFrame.Angles(0, math.atan2(lookVec.X, lookVec.Z), 0)

    print("✅ Lissajous Loop STARTED at " .. tostring(hrp.Position))

    lissajousConn = RunService.Heartbeat:Connect(function(dt)
        if not Config.EnableLissajous then return end

        local char2 = LocalPlayer.Character
        if not char2 then return end
        local hrp2 = char2:FindFirstChild("HumanoidRootPart")
        if not hrp2 then return end

        lissajousT = lissajousT + Config.LissajousSpeed * dt

        local R  = Config.LissajousRadius
        local t  = lissajousT
        local ph = math.pi / 4   -- desfase π/4 para el eje Z (lazo cerrado)

        local localX = R * math.sin(3 * t)
        local localZ = R * math.sin(2 * t + ph)
        local tanX   = 3 * R * math.cos(3 * t)
        local tanZ   = 2 * R * math.cos(2 * t + ph)

        local right   = lissajousCenterCF.RightVector
        local forward = lissajousCenterCF.LookVector
        local origin  = lissajousCenterCF.Position

        local worldPos = origin + right * localX + forward * localZ
        worldPos = Vector3.new(worldPos.X, origin.Y, worldPos.Z)

        local worldTan = right * tanX + forward * tanZ
        local tanMag   = worldTan.Magnitude

        local newCF
        if tanMag > 0.01 then
            newCF = CFrame.new(worldPos, worldPos + worldTan)
        else
            newCF = CFrame.new(worldPos, worldPos + forward)
        end

        hrp2.CFrame = newCF
        hrp2.AssemblyLinearVelocity  = Vector3.zero
        hrp2.AssemblyAngularVelocity = Vector3.zero
    end)
end

LocalPlayer.CharacterAdded:Connect(function()
    if Config.EnableLissajous then
        Config.EnableLissajous = false
        StopLissajousLoop()
        print("[Lissajous] Character respawned — loop deactivated automatically")
    end
end)

print("OK LISSAJOUS LOOP: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// HIPOTROCOIDE (Spirograph) LOOP
--//   r  = R * SpiroRatio   (radio del círculo interno)
--//   k  = (R - r) / r      (ratio de giro)
--//   x(t) = (R - r) · cos(t) + r · cos(k · t)
--//   z(t) = (R - r) · sin(t) - r · sin(k · t)
--//   Tangente:
--//   dx/dt = -(R-r)·sin(t) - r·k·sin(k·t)
--//   dz/dt =  (R-r)·cos(t) - r·k·cos(k·t)
--// ═════════════════════════════════════════════════════════════════════════════

local spiroT            = 0
local spiroCenterCF     = nil
local spiroConn         = nil
local spiroPrevAnchor   = false
local spiroPrevWalkSpd  = 16

function StopSpiroLoop()
    if spiroConn then
        spiroConn:Disconnect()
        spiroConn = nil
    end
    local char = LocalPlayer.Character
    if char then
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = spiroPrevWalkSpd end
    end
    spiroCenterCF = nil
    print("❌ Spirograph Loop STOPPED")
end

function StartSpiroLoop()
    StopSpiroLoop()

    local char = LocalPlayer.Character
    if not char then
        warn("[Spirograph] No character found")
        return
    end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hrp or not hum then
        warn("[Spirograph] HumanoidRootPart or Humanoid not found")
        return
    end

    spiroPrevAnchor  = hrp.Anchored
    spiroPrevWalkSpd = hum.WalkSpeed

    -- Inmovilizar sin anclar — el CFrame se replica a todos los jugadores
    hum.WalkSpeed = 0
    spiroT        = 0

    local lookVec = hrp.CFrame.LookVector
    spiroCenterCF = CFrame.new(hrp.Position)
        * CFrame.Angles(0, math.atan2(lookVec.X, lookVec.Z), 0)

    print("✅ Spirograph Loop STARTED at " .. tostring(hrp.Position))

    spiroConn = RunService.Heartbeat:Connect(function(dt)
        if not Config.EnableSpiro then return end

        local char2 = LocalPlayer.Character
        if not char2 then return end
        local hrp2 = char2:FindFirstChild("HumanoidRootPart")
        if not hrp2 then return end

        spiroT = spiroT + Config.SpiroSpeed * dt

        local R  = Config.SpiroRadius
        local r  = R * Config.SpiroRatio
        local Rr = R - r
        local k  = Rr / r   -- ratio de giro interno
        local t  = spiroT

        local localX = Rr * math.cos(t) + r * math.cos(k * t)
        local localZ = Rr * math.sin(t) - r * math.sin(k * t)
        local tanX   = -Rr * math.sin(t) - r * k * math.sin(k * t)
        local tanZ   =  Rr * math.cos(t) - r * k * math.cos(k * t)

        local right   = spiroCenterCF.RightVector
        local forward = spiroCenterCF.LookVector
        local origin  = spiroCenterCF.Position

        local worldPos = origin + right * localX + forward * localZ
        worldPos = Vector3.new(worldPos.X, origin.Y, worldPos.Z)

        local worldTan = right * tanX + forward * tanZ
        local tanMag   = worldTan.Magnitude

        local newCF
        if tanMag > 0.01 then
            newCF = CFrame.new(worldPos, worldPos + worldTan)
        else
            newCF = CFrame.new(worldPos, worldPos + forward)
        end

        hrp2.CFrame = newCF
        hrp2.AssemblyLinearVelocity  = Vector3.zero
        hrp2.AssemblyAngularVelocity = Vector3.zero
    end)
end

LocalPlayer.CharacterAdded:Connect(function()
    if Config.EnableSpiro then
        Config.EnableSpiro = false
        StopSpiroLoop()
        print("[Spirograph] Character respawned — loop deactivated automatically")
    end
end)

print("OK SPIROGRAPH LOOP: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// SPEED MULTIPLIER (WalkSpeed real, vía hook al sistema interno del juego)
--// ═════════════════════════════════════════════════════════════════════════════
--// Distinto de Teleport Walk: engancha la función real que el juego usa para
--// cambiar la velocidad del personaje (MovementStat.SpeedChange), en vez de
--// mover el personaje a la fuerza con TranslateBy

local MovementStatModule = nil
local originalSpeedChange = nil
local speedMultiplierHooked = false
local inEmote = false
getgenv().YYSpeedApplyRequested = Config.EnableSpeedMultiplier == true

function FindMovementStat()
    if MovementStatModule then return MovementStatModule end
    local ok, gcTable = pcall(getgc, true)
    if not ok or not gcTable then return nil end

    for i = 1, #gcTable do
        local v = gcTable[i]
        if type(v) == "table" then
            local ok2, isValid = pcall(function()
                return rawget(v, "SpeedChange") and type(v.SpeedChange) == "function"
            end)
            if ok2 and isValid then
                MovementStatModule = v
                break
            end
        end
    end

    return MovementStatModule
end

function SetupSpeedMultiplierHook()
    if speedMultiplierHooked then return end
    if not hookfunction then return end

    local movementStat = FindMovementStat()
    if not movementStat then return end

    inEmote = false

    local ok = pcall(function()
        originalSpeedChange = hookfunction(movementStat.SpeedChange, function(self, state, value)
            if state == "Emote" then
                if value ~= nil then
                    -- Emote empieza: aplicar multiplicador si EnableEmoteSpeed está activo
                    inEmote = true
                    inEvadeEmote = true
                    if Config.EnableEmoteSpeed then
                        return originalSpeedChange(self, state, Config.EmoteSpeedMultiplier)
                    end
                else
                    -- Emote termina (value == nil): dejar pasar el valor original
                    inEmote = false
                    inEvadeEmote = false
                    task.spawn(cleanupActiveEmote)
                end
            end
            if state == "State" then
                lastSpeedSelf, lastSpeedState, lastSpeedValue = self, state, value
                if getgenv().YYSpeedRestoreRequested and not Config.EnableSpeedMultiplier then
                    task.defer(function()
                        if getgenv().YYRestoreSpeedMultiplier then
                            getgenv().YYRestoreSpeedMultiplier()
                        end
                    end)
                end
            end
            if (state ~= "State" and state ~= "Emote") or not Config.EnableSpeedMultiplier then
                return originalSpeedChange(self, state, value)
            end
            return originalSpeedChange(self, state, Config.SpeedMultiplierValue)
        end)
    end)

    if ok then
        speedMultiplierHooked = true
        print("✅ SPEED MULTIPLIER: Hook conectado")
        if Config.EnableSpeedMultiplier then
            getgenv().YYSpeedApplyRequested = true
            task.defer(function()
                if getgenv().YYApplySpeedMultiplier then getgenv().YYApplySpeedMultiplier() end
            end)
        end
        if getgenv().YYSpeedRestoreRequested and not Config.EnableSpeedMultiplier then
            task.defer(function()
                if getgenv().YYRestoreSpeedMultiplier then
                    getgenv().YYRestoreSpeedMultiplier()
                end
            end)
        end
    end
end

getgenv().YYApplySpeedMultiplier = function()
    if not Config.EnableSpeedMultiplier or not originalSpeedChange then return false end
    local target = lastSpeedSelf or MovementStatModule
    if not target then return false end
    local ok = pcall(originalSpeedChange, target, "State", Config.SpeedMultiplierValue)
    if ok then getgenv().YYSpeedApplyRequested = false end
    return ok
end

getgenv().YYRestoreSpeedMultiplier = function()
    if not originalSpeedChange or not lastSpeedSelf or lastSpeedState ~= "State" then
        return false
    end
    if lastSpeedValue == nil then
        return false
    end

    local ok = pcall(originalSpeedChange, lastSpeedSelf, lastSpeedState, lastSpeedValue)
    if ok then
        getgenv().YYSpeedRestoreRequested = false
        lastSpeedSelf, lastSpeedState, lastSpeedValue = nil, nil, nil
    end
    return ok
end

task.spawn(function()
    while not speedMultiplierHooked do
        pcall(SetupSpeedMultiplierHook)
        task.wait(0.5)
    end
end)

print("✅ SPEED MULTIPLIER: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// AUTO TRIMP
--// ═════════════════════════════════════════════════════════════════════════════
--// Al tocar el suelo aplica impulso vertical multiplicado. En el aire frena
--// la caída según AutoTrimpFallSpeed para acumular más velocidad al rebotar.

local trimpWasAir = false

function applyAutoTrimp()
    if not Config.EnableAutoTrimp and not Config.EnableTrimpTeleport then return end

    local char = LocalPlayer.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    local humanoid = char:FindFirstChild("Humanoid")
    if not root or not humanoid then return end
    if humanoid.Health <= 0 then return end

    local isAir = humanoid.FloorMaterial == Enum.Material.Air

    pcall(function()
        if isAir then
            -- En el aire: frenar la caída para acumular velocidad
            local vel = root.AssemblyLinearVelocity
            if vel.Y < 0 then
                root.AssemblyLinearVelocity = Vector3.new(
                    vel.X,
                    vel.Y * Config.AutoTrimpFallSpeed,
                    vel.Z
                )
            end
        else
            -- Acaba de tocar el suelo: aplicar impulso
            if trimpWasAir then
                local vel = root.AssemblyLinearVelocity
                local horizontalSpeed = Vector3.new(vel.X, 0, vel.Z).Magnitude
                local impulse = math.max(horizontalSpeed, 20) * Config.AutoTrimpMultiplier
                root.AssemblyLinearVelocity = Vector3.new(
                    vel.X,
                    impulse,
                    vel.Z
                )
            end
        end
    end)

    trimpWasAir = isAir
end

print("OK AUTO TRIMP: Function loaded successfully")

--// ═════════════════════════════════════════════════════════════════════════════
--// MAIN LOOP
--// ═════════════════════════════════════════════════════════════════════════════

print("\nActivating main loop...")

local connection = RunService.Heartbeat:Connect(function()
    pcall(function()
        applyTeleportWalk()
        applyEnhancedJump()
        applyAutoJump()
        applyAutoCrouch()
        applyLayerJumps()
        applyImpulseJump()
        applyGravityModification()
        applyFrontalJump()
        applyRampMultiplier()
        applyBackJump()
        applyVoidEscape()
        applyAutoTrimp()
        applyInfiniteSlide()
    end)
end)

print("✅ LOOP: Main loop started")

print("\n" .. string.rep("=", 80))
print("OK: EVADE v5.2 BETA - FULLY FUNCTIONAL")
print(string.rep("=", 80))

print("\nSUMMARY:")
print("   ✅ Yin Yang v27 Final library loaded")
print("   ✅ UI with 2 tabs (Move + Map)")
print("   ✅ FloatingToggle on all options")
print("   ✅ Frontal Jump ADDED to Move")
print("   ✅ FullBright ADDED to Map")
print("   ✅ Beta with Yellow-Black rainbow")
print("   ✅ Movement functions COMPLETE")

print("\nHOW TO USE:")
print("   1. Go to the MOVE tab for movement features")
print("   2. Go to the MAP tab for map features (FullBright)")
print("   3. Enable 'FullBright' to illuminate the map")
print("   4. FloatingToggles can be moved freely")

print("\n" .. string.rep("=", 80) .. "\n")

--// ═════════════════════════════════════════════════════════════════════════════
--// TAB: ALL — integración estable con primitivas nativas de la librería
--// No crea un contenedor ni un layout paralelo: todos los controles visibles
--// usan directamente Tab:CreateLabel y Tab:CreateButton.
--// ═════════════════════════════════════════════════════════════════════════════
local TabAll = UI:CreateTab("All", "All", "rbxassetid://138091938362842")
local AllStatus
local AllCount

--// Estos controles se crean antes del escaneo para que la pestaña nunca quede
--// completamente vacía aunque el descubrimiento tarde o falle.
TabAll:CreateLabel("ALL • CATÁLOGO VISUAL", "ALL • VISUAL CATALOG", 15)
AllStatus = TabAll:CreateLabel("Preparando catálogo...", "Preparing catalog...", 10)
AllCount = TabAll:CreateLabel("Candidatos: 0", "Candidates: 0", 10)

local AllPage = TabAll.Page
local AllSearch = Instance.new("TextBox")
AllSearch.Name = "AllSearch"
AllSearch.Size = UDim2.new(1, 0, 0, 34)
AllSearch.BackgroundColor3 = Color3.fromRGB(42, 42, 48)
AllSearch.BackgroundTransparency = 0.08
AllSearch.BorderSizePixel = 0
AllSearch.ClearTextOnFocus = false
AllSearch.PlaceholderText = "Buscar nombre o ruta..."
AllSearch.PlaceholderColor3 = Color3.fromRGB(160, 160, 168)
AllSearch.Text = ""
AllSearch.TextColor3 = Color3.fromRGB(240, 240, 242)
AllSearch.Font = Enum.Font.Gotham
AllSearch.TextSize = 11
AllSearch.TextXAlignment = Enum.TextXAlignment.Left
AllSearch.ZIndex = 9
AllSearch.Parent = AllPage
local AllSearchCorner = Instance.new("UICorner")
AllSearchCorner.CornerRadius = UDim.new(0, 7)
AllSearchCorner.Parent = AllSearch
local AllSearchStroke = Instance.new("UIStroke")
AllSearchStroke.Color = Color3.fromRGB(110, 110, 122)
AllSearchStroke.Transparency = 0.35
AllSearchStroke.Thickness = 1
AllSearchStroke.Parent = AllSearch

local AllCandidates = {}
local AllRows = {}
local AllCurrentRoot = nil
local AllDiscoveryRunning = false
local AllDiscoveryToken = 0
local AllMaxDiscoveryNodes = 2200
local AllDiscoveryBatch = 18
local AllDiscoveryWait = 0.035

function allSetStatus(text)
    if AllStatus and AllStatus.Parent then
        AllStatus.Text = tostring(text)
    end
end

function allSetCount(value)
    if AllCount and AllCount.Parent then
        AllCount.Text = "Candidatos: " .. tostring(value)
    end
end

function allLower(value)
    return string.lower(tostring(value or ""))
end

-- Exclusión de prueba: nombres legibles marcados por el usuario en las capturas.
-- Se comparan en minúsculas y solo por nombre exacto; no se inventan patrones.
local AllExcludedNames = {
    defectrebel = true,
    davethebrickbattler = true,
    darkestknight = true,
    cursedkrampus = true,
    combustor = true,
    clark = true,
    catbobosuit = true,
    capeofposeidon = true,
    bunnyjard = true,
    bobo = true,
    anniversaryfireworks = true,
    anniversarycatjard = true,
    duckywaterfall = true,
    frostysnowman = true,
    gardenerjard = true,
    ghost = true,
    ghostlyanchor = true,
    frankenstein = true,
    interloper = true,
    jardclown = true,
    jard = true,
    jardsunderland = true,
    jollysanta = true,
    krampus = true,
    leviathananchor = true,
    lupen = true,
    markv = true,
    masterchief = true,
    mechanicalbugler = true,
    overseer = true,
    pootis = true,
    poseidon = true,
    retrosunset = true,
    seal = true,
    sharkbobo = true,
    spy = true,
    summerjard = true,
    thecorrupted = true,
    thehidden = true,
    trooper = true,
    vanguard = true,
    weepingbobo = true,
    werewolf = true,
    woodennutcracker = true,
    workerelf = true,
    zombie = true,
    zombieskeleton = true,
}

function allIsExcludedName(name)
    return AllExcludedNames[allLower(name)] == true
end

function allPathOf(instance)
    local parts = {}
    local current = instance
    while current and current ~= game do
        parts[#parts + 1] = current.Name
        current = current.Parent
    end
    local result = {}
    for index = #parts, 1, -1 do
        result[#result + 1] = parts[index]
    end
    return table.concat(result, ".")
end

function allIsCosmeticsPath(instance)
    local path = allLower(allPathOf(instance))
    return string.find(path, ".cosmetics", 1, true) ~= nil
        or allLower(instance.Name) == "cosmetics"
end

function allMatchesSearch(candidate)
    if allIsExcludedName(candidate.name) then return false end
    local query = allLower(AllSearch.Text)
    if query == "" then return true end
    return string.find(allLower(candidate.name), query, 1, true) ~= nil
        or string.find(allLower(candidate.path), query, 1, true) ~= nil
end

function allClearRows()
    for _, row in ipairs(AllRows) do
        pcall(function() row:Destroy() end)
    end
    AllRows = {}
end

local bodyNames = {
    ["head"] = true, ["torso"] = true, ["left arm"] = true, ["right arm"] = true,
    ["left leg"] = true, ["right leg"] = true, ["uppertorso"] = true,
    ["lowertorso"] = true, ["lefthand"] = true, ["righthand"] = true,
    ["leftupperarm"] = true, ["rightupperarm"] = true,
    ["leftlowerarm"] = true, ["rightlowerarm"] = true,
    ["leftupperleg"] = true, ["rightupperleg"] = true,
    ["leftlowerleg"] = true, ["rightlowerleg"] = true,
    ["leftfoot"] = true, ["rightfoot"] = true,
    ["humanoidrootpart"] = true, ["torsorot"] = true, ["armrot"] = true,
}

function allIsReferenceBodyPart(instance)
    if not instance:IsA("BasePart") then return false end
    local name = allLower(instance.Name)
    return bodyNames[name]
        or string.find(name, "body", 1, true) ~= nil
        or string.find(name, "limb", 1, true) ~= nil
end

function allPrepareVisualModel(model)
    local hidden = 0
    local parts = 0
    local vfx = 0
    local sounds = 0
    for _, instance in ipairs(model:GetDescendants()) do
        if instance:IsA("BasePart") then
            parts = parts + 1
            instance.CanCollide = false
            instance.CanTouch = false
            instance.CanQuery = false
            instance.Massless = true
            if allIsReferenceBodyPart(instance) then
                instance.Transparency = 1
                hidden = hidden + 1
            end
        elseif instance:IsA("ParticleEmitter")
            or instance:IsA("Beam")
            or instance:IsA("Trail")
            or instance:IsA("Highlight")
            or instance:IsA("PointLight")
            or instance:IsA("SpotLight")
            or instance:IsA("SurfaceLight") then
            vfx = vfx + 1
        elseif instance:IsA("Sound") then
            sounds = sounds + 1
        end
    end
    return hidden, parts, vfx, sounds
end

function allRepairInternalJoints(model)
    local repaired = 0
    for _, joint in ipairs(model:GetDescendants()) do
        if joint:IsA("Motor6D") or joint:IsA("Weld") then
            local p0 = joint.Part0
            local p1 = joint.Part1
            if p0 and not p0:IsDescendantOf(model) then
                local replacement = model:FindFirstChild(p0.Name, true)
                if replacement and replacement:IsA("BasePart") then
                    pcall(function() joint.Part0 = replacement end)
                    repaired = repaired + 1
                end
            end
            if p1 and not p1:IsDescendantOf(model) then
                local replacement = model:FindFirstChild(p1.Name, true)
                if replacement and replacement:IsA("BasePart") then
                    pcall(function() joint.Part1 = replacement end)
                    repaired = repaired + 1
                end
            end
        end
    end
    return repaired
end

function allGetTargetRig()
    local rigs = Workspace:FindFirstChild("Rigs")
    local rig = rigs and rigs:FindFirstChild(LocalPlayer.Name)
    if not rig then rig = Workspace:FindFirstChild(LocalPlayer.Name) end
    if not rig then return nil, nil end
    local root = rig:FindFirstChild("HumanoidRootPart")
        or rig:FindFirstChild("Torso")
        or rig:FindFirstChild("UpperTorso")
        or rig.PrimaryPart
    return rig, root
end

function allRemoveCurrent()
    if AllCurrentRoot then
        pcall(function() AllCurrentRoot:Destroy() end)
        AllCurrentRoot = nil
    end
    local existing = Workspace:FindFirstChild("__YY_AllVisualCandidates")
    if existing then pcall(function() existing:Destroy() end) end
    allSetStatus("Todos los candidatos fueron retirados")
end

function allTestCandidate(candidate)
    allRemoveCurrent()
    local sourceModule = candidate.module
    if not sourceModule or not sourceModule.Parent then
        allSetStatus(candidate.name .. " ya no está disponible")
        return
    end
    local rig, targetRoot = allGetTargetRig()
    if not rig or not targetRoot then
        allSetStatus("No se encontró el rig visual o su root")
        return
    end

    local characterModel = sourceModule:FindFirstChild("CharacterClassic")
        or sourceModule:FindFirstChild("Character")
    local sourceAura = sourceModule:FindFirstChild("Aura")
    local useModel = candidate.kind == "model" and characterModel and characterModel:IsA("Model")
    local useAura = candidate.kind == "aura"
        and sourceAura
        and (sourceAura:IsA("BasePart") or sourceAura:IsA("Model"))
    if not useModel and not useAura then
        allSetStatus(candidate.name .. " ya no tiene un formato visual compatible")
        return
    end

    local container = Workspace:FindFirstChild("__YY_AllVisualCandidates")
    if not container then
        container = Instance.new("Folder")
        container.Name = "__YY_AllVisualCandidates"
        container.Parent = Workspace
    end

    local ok, clone = pcall(function()
        if useModel then
            return characterModel:Clone()
        end
        if sourceAura:IsA("BasePart") then
            local wrapper = Instance.new("Model")
            local auraClone = sourceAura:Clone()
            auraClone.Parent = wrapper
            wrapper.PrimaryPart = auraClone
            return wrapper
        end
        return sourceAura:Clone()
    end)
    if not ok or not clone then
        allSetStatus("No se pudo clonar " .. candidate.name)
        return
    end

    clone.Name = candidate.name .. (useModel and "_VisualOnly" or "_AuraVisualOnly")
    clone.Parent = container
    local hidden, parts, vfx, sounds = allPrepareVisualModel(clone)
    if useAura then
        local carrier = clone:FindFirstChild("Aura", true)
        if carrier and carrier:IsA("BasePart") then
            carrier.Transparency = 1
        end
    end
    local repaired = allRepairInternalJoints(clone)
    local cloneRoot = clone:FindFirstChild("HumanoidRootPart", true)
        or clone:FindFirstChild("Torso", true)
        or clone.PrimaryPart
    if not cloneRoot or not cloneRoot:IsA("BasePart") then
        pcall(function() clone:Destroy() end)
        allSetStatus(candidate.name .. " no tiene root part")
        return
    end

    clone.PrimaryPart = cloneRoot
    pcall(function() clone:PivotTo(targetRoot.CFrame) end)
    local weld = Instance.new("WeldConstraint")
    weld.Name = "__YY_AllFollowTarget"
    weld.Part0 = cloneRoot
    weld.Part1 = targetRoot
    weld.Parent = cloneRoot
    AllCurrentRoot = container
    allSetStatus(candidate.name .. " activo • cuerpo oculto " .. tostring(hidden) .. "/" .. tostring(parts) .. " • VFX " .. tostring(vfx) .. " • joints " .. tostring(repaired) .. " • sonidos " .. tostring(sounds))
end

function allRebuildRows()
    allClearRows()
    local shown = 0
    for _, candidate in ipairs(AllCandidates) do
        if allMatchesSearch(candidate) then
            shown = shown + 1
            local kindLabel = candidate.kind == "aura" and "AURA" or "MODEL"
            local row = TabAll:CreateButton(
                candidate.name .. "  •  " .. kindLabel,
                candidate.name .. "  •  " .. kindLabel,
                function() allTestCandidate(candidate) end
            )
            AllRows[#AllRows + 1] = row
        end
    end
    allSetStatus(tostring(shown) .. " visibles • " .. tostring(#AllCandidates) .. " candidatos descubiertos")
end

function allDiscoverCandidates()
    if AllDiscoveryRunning then return end
    AllDiscoveryRunning = true
    AllDiscoveryToken = AllDiscoveryToken + 1
    local token = AllDiscoveryToken
    allClearRows()
    AllCandidates = {}
    allSetCount(0)
    allSetStatus("Descubriendo módulos bajo Cosmetics...")

    task.spawn(function()
        local items = ReplicatedStorage:FindFirstChild("Items")
        local roots = {}
        if items then
            local baseItems = items:FindFirstChild("BaseItems")
            local baseCosmetics = baseItems and baseItems:FindFirstChild("Cosmetics")
            if baseCosmetics then roots[#roots + 1] = baseCosmetics end
            local itemPacks = items:FindFirstChild("ItemPacks")
            if itemPacks then roots[#roots + 1] = itemPacks end
        end

        local queue = {}
        for _, root in ipairs(roots) do queue[#queue + 1] = root end
        local queueIndex = 1
        local visited = {}
        local seenModules = {}
        local scanned = 0
        local limitReached = false

        while queueIndex <= #queue and scanned < AllMaxDiscoveryNodes and token == AllDiscoveryToken do
            local batch = 0
            while batch < AllDiscoveryBatch
                and queueIndex <= #queue
                and scanned < AllMaxDiscoveryNodes
                and token == AllDiscoveryToken do
                local node = queue[queueIndex]
                queueIndex = queueIndex + 1
                batch = batch + 1
                scanned = scanned + 1
                if node and not visited[node] then
                    visited[node] = true
                    if node:IsA("ModuleScript") and allIsCosmeticsPath(node)
                        and not allIsExcludedName(node.Name)
                        and not seenModules[node] then
                        local characterModel = node:FindFirstChild("CharacterClassic")
                            or node:FindFirstChild("Character")
                        local auraObject = node:FindFirstChild("Aura")
                        local candidateKind
                        if characterModel and characterModel:IsA("Model") then
                            candidateKind = "model"
                        elseif auraObject
                            and (auraObject:IsA("BasePart") or auraObject:IsA("Model")) then
                            candidateKind = "aura"
                        end
                        if candidateKind then
                            seenModules[node] = true
                            AllCandidates[#AllCandidates + 1] = {
                                name = node.Name,
                                path = allPathOf(node),
                                module = node,
                                kind = candidateKind,
                            }
                        end
                    end
                    if node:IsA("Folder") then
                        local nodeName = allLower(node.Name)
                        local skipFolder = nodeName == "emotes"
                            or nodeName == "boomboxes"
                            or nodeName == "loadout"
                            or nodeName == "carryanimations"
                        if not skipFolder then
                            for _, child in ipairs(node:GetChildren()) do
                                if child:IsA("Folder") or child:IsA("ModuleScript") then
                                    queue[#queue + 1] = child
                                end
                            end
                        end
                    end
                end
            end
            allSetCount(#AllCandidates)
            allSetStatus("Escaneando " .. tostring(scanned) .. " nodos • " .. tostring(#AllCandidates) .. " candidatos")
            task.wait(AllDiscoveryWait)
        end

        if scanned >= AllMaxDiscoveryNodes and queueIndex <= #queue then
            limitReached = true
        end
        table.sort(AllCandidates, function(a, b)
            local al = allLower(a.name)
            local bl = allLower(b.name)
            if al == bl then return allLower(a.path) < allLower(b.path) end
            return al < bl
        end)
        AllDiscoveryRunning = false
        allSetCount(#AllCandidates)
        allRebuildRows()
        if limitReached then
            allSetStatus("Límite alcanzado • " .. tostring(#AllCandidates) .. " candidatos • pulsa REFRESH")
        else
            allSetStatus("Terminado • " .. tostring(#AllCandidates) .. " candidatos • busca o desplázate")
        end
    end)
end

local AllRefresh = TabAll:CreateButton("↻  ACTUALIZAR LISTA", "↻  REFRESH LIST", allDiscoverCandidates)
local AllRemove = TabAll:CreateButton("×  REMOVE ALL", "×  REMOVE ALL", allRemoveCurrent)
AllRefresh.Name = "AllRefresh"
AllRemove.Name = "AllRemove"

AllSearch:GetPropertyChangedSignal("Text"):Connect(function()
    if not AllDiscoveryRunning then allRebuildRows() end
end)

local oldAllVisuals = Workspace:FindFirstChild("__YY_AllVisualCandidates")
if oldAllVisuals then pcall(function() oldAllVisuals:Destroy() end) end

local AllInitOk, AllInitError = pcall(allDiscoverCandidates)
if not AllInitOk then
    allSetStatus("Error de All: " .. tostring(AllInitError))
    warn("[All] Error al iniciar el descubrimiento: " .. tostring(AllInitError))
end

--// Normaliza todo el texto creado por las pestañas con el idioma detectado.
refreshScriptLanguage()

return UI
